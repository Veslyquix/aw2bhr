{
 "_comment": [
  "Functions that ARE compiler output and should match, but have not yet.",
  "They are held out of the work queue so waves stop rediscovering them,",
  "and they are deliberately NOT counted as progress anywhere: a parked",
  "function is still assembly, the ROM is still built from its asm/ unit,",
  "and the C in work/<fn>/ does not go into src/decomp/.",
  "",
  "This is a different claim from data/asm-resident.json. That file says",
  "'no C will ever match this, because a compiler did not write it'.",
  "This file says 'C should match this and ours does not yet'. Do not put",
  "an entry here in asm-resident.json to get it out of the queue -- that",
  "file is the evidence base for what hand-written assembly looks like,",
  "and a wrong entry poisons every later judgement made from it.",
  "",
  "An entry needs three things: the best percentage reached, the EXACT",
  "remaining difference, and the axes already ruled out. Without the",
  "ruled-out list the next wave repeats the same attempts -- which is the",
  "specific failure this file exists to prevent.",
  "",
  "TWO SHAPES ARE ACCEPTED and both are read as opaque by index_functions.",
  "Older entries are a single prose string. PREFER THE OBJECT FORM for new",
  "entries -- sub_0804FA2C is the worked example -- because it makes the",
  "ruled-out list a real list instead of a sentence buried in prose:",
  "  bytes, best, remaining_diff, why_it_is_close,",
  "  axes_ruled_out[], settled_by_this_attempt[], reason",
  "`settled_by_this_attempt` is the one that pays: it names derivations the",
  "attempt PROVED, so an unpark does not re-litigate them.",
  "",
  "Before parking anything, check the toolchain axis. The ROM was built",
  "with two compilers and more than one flag set, and three functions were",
  "parked as source-exhausted across two waves and permuter runs when the",
  "source was never the axis. See data/compiler-overrides.json and the",
  "'Per-file compiler and flags' section of docs/agbcc-codegen.md.",
  "",
  "Unparking is the goal. Re-run `python tools/trymatch.py <fn>` against a",
  "parked entry whenever a new codegen rule lands; the draft is kept at",
  "work/<fn>/<fn>.c and best.c holds the closest attempt.",
  "",
  "THE .rodata WALL IS GONE (wave 18, W18-B). Twelve entries here used to say",
  "'re-test when the split can express a .rodata word'. It can now. agbcc parks",
  "a -fforce-addr address constant in the compiling unit's own .rodata, and in",
  "this repo those same bytes also arrive as incbin'd ROM data in data/rodata.s",
  "or data/data.s -- so a byte-exact honest draft could not be promoted, because",
  "its word had nowhere to live and the blob was still supplying it.",
  "tools/split_rodata.py now splits those blobs per symbol into generated pieces",
  "under build/rodata (reading data/, never modifying it, exactly as",
  "split_asm.py treats asm/), and gen_lds.py interleaves a promoted unit's own",
  ".rodata between the pieces at the address the original build used. Declare",
  "the words a unit owns with a `rodata` list in data/promoted.json.",
  "ALL TWELVE were unparked this way -- sub_080081E0, sub_080083E0,",
  "sub_0800977C, sub_0804E8F0, sub_0804FE10 and the seven-member sub_0805CA60",
  "group, 3,368 bytes -- every one from a draft that names the global DIRECTLY,",
  "with both builds reproducing the SHA1. See docs/agbcc-codegen.md.",
  "",
  "COROLLARY, and it matters for what you keep in work/: when the honest",
  "spelling is byte-exact and only its relocation differs, THAT draft is the",
  "deliverable and the c_local workaround is not. Keep the honest one as",
  "work/<fn>/<fn>.c. Three functions were held parked for several waves by",
  "grinding on the workaround's register allocation when the honest draft had",
  "been byte-exact the whole time.",
  "",
  "DO NOT relax trymatch's reloc_equivalent to bless a .rodata word without",
  "checking the placement exists. trymatch judges one unit; the split build",
  "links all of them. A promoted unit whose .rodata the linker script does not",
  "place either loses the word or shifts every address after it, and because",
  "promote.py acts on trymatch's verdict the failure lands in the build with",
  "functions already moved. Placement first, verifier second.",
  "",
  "FORMATTING: this file is written with json.dump(..., indent=1). Dump it with any other indent and every line churns in the diff, which buries the one entry you added -- wave 26 did exactly that and had to redo it. LINE ENDINGS TOO, and the two data files DISAGREE: this file is CRLF, data/asm-resident.json is LF. json.dump writes '\n', so open this one with newline='\r\n' -- writing LF here churns all 394 lines exactly the way a wrong indent does, and hides the one entry you changed. Wave 28 hit it and caught it only by running `git diff --ignore-all-space --stat` when the diff looked absurd. Do that check before committing either file."
 ],
 "functions": {
  "sub_0802E010": {
   "bytes": 288,
   "best": "284 bytes (-4), 24.3% identical",
   "remaining_diff": "Two spots, 2 bytes each, and NOTHING ELSE -- the prologue, the clamp, the clear loop, all of block 3, both loop-bottom tests and the tail are byte-exact. (1) The fill loop's bound: the ROM narrows n >> 1 to u16 at the USE, two basic blocks after the definition (`lsls r0, r6, #0x10; lsrs r0, r0, #0x10; cmp r3, r0`), and every spelling collapses that pair. (2) Block 4's inner loop: the ROM keeps 0x100 in a HIGH register (`mov r9, r1` in the preheader, `mov r7, r9` per iteration) and computes the index `adds r1, r3, r1` (i first); the candidate keeps 0x100 in low r7, loses the `mov`, and emits `adds r1, r1, r3`. Also expected and NOT a difference: the .text pool word relocates against `.rodata` rather than gUnknown_08090C28, which is the wave-21 same-address case and needs rodata: [\"0x08090C28\"] at promotion.",
   "why_it_is_close": "The shape is fully derived and the two residuals are both 'the ROM carries one more live value than this C', not two independent statement errors. The hard part is (1) and it is a genuine contradiction rather than an unexplored spelling: the ROM's own `lsrs r6, r1, #1` proves the shift operand is known non-negative (an arithmetic shift would be `asrs`), so `nonzero_bits` demonstrably holds for that pseudo -- yet the SAME pseudo's u16 narrowing survives at the use, which `nonzero_bits` should fold. The usual escape (a second, possibly-negative assignment poisoning reg_nonzero_bits) is ruled out by the register evidence: r6 is reused for 0x5000 inside block 4, so the bound's pseudo is dead by then and cannot have a later set. Whatever produces that narrowing is outside the nonzero_bits story, so the next wave should look for a different SOURCE SHAPE for the bound rather than another cast spelling.",
   "axes_ruled_out": [
    "`u16 half` local -- folds the narrowing entirely AND finalises the bound into its register before the intervening clear loop; 14.6%, -4",
    "`int half` + `(u16)half` at the use -- leaves the bare copy `adds r0, r6, #0`, one insn short; 24.3%, -4. This is the kept draft and it REFUTES docs/agbcc-codegen.md's 'int local + cast at the use holds the shift pair' for a COMPARISON use (true only for an arithmetic use); the chapter has been amended",
    "`int n` -- makes the clamp signed (`ble`); the ROM has `bls`. Probed",
    "unsigned n -- would keep `bls` but breaks the `asrs` in block 4's bound, which needs signed int arithmetic",
    "bound inlined in the loop condition -- LICM puts the `lsrs` after the clear loop; the ROM has it before",
    "all three groupings of block 4's index (`i + j * 2 + 0x10`, `i + 0x10 + j * 2`, `i + (j * 2 + 0x10)`) compile BYTE-IDENTICALLY, so the `adds` operand order is downstream of register pressure, not of source grouping -- do not respell it",
    "declaring gUnknown_08090C28 as a real `u8 **` global (wave-39 lead's hypothesis) -- probed and REFUTED BY CONSTRUCTION: it emits FOUR loads (`ldr/ldr/ldr/ldrb`) where the ROM has three, because agbcc adds its own `.LC` force-addr level on top of the declared global. It is bigger, not smaller"
   ],
   "settled_by_this_attempt": [
    "gUnknown_08090C28 and gUnknown_08090C2C are NOT globals: both ROM words hold 0x03003F68 (= &gUnknown_03003F68), one -fforce-addr copy per function, which is why one address wears two invented names. Naming gUnknown_03003F68 honestly reproduces the ROM's three-load chain exactly. Recorded in include/unknown-globals.h beside the wave-21 note",
    "`u8 n` is settled: it is the only width giving all three of the ROM's readouts at once -- unsigned clamp (`bls`), logical `lsrs` for n >> 1, and arithmetic `asrs` for (m - n) >> 1",
    "the whole of block 3 is settled, including the re-read of ((u8 *)gUnknown_03003F68)[1] on every inner iteration for both the guard and the bound, and the `j * 2 + i` operand order",
    "the +0x20 store displacement, the two separately-materialised constants 0x100 and 0x5000, and the value expression `i + (j + 0x100) + 0x5000` are all byte-exact",
    "'first difference at +0x8' is NOT an opening error: +0x8 is a pc-relative `ldr`, and EVERY pc-relative load in the function differs by exactly 4 because the candidate is 4 bytes short. The three opening loads, `mov r8, r1` and `mov sb, r0` are unchanged context in the diff"
   ],
   "reason": "One shape, two functions. sub_0802E130 is this function with gUnknown_08499578 for gUnknown_0849957C and sub_08013AEC for sub_08013AFC; its trymatch diff is character-for-character identical apart from those symbols. A future wave gets BOTH from one derivation -- do not work them separately."
  },
  "sub_0802E130": {
   "bytes": 288,
   "best": "284 bytes (-4), 24.3% identical",
   "remaining_diff": "Character-for-character the same diff as sub_0802E010, apart from the three substituted symbols (gUnknown_08499578, sub_08013AEC, and its own force-addr .rodata word at 0x08090C2C). See that entry for the two 2-byte residuals.",
   "why_it_is_close": "It is sub_0802E010 with three substitutions and no other difference; the identical diffs are the confirmation that the pair is one shape. It cannot fall until the first one does, and it falls automatically when it does.",
   "axes_ruled_out": [
    "everything listed under sub_0802E010 -- the two functions compile to the same instruction stream, so no axis is worth re-testing here"
   ],
   "settled_by_this_attempt": [
    "the pair reading itself: 139 = 139 instructions, and the two candidates' diffs match character-for-character apart from the substituted symbols",
    "0x08090C2C is this function's own -fforce-addr copy of &gUnknown_03003F68, not a second global; promotion needs rodata: [\"0x08090C2C\"]"
   ],
   "reason": "Derived from sub_0802E010 by substitution, as intended. Re-run it unchanged when that function closes."
  },
  "sub_0801A6C0": "98.4% -- 64 bytes, size-exact, ONE byte differs at +0x2a. The original stores `strh r2, [r1, #0]`; every candidate produces `strh r3, [r1, #0]`. Both registers hold 0 at that point (r2 was just zeroed and stored to [r1, #4], r3 likewise), so this is a pure register-allocation tie-break with no source-level lever on it -- the instruction selection, the order and every other byte are already correct. Ruled out: the source axis, exhaustively, across two waves plus decomp-permuter runs; and the toolchain axis -- old_agbcc and dropping -fforce-addr were both tried after the wave-10 two-compiler finding and neither moves this byte. Not every allocation near-miss is a flag problem. Worth re-testing whenever a register-allocation rule lands in docs/agbcc-codegen.md. WAVE 28 (W28-B), unit-level oracle checked: `--unit` runs (single-function unit) and still fails by ONE byte at +0x2a. Static-helper axis ruled out structurally. The residual is not at a `bl` and is not allocation noise either -- it is a constant-materialisation fact and it names its own fix. Original: `movs r2, #0; str r2, [r1, #4]; strh r2, [r1, #0]`, i.e. ONE zero used for both stores. Candidate: `... strh r3, [r1, #0]`, i.e. a SECOND zero materialised for the halfword store. The source writes both fields from one zero -- one local, or one statement -- where the draft has two independent literal 0s. Of the five this is the closest to closable and the cheapest to try.",
  "sub_08022618": "392 of 400 bytes, 8 SHORT (wave 15, C). Straight-line, 2 `.LC` pool words (gUnknown_080909A8 -> &gUnknown_08499580, gUnknown_080909AC -> &gUnknown_085D5ABC), both spelled with A2's `pp = &<word>` and both relocation-correct; `pp` stays in sl as the ROM has it. Every statement, the || chain, the four 2x2 tile stores and the two Div clamps are identified. The remaining 8 bytes are zero-extensions agbcc elides and the ROM keeps: `id`'s store truncation (`ldrb; lsls #0x10; lsrs #0x10`) is folded away by combine's nonzero_bits. The SAME effect on `ax`/`by` was worth 16 bytes and IS fixed, by spelling them `int` with an explicit `(u16)` cast at each use instead of `u16` locals -- see docs/agbcc-codegen.md. `int id` with `(u16)`/`(s16)` casts fixes id's 4 bytes and re-breaks ax/by, so the two want different spellings of one idea. Ruled out: old_agbcc (identical size); ax/by as dead entry parameters (identical output); 300 s of decomp-permuter from this draft (best 31.0%, no size improvement). Draft in work/sub_08022618/sub_08022618.c.",
  "sub_08042998": "444 of 472 bytes, 28 SHORT (wave 15, C). Straight-line, 2 `.LC` pool words (gUnknown_08091364 -> &gUnknown_030040D8, gUnknown_08091368 -> &gUnknown_085D5ABC) needing THREE different read-site spellings in one function (`**pp`, `w = *pp` then `*w`, `(*pp2)[i]`). Semantics complete: the bitfield model is struct Unk08499594's, which was derived from this function in an earlier wave. 12 of the 28 bytes are one thing -- the ROM emits both arms of the unk04_7 clamp in full, with two `.word 0xFFFFF87F` pool words, because its `cap` arm RE-LOADS `ldrb r0, [r0, #0xb]` rather than reusing the compare's value; agbcc CSEs that reload whether the field is read inline or through a local, so the two arms converge on the same registers and cross-jumping merges them. 4 more: gUnknown_08091368's `pp2` is const-propagated (two uses is under the threshold) so the second high register is never pushed. Ruled out: old_agbcc (identical size). Draft in work/sub_08042998/sub_08042998.c.",
  "sub_08058A2C": "400 of 392 bytes, 8 LONG (wave 15, C). Straight-line, 1 `.LC` pool word (gUnknown_0816D948 -> &gUnknown_030013B0, in the previously undocumented 0x0816D938-0x0816D9BC pool block). Semantics and the whole tail are exact. The 8 bytes are three separate allocation facts: (a) +2, `pp` is const-propagated at its second use so the pool word is emitted twice and `mov sl, r1` never appears -- TWO USES OF A `pp = &<word>` LOCAL IS NOT ENOUGH to keep it live, which is a boundary on wave-13 A2's `c_local` that was not previously recorded (sub_08022618 keeps the same idiom on four uses); (b) +4, the 0x417A pool constant lands in r8 instead of r4 and pays a `mov` at each of its two uses, downstream of (a); (c) +2, the `n` local is allocated to r1 and copied into r0 for __divsi3. Ruled out: old_agbcc (identical size); 300 s of decomp-permuter (best 75.8%, still +8); five spellings of the pool-word access. Draft in work/sub_08058A2C/sub_08058A2C.c.",
  "sub_0808B91C": "99.4% -- 496 bytes, size-exact, THREE bytes differ and they are one register-allocation choice: `v` (the final float result) is allocated r1 and the ROM allocates it r4, so `adds r4, r0, #0` and its two `adds r0, r4, #0` readers come out on r1. Every other instruction, every pool word and every relocation match. gcc's REG_ALLOC_ORDER offers the call-clobbered registers first and `v` crosses no call, so r1 is taken before r4 is considered; making `v` cross a call (`v = -v;` instead of `return -v;`) reshapes the tail and drops the whole function to 75.2%.\n\nThis is the tree's FIRST soft-float function, a cosf: range-reduce by whole pi, cosine Taylor series in r*r to the r^16 term, negate on odd n. Everything it established is in the 'Soft float' chapter of docs/agbcc-codegen.md and matters more than the three bytes -- double word order in registers is HIGH FIRST (FPA layout, so `ldr r2,=0x400921FB; ldr r3,=0x54442D18` is pi and not 9e97), every __*df3 comes from an UNSUFFIXED decimal constant promoting a float operand rather than from double source, `t - C` emits __subdf3 so an alternating series needs explicit negative literals with `+`, and the coefficients are 13-significant-digit hand-typed decimals that 1.0/24.0 does not reach.\n\nRuled out. TOOLCHAIN: default, old_agbcc without -fprologue-bugfix, no -fforce-addr, and both together all produce the IDENTICAL 99.4% and the same three bytes (tested by monkeypatching agbenv._OVERRIDES, leaving data/compiler-overrides.json alone). SOURCE: two 300 s decomp-permuter runs plus hand sweeps over the return form (`v = -v`, `?:`, explicit else, `n % 2`, `0.0f - v`, `n & 1` hoisted to a local), local declaration order, binding the quotient to a `double`, wrapping the tail and the return in their own do/while, and reusing the parameter for the square as well as the reduced value (+12 bytes: one long live range where the ROM has two that share r4).\n\nNOTE on the draft: work/sub_0808B91C/sub_0808B91C.c carries a `do { } while (0)` around the two range-reduction statements. It is a diagnostic, not a claim about the source. Without it the file scores 97.6% with twelve differing bytes -- `x` lands in r6 and the x/pi quotient takes r4:r5 where the ROM has x in r4 and the quotient in r5:r6. The wrapper flips that because gcc weights allocno priority by LOOP DEPTH, so a zero-trip loop promotes everything inside it above everything outside; the original was more likely written through a macro. A cleaner way to raise x's priority would close nine of the twelve bytes and is the thing to look for first on any re-attempt. WAVE 28 (W28-B), unit-level oracle checked: `--unit` runs (the unit is this function alone) and still fails, 3 of 496 bytes at +0x17e. THE STATIC-HELPER AXIS IS RULED OUT STRUCTURALLY -- a single-function unit has no sibling to call, so no call of its can lose a relocation. The residual is NOT at a `bl`: it is `adds r4, r0, #0` where the candidate has `adds r1, r0, #0`, on the float that __truncdfsf2 returns, plus the two later reads of it. The ROM parks that temp in a CALLEE-SAVED register across the `movs r0,#1; ands r0,r7` sign test; the candidate leaves it in a scratch. This is soft-float register allocation, the class the Soft float chapter's zero-trip do/while lever exists for.",
  "sub_08070F44": {
   "bytes": 104,
   "best": "104 bytes (size EXACT), 68.3% identical UNDER old_agbcc, first difference at +0x2. The 56.7% previously recorded here was measured under the DEFAULT compiler, which is the wrong configuration for this block -- see data/compiler-overrides.json, where its neighbour sub_08070BAC now matches byte-for-byte under old_agbcc with no source change.",
   "remaining_diff": "One register rotation, unchanged in kind by the compiler fix. The ROM copies the parameter into r1 (`adds r1, r0, #0`) and the candidate into r2, so `chan`, the `<<24` temp and `left` come out as r2/r3/r1 against the ROM's r1/r2/r3 and almost every later byte renumbers. 33 of 104 bytes. The only instruction-count difference is a consequence of the same fact: the ROM emits `adds r0, r2, #0` ahead of `muls r0, r3` because thumb `muls` is two-operand and its operands land where a copy is needed.",
   "why_it_is_close": "Every instruction is the right instruction in the right order, the block layout including agbcc's cross-jump of the two `envelopeGoal` stores is exact, and the size is exact. This is an allocno-priority near-tie: gcc 2.x local-alloc orders by roughly log2(n_refs)*freq/live_length, and the pointer parameter (many refs, function-long range) ties with the short-lived `left` to within a few percent. The lever is live-range length, not expression spelling.",
   "axes_ruled_out": [
    "COMPILER: old_agbcc is RIGHT for this function and is now the configuration everything below was measured under. It moves 56.7% -> 68.3% on the flag alone with the source untouched. -O1 (the wave-47 flash-library finding) is strictly WORSE here, 19.2%, so the sound and flash libraries are different configurations; -fno-force-addr changes nothing; old_agbcc+-O1 is worse still (10.6%). Do NOT add a compiler-overrides.json entry until it matches -- an override that changes no verdict is unfalsifiable.",
    "Local types and declaration order, RE-MEASURED under old_agbcc because every earlier negative for this function was recorded under the wrong compiler: u8/u8, u8/u8 declared in the other order, u16/u16 and u32/u32 all leave `chan` in r2 (68.3%, 68.3%, 68.3%, 67.3%); int and s32 locals break the shape outright (96 bytes, 2.9%). The declaration-order and local-type axis is dead in BOTH configurations.",
    "decomp-permuter, 300 s x 4 threads, re-run under old_agbcc after fixing tools/permute.py (it was calling agbenv.flags() WITHOUT the function name, so every previous run on this function searched against the default toolchain). Best 73.1%, no byte-level match. The earlier 59.6% figure was scored under the wrong compiler and should not be quoted. NOTE: work/sub_08070F44/permuter left best.c holding that 73.1% candidate -- check its body before treating the score as real (wave 46).",
    "flattening the four arms (wave 35) -- +12 bytes, the ROM shares one clamp block",
    "a single common `x = sum; if (x > 15) x = 15;` -- collapses the two stores the ROM keeps",
    "binding chan->unk06 to a local before the multiply -- no allocation change, and it turns the tail `asrs` into `lsrs`, which is wrong"
   ],
   "settled_by_this_attempt": [
    "RESIDUAL 1 OF THE WAVE-32 PARK IS SOLVED: rightVolume and leftVolume at +0x02/+0x03 are `volatile u8`. The qualifier is the only thing that reproduces the `ldrb; lsls #0x18; lsrs #0x18` round trip, CSE sharing that `<<24` value for the halving (`lsrs r0, r2, #0x19`), and the function re-reading both bytes for the sum. Wave 32's bitfield guess and wave 35's u8-local test are both refuted by it. Reproduced instruction-for-instruction by compile_probe; carved into struct CgbChannel with the evidence.",
    "RESIDUAL 2 IS CLOSED with wave 35's own unapplied fix: `(u32)(chan->leftVolume + chan->rightVolume) >> 4` gives the ROM's `lsrs` where the plain `>> 4` on two u8 members gives `asrs`.",
    "The tail is `chan->pan = chan->panMask & chan->pan;` and NOT `chan->pan &= chan->panMask;` -- the ROM loads +0x1c before +0x1b.",
    "The final scale is `(x + 15) >> 4`, not `x / 16`: `adds r0,#0xf; asrs r0,r0,#4` is an unconditional bias, and a real signed divide costs the sign-correction sequence.",
    "The goto shape is forced, not stylistic: both clamped arms branch forward PAST the 0xFF block to one shared clamp, so the 0xFF block must sit between them and it."
   ],
   "reason": "Register allocation, not source semantics and not the toolchain. The compiler axis is now SETTLED (old_agbcc) and closed 11.6 points of it; the type model, the control flow and the block layout are all confirmed by the diff staying silent on them. What is left is an allocno-priority near-tie between `chan` (many refs, function-long range) and `left` (few refs, short range), and no source-level lever has been found in three waves. Needs a reader of gcc 2.9 local-alloc priority, not another spelling."
  },
  "sub_08070D98": {
   "bytes": 180,
   "best": "180 bytes (size EXACT), 10.6% identical",
   "remaining_diff": "The ROM puts `track` in r2 and the volume accumulator in r3; the candidate does the opposite, which renumbers every register in the function and so diffs almost every byte. Three knock-on effects are consequences of that one fact and not separate problems: the ROM's extra `adds r0,r3,#0` copy before the first `muls`, the swapped multiply operand registers in the pitch block, and `adds r1,r0,r1` rather than `adds r1,r1,r0` for the final `+ pitX`.",
   "why_it_is_close": "Size-exact with every instruction correct. WAVE 47: this is the same failure as sub_08070F44 in the same block -- an allocno-priority tie where the pointer parameter and a short-lived value pseudo swap slots. decomp-permuter was run against that identical residual on sub_08070F44 (300 s x 4 threads) and found nothing, so do not expect it to close this one.",
   "axes_ruled_out": [
    "`(track->tune + track->bend * track->bendRange)` -- produces the ROM's add operand order but loads tune before the multiply, which the ROM does not; 0.6% apart, the same allocation fact seen twice",
    "one `s32 x` spanning both blocks instead of block-scoped locals -- 10.0% vs 10.6%",
    "writing the constant on the left (`1 & x`) to reproduce the ROM's `movs r0,#1` BEFORE `ldrb r1,[r2,#0]` -- measured on sub_08070CD0, where it hoists the constant into the loop preheader instead, strictly worse"
   ],
   "settled_by_this_attempt": [
    "The first parameter is DEAD -- it arrives in r0, is never read, and the function carries agbcc's own epilogue (`push {r4,lr}` ... `pop {r4}; pop {r0}; bx r0`). This is ordinary compiler output with a dead leading argument and does NOT belong in data/asm-resident.json.",
    "`s8 tune` at +0x0c, not u8 (wave 35): the ROM reads it `movs r1,#12; ldrsb`, and with u8 the function was 4 bytes short.",
    "`(u32)(track->volX * track->vol) >> 5`, volX FIRST -- the ROM loads +0x13 before +0x12, and the `(u32)` is what makes all four volume shifts `lsrs` while the pitch shift stays `asrs`.",
    "An open tell worth one controlled probe: the ROM emits `movs r0,#1` BEFORE `ldrb r1,[r2,#0]` for the first flags test while every candidate emits the load first. The identical asymmetry appears in sub_08070CD0 (load-first for `& 2`, constant-first for both `& 1` sites). Source operand order is NOT the cause."
   ],
   "reason": "Register allocation, not source semantics -- the same allocno tie as sub_08070F44, in the same block."
  },
  "sub_08070CD0": {
   "bytes": 200,
   "best": "196 bytes (-4), 10.0% identical, first difference at +0x8",
   "remaining_diff": "ONE instruction plus the pool alignment it would create. The ROM materialises the 0xFFFF mask as `ldr r3, =0x0000FFFF` followed by `adds r2, r3, #0`; the candidate emits the `ldr` in the right place but not the copy, and losing the copy also loses the `.short 0x0000` the pool needs for alignment. Everything else is byte-exact: both fade arms, the shutdown loop, the status write, the final per-track loop, the dead `movs r7,#0` in the tail preheader, and the two known-zero register reuses CSE supplies from the preceding `cmp rN,#0`.",
   "why_it_is_close": "Two size-neutral byte diffs are also open and are pure register/order facts: the ROM emits `movs r0,#1` BEFORE `ldrh r7,[r6,#0x28]` at both `fadeOV & 1` sites, and it keeps the fadeOV loads inside both loops in r7.",
   "axes_ruled_out": [
    "a bare member re-read, an explicit `& 0xFFFF` literal, a `u16` local, an `s32` local, and a `(u16)` cast on any of them -- NONE produces the `ands rD, rMask` form; all give `lsls #16; lsrs #16`",
    "`u32 v` locals for the fade arithmetic -- kills the post-store reload but breaks the CSE of the initial load, so `adds r0,r1,#0` becomes `ldrh r0,[r6,#40]`",
    "`1 & mplayInfo->fadeOV` to reproduce the constant-before-load order -- hoists the constant into the loop preheader instead, strictly worse"
   ],
   "settled_by_this_attempt": [
    "The `ands rD, rMask` form appears ONLY when 0xFFFF is already in a register, and the only thing that puts it there is a source-level VARIABLE (`u32 m; m = 0xFFFF;`). That spelling also reproduces the ROM's placement of the `ldr`, which agbcc's scheduler sinks to just after the `strh` of the fadeOC decrement.",
    "It explains the ROM's redundant `ands r0,r2` in FRONT of `lsls r0,r0,#0x10` in the fade-out arm: combine cannot fold an AND whose mask is a REGISTER, so the mask survives where a constant would have been absorbed by the shift.",
    "`(u16)` on the masked value in the fade-IN arm is load-bearing -- without it agbcc re-loads the member after the `strh` instead of reusing the stored register. With it the arm is byte-exact.",
    "`--mplayInfo->fadeOC != 0` (not a separate decrement and test) is what keeps the fadeOC zero-extension on `lsls/lsrs` rather than on the mask register."
   ],
   "reason": "One uncoalesced constant-load copy. The next thing to try is the spelling that keeps the fadeOC test on shifts while still costing a copy at the mask's DEFINITION -- `u32 m = 0xFFFF;` as an initialiser produces a copy, but at the USE site instead, and moves the `ldr` to the top of the function."
  },
  "sub_0806FD98": {
   "bytes": 76,
   "best": "not scorable -- see reason",
   "remaining_diff": "The first two `ldr`s are `ldr r0, _08070044` and `ldr r2, _08070048`, i.e. PC+680, while the function ends at 0x0806FDE4. Those two pool words live 600+ bytes past the end of the function, inside a LATER function's pool, and this function also carries a local pool for the two constants its tail uses. A function compiled on its own can only emit its own pool, so the `ldr rN, [pc, #680]` encodings cannot be reproduced at any spelling: the differing bytes are the PC offset, not the code.",
   "why_it_is_close": "The C is believed right -- it is the m4aSoundVSync shape, the fe_matches index agrees, and the 'Smsh' guard plus the pcmDmaCounter/pcmDmaPeriod reload are the canonical MP2K ones. This is a POOL park, not a codegen park: check every pool word's address against the function's extent before spending attempts, and unpark it when this region is promoted as a whole file.",
   "axes_ruled_out": [
    "WAVE 47: the local-temp spelling `t = p->pcmDmaCounter - 1; p->pcmDmaCounter = t; if (t > 0)` does NOT rescue the `volatile` qualifier. With `vu8` it still emits a dead `ldrb` between the `subs` and the `strb` -- four instructions where the ROM has three. `--p->pcmDmaCounter > 0` is worse again at three loads. So the wave-33 contradiction between this function and the matched sub_08070AF8 stands; my hypothesis for dissolving it is refuted.",
    "hardware.h's separate REG_DMA1CNT/REG_DMA2CNT macros -- they CSE to 0x040000C4 and 0x040000D0 instead of the ROM's single 0x040000BC base with the 12-byte DMA stride"
   ],
   "settled_by_this_attempt": [
    "`subs r3,r3,r2; cmp r3,#1; bhi` is ONE unsigned expression, `(ident - MPLAY_ID_NUMBER) > 1`, not two equality tests.",
    "The ROM pools 0x68736D53 POSITIVE and subtracts it. The honest spelling does not do that: agbcc canonicalises subtract-a-constant into add-the-negative and pools the NEGATED value. A positive pool word plus a register `subs` means the constant reached the expression from a register, i.e. the source held it in a variable -- the same idiom as the 0xFFFF mask in sub_08070CD0.",
    "`ldr r1,[r2,#8]; lsls r1,r1,#7; blo` is a single-bit test of bit 25 shifted into the carry; `blo` is carry-clear, so the store runs when the bit is SET."
   ],
   "reason": "Blocked by a literal pool that sits outside the function's own extent. Not a source problem and not scorable per-function; it needs whole-file promotion of the region."
  },
  "sub_0804FA2C": {
   "bytes": 632,
   "best": "636 bytes (+4), 46.2% identical",
   "remaining_diff": "&gUnknown_03004580 is materialised early and register-allocated, but into sl and ONE INSTRUCTION BEFORE `ldr r5, =gUnknown_085D6A48`, where the ROM has it in r8 one instruction AFTER -- so the last two literal-pool words are swapped and the tile multiply pays two extra copies (`adds r3,r1,#0; muls r3,r0; adds r0,r3,#0` against the ROM's bare `muls r0, r1`). The multiply is downstream of the allocation, not independent: both operand orders and a temporary for the table read give byte-identical output.",
   "why_it_is_close": "This is the same failure mode that sub_0804D290 and sub_0804DCA8 had, and both of those are now MATCHED. Of two address constants used the same number of times in the same statements, agbcc gives the callee-saved register to whichever pseudo is created first and emits that one's ldr at the head of the statement; the fix on the twins was a comma operator inside the other argument's index, moving the reference one slot later. Here the identical comma costs &gUnknown_0300453C its r6 and is a net loss, because FA2C carries one more simultaneously live value than the twins do. See 'Of two address constants, the pseudo created FIRST wins the register' in docs/agbcc-codegen.md.",
   "axes_ruled_out": [
    "old_agbcc -- 624 bytes on the twins, worse here too",
    "-fno-force-addr -- BIT-IDENTICAL output, so these `ldr rN, =sym` are ordinary address loads and not -fforce-addr artefacts",
    "lc_screen reports 0 pool words, so this failure mode is invisible to it",
    "do { } while (0) round the tileNum statement, the sub_08057D44 statement, the sub_08015928 statement, or the four entry writes -- +8 to +20",
    "the comma anchor in either the tileNum or the sub_08057D44 statement -- same +4 size, worse bytes",
    "anchoring through pos, row, an int, or a register-qualified local -- all bit-identical, so the anchor's variable is irrelevant and only its position matters",
    "a u16 (*)[8] pointer local for the gUnknown_03004580 reads -- collapses the base-plus-constant form the ROM CSEs into an ldrh displacement",
    "two decomp-permuter runs of 300 s+ (3000 and 12000 iterations) -- best edits were noise (`new_var = 5` used as an array index) and both scored worse than the hand fix"
   ],
   "settled_by_this_attempt": [
    "gUnknown_085D6A48's rows are STRUCTS, not u16[12] -- the ROM keeps column 9 in the ldrh displacement, which an array row cannot emit (4 bytes)",
    "(rN = pos[k])[i*5+j] and not pos[k][i*5+j] -- the ROM builds the row address first (12 bytes)",
    "ONE binding local PER STATEMENT: a single `row` reused across four statements is one pseudo spanning all of them and takes a low callee-saved register the ROM spends on the constant 180 (4 bytes)",
    "* 0x100 and not << 8 (4 bytes)"
   ],
   "reason": "Register allocation, not source semantics. The instruction stream, the type model and every read form are settled and recorded in the draft's header; what remains is one address constant landing one slot early in a function that is one live value tighter than its two matched twins. Worth a wave-18 look precisely because the twins closed on the same axis."
  },
  "sub_0804E7A8": {
   "bytes": 328,
   "best": "94.2% -- 328 of 328, SIZE EXACT",
   "remaining_diff": "Three byte positions, and one of them is not a difference: the pool word relocates against `gUnknown_085D6C88 + 0xc` where the ROM uses `gUnknown_085D6C94 + 0` -- the same address, which reloc_equivalent would bless once the instruction stream is clean. The real residual is that the ROM emits `lsls rN,#16` beside EACH of the two tail subtractions and defers BOTH `asrs` past the gUnknown_03001FBC load, splitting each s16 conversion around the first argument's setup.",
   "why_it_is_close": "Its sibling sub_0804FCA4 MATCHES from the same shape and the same four spelling decisions; the two were solved by diffing them against EACH OTHER rather than against the ROM, which is what found the sound table's declaration. Everything except the two shift placements is the ROM's.",
   "axes_ruled_out": [
    "s16 / u16 / int / short locals for the two computed arguments, `(s16)` casts at the call, inline arguments, reversed assignment order, separate declarations, an `idx` local for the first argument, and do/while(0) round each assignment, both assignments and the call.",
    "Eight declaration orders and int/u16 for c, e, t, w.",
    "Reaching the sound table as the flat `s16 [][6][2]` view its own pool word suggests: 136 bytes worse (52.7%). The struct-member spelling is required.",
    "do/while(0) round the p1/p2 pair and round the `entry->x +=` statement."
   ],
   "settled_by_this_attempt": [
    "The 0x085D6C88 sound table is a STRUCT array reached as `g[B].unk0c[j][i]`, NOT the flat 3-D view. That single declaration is worth 40 points here and the whole match on sub_0804FCA4.",
    "`&gUnknown_02029A10[c].entries[e]` CSEd across four member accesses needs the explicit byte-offset sum; used once (sub_0804E8F0) it needs the plain subscript.",
    "`row = gUnknown_02028E5C[c]` must be bound INSIDE the subscript that uses it: as its own statement the two pool words come out in the wrong order (5 bytes), inline it loses the `[rB,#2]` displacement (44 bytes)."
   ],
   "reason": "instruction placement"
  },
  "sub_0807166C": {
   "bytes": 344,
   "best": "81.4% -- 344 of 344, SIZE EXACT",
   "remaining_diff": "The eleven-instruction prologue only; all eighteen case bodies and the jump table are byte-identical. The ROM has a->r4, b->r6, op->r5 and keeps the ORIGINAL cursor live, reading the second and third bytes as `[cur,#1]` and `[cur+1,#1]` and loading a->unk18 BEFORE the offset byte (`off + base`); the draft has a->r5, b->r4, op->r6, advances the cursor in place and adds `base + off`.",
   "why_it_is_close": "It is one opcode of a bytecode interpreter: three bytes off b->unk40, then a switch of six read-modify-writes and twelve comparisons whose twenty-four written-out arms cross-jump into the ROM's two shared tails.",
   "axes_ruled_out": [
    "Eight declaration orders; int/u8/u32 for `op` and `v`.",
    "`*b->unk40++` three times, an explicit `cur` local with three separate `b->unk40 = cur + n;` stores, `*cur++` with the write-back after each, and binding the offset byte to its own `int d` (21 bytes worse -- the offset must stay inside the address expression).",
    "Both operand orders of `off + a->unk18`."
   ],
   "settled_by_this_attempt": [
    "`v` is a 32-BIT local, not `u8`: `u8` costs 15 bytes and `int` 4, while `u32` makes all eighteen case bodies byte-identical.",
    "gUnknown_03005744 is a two-argument function pointer (from `_call_via_r2`) taking this function's own two context pointers.",
    "The opcode map: 0-2 store/add/subtract an immediate, 3-5 the same through `a->unk18[operand]`, 6-11 compare against the immediate and 12-17 against `a->unk18[operand]`, in the order ==, !=, >, >=, <=, <."
   ],
   "reason": "register allocation"
  },
  "sub_08079EA4": {
   "bytes": 264,
   "best": "size-exact (264 bytes), 99.2% identical, exactly 2 bytes differ",
   "remaining_diff": "The two bytes are the immediate field of ONE `bl` at +0x16, and nothing else in the function differs -- every other instruction, register and constant matches, and the only other diff lines are branch/reloc ADDRESSES that differ because the candidate sits at offset 0 while the original sits at +0x766 inside its unit. The original's instruction is `bl 3ec <_08079B38>`, an intra-file branch to a LOCAL label with the offset already assembled in and NO relocation. Any C caller necessarily emits `bl 0` plus an `R_ARM_THM_CALL` against an external name, so the bytes cannot agree. This is NOT a codegen difference and there is no source spelling that fixes it.",
   "why_it_is_close": "0x08079B38 is a genuine function entry that tools/split_asm.py merged into sub_08079B04, which is two bytes of `b _08079B38` sitting in front of the PREVIOUS function's 13-word literal pool and has zero callers. docs/agbcc-codegen.md already classifies this exact site under 'bl to a mid-function local label is NOT a tell' (the split-artefact row, `_08079B38` x3). The consequence for CALLERS had not been noticed: because `asm/` names the entry only as the local label `_08079B38` and no global symbol exists at 0x08079B38 in either build, a promoted C caller has nothing to link against. sub_08079EA4 is the first caller of such an entry that anyone has tried to match -- the only other site is `_08036B48`, whose two callers are both inside AgbMain, which is still assembly.",
   "axes_ruled_out": [
    "the source axis for everything except the call -- the draft is instruction-for-instruction the ROM, so there is nothing left to derive",
    "naming the callee `_08079B38` in C -- this WOULD make trymatch report a match, because both relocations would then name the identical symbol. It is a FALSE match and must not be used: `_08079B38` is a local (non-global) label in asm/code-0806CFC8.s, so the promoted unit would fail to link. This is exactly the class of error the brief warns trymatch cannot see, and it is the tempting wrong fix here.",
    "an lds symbol (`sub_08079B38 = 0x08079B38;` from gen_lds.py) -- would make the split build LINK correctly and produce identical ROM bytes, but trymatch still rejects it: reloc_equivalent resolves ABS32 through the symbol table and requires a non-ABS32 relocation to name the IDENTICAL symbol, so `_08079B38` vs `sub_08079B38` is a mismatch regardless of both resolving to one address.",
    "a function-pointer cast to the literal address -- emits a pool word and `bl _call_via_rN`, not a `bl`, so it is a different instruction stream"
   ],
   "settled_by_this_attempt": [
    "the whole function's source is settled; the draft in work/sub_08079EA4/ is the deliverable the moment the symbol exists",
    "the affine denominator is `proc->unk34 == 0x200 ? 2 : 0x200 - proc->unk34` and NOT the `d != 0 ? d : 2` form the promoted c_08027B68.c uses. Both are the same predicate; they are different code. The ROM presets `movs r1, #2` and conditionally overwrites it with `subs r1, r4, r2`, so the arm that gcc computes UNCONDITIONALLY is the `2`, which means `2` must be the TRUE arm in the source. The `!= 0` spelling put `0x200 - x` first and cost +4 bytes across the four call sites (two extra `adds r0, r2, #0` shuffles), scoring 28.8%. This is the general rule: in `c ? A : B` compiled to one branch, A is the arm emitted unconditionally -- read the presetting `movs` to recover which arm the source wrote first.",
    "sub_080795A8 and sub_08079B38 both take TWO arguments with the first unused in the body; the proc pointer left sitting in r0 costs no instruction and is not evidence of arity, the `movs r1, #0` is",
    "gUnknown_08615E40 is a real 12-byte u8 ROM table read `base + index; ldrb`, NOT a .LC pool word -- tools/lc_screen.py misclassifies it (fanin 1, density 4). Dereferenced against baserom.gba: 02 03 04 05 x3, with address-shaped words starting at +0x0c.",
    "proc offsets: +0x30 int, +0x34 int, +0x40 int, +0x4c u16, +0x52 u16"
   ],
   "reason": "TOOLING, not decompilation, and the fix is one line in the right place: tools/split_asm.py should treat a label that is the target of a `bl` from outside its own unit as a function boundary (there are 4 such sites, all listed in docs/agbcc-codegen.md), or failing that gen_lds.py should emit an absolute symbol at 0x08079B38 AND trymatch's reloc_equivalent should resolve R_ARM_THM_CALL through the symbol table the way it already does for R_ARM_ABS32. Either change unparks this immediately with no change to the draft. Deliberately NOT put in asm-resident.json: this is ordinary compiler output and the C is already right. WAVE 28 (W28-B), unit-level oracle checked: `--unit` EXITS 2 without comparing anything. Its unit is sub_0807974C.s and holds FIVE functions -- sub_0807974C, sub_08079B04, sub_08079EA4, sub_08079FAC, sub_0807A0C4 -- of which only sub_08079EA4 has a draft. The 99.2% per-function score is therefore not evidence of anything about the source: the per-function oracle is the wrong oracle for a five-member unit and the unit oracle has nothing to run on. THIS IS THE ONE ACTIONABLE ENTRY OF THE FIVE. Unblocking it is a defined, bounded job with no unknowns: draft sub_0807974C, sub_08079B04, sub_08079FAC and sub_0807A0C4, then re-run `--unit`. Do not spend another attempt on sub_08079EA4's own C until those four exist."
  },
  "sub_0804F18C": {
   "bytes": 572,
   "best": "34.4% -- 552 of 572, 20 bytes SHORT (wave 19, W19-B)",
   "remaining_diff": "Register allocation only; the instruction stream and all the address arithmetic match. The ROM parks the 0xb4 gUnknown_02029A10 group stride in r8 and pays `mov rN, r8` at each of its five `muls` uses, and gives `pos` (the sub_08057D44 result) the low callee-saved r7. The draft does the opposite: the stride takes r6 and is used directly, and `pos` is pushed out to `ip`. That is why the candidate is SHORTER than the original rather than longer -- the ROM's extra bytes are the copies its own allocation costs it. Secondary, and downstream of the same choice: the ROM shares ONE `lsls #16; asrs #16` of `v` between the two uses and the draft emits it only at the second, because with one reference combine substitutes it into the first `strh`.",
   "why_it_is_close": "Everything except the allocation is settled and verified. The three `.LC` pool words are REPRODUCED by the honest spelling -- agbcc emits &gUnknown_03001FBC, &gUnknown_0300451C and &gUnknown_0300453C into this unit's .rodata in the ROM's own slot order for 0x081360A0/A4/A8, and the text pool relocates R_ARM_ABS32 .rodata at three offsets, which is the placeable shape the twelve wave-18 unparks used. This is the same residual, in the same direction, as its matched twins sub_0804D290 / sub_0804DCA8 had before the zero-trip loop and the comma anchor closed them, and the same as parked sub_0804FA2C.",
   "axes_ruled_out": [
    "The wave-13 `c_local` spelling (`&gUnknown_081360Ax` bound to a local, read `**local`) -- +28 bytes at 26.8%, kept as work/sub_0804F18C/_clocal_nearmiss.c. Worse on every axis than the honest draft and no longer needed for the relocations.",
    "c_local with the MIDDLE level bound explicitly (`ppA = &LC; a = *ppA;` then `*a`), which is wave-13 finding (1) and is what the ROM's text literally shows -- 548 bytes (-24) at 10.8%: each `pp` has only two uses, agbcc const-propagates it at the second, and the high-register copy never appears. Two uses of a `pp = &<pool word>` local is not enough to keep it live, as sub_08058A2C already records.",
    "`do { } while (0)` round the sub_08057D44 statement -- the lever that closed sub_0804D290. 548 bytes (-24), 35.5%: it moves the wrong way here, because D290 needed it together with two camera-origin pointer locals this function does not have.",
    "`s16 v` versus `int v` with `(s16)v` at each use -- byte-identical, so the shared sign extension is not reachable by either.",
    "`u16 (*t)[5][2]` with `t[side][slot][0]` versus `struct UnkPosPair *` with the flat `(e1 = &pos[side * 5 + slot])->x`. The flat form is correct and is what the draft carries: it reproduces the ROM's `lsls #2; adds; adds; lsls #2` single scaling exactly, where the 2-D form scales the two indices separately. F658 is the opposite way round -- its ROM really is 2-D with a 40-byte row.",
    "TOOLCHAIN, all measured on the honest draft: old_agbcc, -fforce-mem, -fno-gcse and -fno-force-addr all leave it unchanged and none of them alters whether the .rodata words are emitted."
   ],
   "settled_by_this_attempt": [
    "The honest spelling REACHES this function's three `.LC` words, which the header comment on gUnknown_081360A0 and the wave-13 scalar/array triage both said it could not. What decides it is the candidate's CFG: with the two-bit guard spelled `if (a.bit0 && !a.bit6)` the tests fold to `(x & 0x41) == 1`, a basic block disappears, and agbcc emits NO .rodata at all. See docs/agbcc-codegen.md.",
    "The guard is `t = *(u8 *)&gUnknown_03004504; if (t & 1) { if (!(t & 0x40)) ... }` -- six spellings probed and only this one gives the ROM's two separate mask tests with no QImode truncation on the second. sub_0804BCB8's own body has the identical shape over the same two bits.",
    "gUnknown_0300454C[0] takes OamData.priority read straight back out of the field it was just assigned (`lsls #0x1c; lsrs #0x1e` on the stored byte), not a second literal 2.",
    "The four gUnknown_03001470 writes are unk28 = gUnknown_0300451C * 8, unk2c = 0, unk30 = gUnknown_0300453C, unk34 = gUnknown_0300451C -- the same run its sibling F658 has minus the unk2c store."
   ],
   "reason": "register allocation"
  },
  "sub_0804F658": {
   "bytes": 612,
   "best": "31.4% -- 604 of 612, 8 bytes SHORT (wave 19, W19-B)",
   "remaining_diff": "The same allocation residual as sub_0804F18C and in the same direction: the ROM keeps the 0xb4 group stride and the sub_08057D44 result in registers whose uses cost it copies the draft does not pay, so the candidate comes out short. The prologue, the four proc-field writes, the OamData bitfield run, both sub_08057D44 arguments, the two-bit guard and the whole 2-D position lookup are byte-identical to the original.",
   "why_it_is_close": "It was produced from sub_0804F18C's finished draft by five substitutions and reproduced that function's residual CLASS on the first trymatch, which is the banked-discount behaviour sub_0804DCA8 showed against sub_0804D290. Its three `.LC` words are reproduced by the honest spelling in the ROM's slot order for 0x081360B8/BC/C0.",
   "axes_ruled_out": [
    "`do { } while (0)` round the sub_08057D44 statement -- 600 bytes at 17.2%, a large regression, so the D290 lever does not transfer to this member either.",
    "`s16 v` versus `int v` with `(s16)v` at the one use -- byte-identical.",
    "Everything ruled out for sub_0804F18C applies here unchanged; the two share the shape and, so far, the residual."
   ],
   "settled_by_this_attempt": [
    "The wave-13 scalar/array triage does not hold as a decision rule. This function's third `.LC` word (0x081360C0) holds &gUnknown_03004580, an ARRAY -- the row that triage parks outright -- and the honest spelling emits it correctly along with the other two. What the triage measured was which spelling reproduces the indirection, and since wave 18 the answer is the honest one.",
    "sub_0804BCB8's THIRD parameter is s16, not u16. This function and sub_0804E584 both pass gUnknown_0855214C[side] and both emit `movs rN, #0; ldrsh rD, [rB, rN]`, which a u16 parameter cannot produce -- it rewrites an ldrsh into an ldrh and needs no zero register. Retyped in include/unknown-functions.h; byte-neutral for the two promoted callers, which pass a literal 0 (re-verified).",
    "gUnknown_0855214C is a per-side ROM halfword, `u16 []`. The `ldrsh` at both readers is the conversion to that s16 parameter and says nothing about the object.",
    "The position record is indexed TWO-dimensionally here -- `pos[gUnknown_03004580[side][5]][side * 5 + slot]` with a 40-byte row and the row bound inside the subscript -- where the otherwise identical sub_0804F18C uses the flat one-dimensional index. The struct Unk02029A10.unk04 comment already recorded this split for sub_0804FA2C; F658 is a third instance, so the flat/2-D choice is per function and must be read off the scaling."
   ],
   "reason": "register allocation"
  },
  "sub_08052718": {
   "bytes": 256,
   "best": "244 bytes (-12), 8.2% identical",
   "remaining_diff": "ONE hoist, and it is sub_08052BBC's hoist (a) verbatim: the ROM computes `b * 2`, the index of the store target gUnknown_02029808[a].unk24[b], immediately after `gUnknown_0300451C = b` and before the sub_08015410 argument setup, holding it across the call; the draft emits it after the call at the store. That one live range is the whole difference: the ROM puts `b` in sb and saves two high registers where the draft keeps `b` in r6 and saves one. There is no gUnknown_02028E5C here, so unlike sub_08052BBC this function has only the one hoist to explain and is the CHEAPER OF THE TWO to finish.",
   "why_it_is_close": "The head and the entire tail line up instruction for instruction, including the two-armed `if` on `gUnknown_03004580[a ^ 1][1] == 0x14` selecting 0x23 against 0x10 (the `eors`, the `lsls #4` row scaling, the `adds r1,#2` column bias, the `bne` and the unconditional `b` all reproduce), and including the `-fforce-addr` .rodata pool word.",
   "axes_ruled_out": [
    "`s16 *` / `u16 *` local bound to &gUnknown_02029808[a].unk24[b] or to gUnknown_02029808[a].unk24 -- both fold +0x24 into the symbol's own relocation, so the literal-pool word becomes `gUnknown_02029808+0x24` where the ROM has the bare symbol plus `adds rN,#0x24` on the base. A strictly WORSE diff, and it rules out every pointer-binding spelling of the store target at once",
    "`(&gUnknown_02029808[a])->unk24[b]` as the store target -- this DOES reach the ROM's register pressure (three saved high registers, the correct `push {r4,r5,r6}`) but it binds &gUnknown_02029808[a], not the index, and it moves `a` into r8. Closest wrong answer found; do not mistake the matching prologue for progress",
    "`*(gUnknown_02029808[a].unk24 + b)` and `unk24[(int)b]` -- bit-identical to the plain subscript",
    "`c = a; d = b;` copies in the c_08051F4C style -- erased by cse in a straight-line block, see the wave-20 sub_08052650 section in docs/agbcc-codegen.md",
    "a zero-trip `do { } while (0)` round the sub_08015410 statement, round the four sub_08016824..sub_080157F4 calls, and round the whole body -- the wave-17 lever makes THIS function worse, +1 instruction and `a` forced into a high register. Do not re-run it here",
    "`e` as u16 / s16 / a `u16 *` bound to &gUnknown_08553B10[a] -- all three byte-identical, and all three already correct; the sub_08052270 lever does not apply because the two-argument ordering in this function is already right"
   ],
   "settled_by_this_attempt": [
    "gUnknown_0813610C IS NOT A GLOBAL. It is a `.LC` address-constant slot in data/data.s holding 0x02029808, with 0x03001470 0x03001FBC 0x020298E0 0x020297C0 0x02029808 0x03004580 0x02029A10 as its neighbours -- a -fforce-addr block by the test in docs/agbcc-codegen.md. Naming gUnknown_02029808 DIRECTLY (the honest spelling) makes agbcc emit its own .LC word and reproduces the ROM's double indirection, `ldr r4,[r1]` then `mov r4,r8; ldr r1,[r4]`. This part needs nothing further and the c_local workaround must not be reached for -- do not spend an attempt here",
    "WHY sub_08052718 has that pool word and its near-twin sub_08052BBC does not, on the same global and the same statements: only this one has a control-flow MERGE. That is the existing -fforce-addr rule seen twice in one pair of near-twins, i.e. independent corroboration for it",
    "`e = gUnknown_08553B14[a]`, the 08553B14 half of the pair include/unknown-globals.h records (sub_08052BBC and sub_08051BEC use 08553B10)",
    "`a ^ 1` and not a second variable -- the ROM has `movs r2,#1; adds r0,r7,#0; eors r0,r2`",
    "0x100 as `movs #0x80; lsls #1`, against sub_08052BBC's 0x180",
    "gUnknown_08553704 is an animation descriptor of the gUnknown_085536A4 family; its draw slot is the matched sub_0805297C and its slot 2 is sub_08052AF4, which wave 20 matched"
   ],
   "reason": "Register allocation, not source semantics, and the SAME residual as sub_08052BBC. Park them together and unpark them together: six spellings were probed against this one and the same six against its twin, in both directions, with every edit transferring one-for-one. Draft at work/sub_08052718/sub_08052718.c."
  },
  "sub_08052BBC": {
   "bytes": 232,
   "best": "224 bytes (-8), 12.1% identical from the honest draft; 69.8% from a permuter edit that is NOT the source (see below)",
   "remaining_diff": "TWO hoists, one fact each, 5 instructions total. (a) The ROM computes `b * 2` -- the index of the store target gUnknown_02029808[a].unk24[b] -- immediately after `gUnknown_0300451C = b` and BEFORE the sub_08015410 argument setup (`mov r2,sb; lsls r5,r2,#1`), holding it in a callee-saved register across the call; every spelling probed emits it after the call, at the store. (b) The ROM also hoists `a * 4`, the gUnknown_02028E5C index whose statement is the LAST in the function, to the same point and parks it in sl (`lsls r0,r6,#2; mov sl,r0` ... `ldr r0,=gUnknown_02028E5C; add sl,r0; mov r2,sl; strh r0,[r2]`). That third long-lived value is why the ROM pushes {r4,r5,r6} where the draft pushes {r5,r6}. Everything else -- every statement, the pool order, the s8 narrowing, the 0x180 as `movs #0xc0; lsls #1`, the literal-0 fifth argument -- is exact.",
   "why_it_is_close": "The instruction stream and the whole type model are settled; this is register allocation plus one instruction position, the same class as sub_0804FA2C. The literal-pool order (0300453C, 0300451C, 0855371C, 02029808, 08553B10, 02028E5C) matches the draft exactly, and since pool order is a direct readout of source order it independently CONFIRMS the statement order, including that the gUnknown_02028E5C store belongs at the very end -- which is what makes the `a * 4` hoist a puzzle rather than a transcription error. THE TWO MATCHED EXEMPLARS CANNOT SETTLE IT: src/decomp/c_08051BEC.c and c_08051F4C.c have `b * 2` before the call too, but CSEd out of `gUnknown_08552178[a][b] * 7 + 6`, the fifth-argument statement -- and sub_08052BBC's fifth argument is the literal 0, so that statement does not exist here. There is no exemplar anywhere for `b * 2` early with nothing else to produce it, which is why the family transfer that closed the other five functions in this batch stops at these two.",
   "axes_ruled_out": [
    "`s16 *` / `u16 *` local bound to &gUnknown_02029808[a].unk24[b] or to gUnknown_02029808[a].unk24 -- both fold +0x24 into the symbol's own relocation, so the literal-pool word becomes `gUnknown_02029808+0x24` where the ROM has the bare symbol plus `adds rN,#0x24` on the base. A strictly WORSE diff, and it rules out every pointer-binding spelling of the store target at once",
    "`(&gUnknown_02029808[a])->unk24[b]` as the store target -- this DOES reach the ROM's register pressure (three saved high registers, the correct `push {r4,r5,r6}`) but it binds &gUnknown_02029808[a], not the index, and it moves `a` into r8. Closest wrong answer found; do not mistake the matching prologue for progress",
    "`*(gUnknown_02029808[a].unk24 + b)` and `unk24[(int)b]` -- bit-identical to the plain subscript",
    "`c = a; d = b;` copies in the c_08051F4C style -- erased by cse in a straight-line block, see the wave-20 sub_08052650 section in docs/agbcc-codegen.md",
    "a zero-trip `do { } while (0)` round the sub_08015410 statement, round the four sub_08016824..sub_080157F4 calls, and round the whole body -- the wave-17 lever makes THIS function worse, +1 instruction and `a` forced into a high register. Do not re-run it here",
    "`e` as u16 / s16 / a `u16 *` bound to &gUnknown_08553B10[a] -- all three byte-identical, and all three already correct; the sub_08052270 lever does not apply because the two-argument ordering in this function is already right",
    "moving `gUnknown_02028E5C[a][0] = 1;` to the top of the function -- REFUTED BY THE POOL ORDER, not by size: its pool word moves to slot 3 and the ROM's is slot 6",
    "moving the gUnknown_02028E5C store after sub_08052818 -- no change",
    "`u16 *p; p = gUnknown_02028E5C[a];` bound early, dereferenced at the end -- puts the SYMBOL's `ldr` at the top as well, where the ROM loads it at the store; the ROM keeps only the INDEX live, which no pointer binding expresses",
    "`gUnknown_02029808[a].unk24[gUnknown_0300451C = b]` (the store folded into the index) -- moves gUnknown_0300451C's pool word to slot 4",
    "420 s of decomp-permuter, best 69.8% kept at work/sub_08052BBC/best.c. ITS WIN IS THE USEFUL PART AND IS NOT A SOURCE CANDIDATE: binding the three `1` constants to one `char` local plus a do/while(0) round the four calls reproduces the ROM's REGISTER ASSIGNMENT exactly (r5 = slot address, r4 = e, a third saved high register in use) with the WRONG VALUE in sl. So the allocation shape is reachable from source; what is missing is only the construct that makes `a * 4` the thing living there"
   ],
   "settled_by_this_attempt": [
    "gUnknown_0855371C is an animation descriptor of the gUnknown_085536A4 family (`u8 []`, address only); the six-record run 085536A4-0855371C is tabulated in include/unknown-globals.h and it is what gives every function in this cluster its zero-fan-in",
    "sub_08052818(u16, u16) -- both parameters narrow in place with no copy, the sub_080504A8 readout",
    "struct Unk02029A10.unk1c is a u16 flag, from an independent producer/consumer pair (sub_08052818 writes 1, sub_08052650 tests == 1)",
    "the fifth argument of sub_08015410 is the literal 0 here, not this family's `gUnknown_08552178[a][b] * 7 + 6` -- which is exactly why the exemplars do not transfer",
    "`e = gUnknown_08553B10[a]` is a LOCAL, and the `ldrsh` sitting immediately before the `bl` does not argue against one: combine folds the load down to the call site. See the wave-20 sub_08052270 section"
   ],
   "reason": "Register allocation and one instruction position, not source semantics. Nine spellings probed byte-for-byte plus a permuter run; the draft at work/sub_08052BBC/sub_08052BBC.c carries the whole list in its header. Take this one WITH sub_08052718 -- they are near-twins and they share hoist (a), so whatever produces it produces both."
  },
  "sub_0800CFDC": {
   "bytes": 6384,
   "best": "0.0% -- NOTHING WAS SUBMITTED. 0 try_match attempts spent. The head (first 72 instructions) was verified EXACT with compile_probe; the remaining ~90% is untranscribed, so the draft in work/ is deliberately #if 0'd and scores zero. Do not read 0.0% as a near-miss in the usual sense -- it is an absence, not a failure.",
   "park_kind": "VOLUME, NOT CODEGEN -- read this field before anything else",
   "remaining_diff": "There is no remaining diff, because there is no complete candidate. What remains is TRANSCRIPTION: ~20 further pattern tests after the two switch statements, each of the form (bounds guards on x/y against WIDTH/HEIGHT) + (a run of the cell predicate at neighbour offsets) + (one call to sub_0800EAF4 or sub_0800EB5C). 159 predicate instances, 187 basic blocks, 466 branches, 254 labels.",
   "why_it_is_close": "Every DERIVATION is done and none of it needs redoing. The function is one idiom -- src/decomp/c_08001158.c's map-descriptor access (p = gUnknown_08499590; t = y*2; rows = p+0x417A; off = (*(u16*)(rows+t)+x)*2; tiles = p+0xA22) -- evaluated 159 times over neighbour offsets, with the predicate (u16)(cell - 0x86) <= 1 and a handful of exact tile compares. Ten distinct expression shapes were predicted from the listing before drafting and exactly ten were found. The head reproduced 72 of 72 instructions from the FIRST draft, including the -fforce-addr reroute, which comes free from naming gUnknown_08499590 honestly. The whole function is readable in work/sub_0800CFDC/_symtrace.txt, produced by tools/symtrace.py, which renders the 3,239-line listing as 1,190 lines with every index expression already resolved to ro[y-1] / cell[(ro[y]+2)+x] / BRIDGE(...) / WIDTH / HEIGHT. A fresh agent resumes at roughly 80%.",
   "axes_ruled_out": [
    "NOT APPLICABLE IN THE USUAL SENSE, and this is the entry's whole point: no codegen axis was ruled out because no codegen axis was ever the obstacle. Nothing was tried and rejected. Do not read the short list below as an exhausted search.",
    "The two casesi jump tables assemble byte-for-byte and need no work -- confirmed by the doc's existing 'jump tables match' result; case bodies are in SOURCE order, which for switch 1 is 28..31, then 7/23, then 12/13, then 6/22, then default.",
    "The -fforce-addr head reroute is settled: the honest `gUnknown_08499590` spelling produces the ROM's three-level `ldr rN,=<word>; ldr r0,[rN]; ldr r1,[r0]`. Do NOT declare the pool word 0x0808D88C as `u8 **const` -- measured on the neighbouring sub_0800F8D4, that adds a fourth level.",
    "The four `bl _0800E8BA` are agbcc FAR JUMPS to the epilogue, not calls and not a second entry point -- THUMB `b` reaches only +/-2KB and this function is 6KB. Likewise `_0800D080` and `_0800D40C` appear in data_refs and are the two jump tables, not globals."
   ],
   "settled_by_this_attempt": [
    "The parameter pair is (int x, int y) -- a map column and row.",
    "All four callees are declared and typed from their own entry narrowing, now in include/unknown-functions.h: int sub_0800E8CC(int,int), int sub_0800E9F4(int,int), void sub_0800EAF4(int,int), void sub_0800EB5C(int,int).",
    "Top-level control flow: guard on the centre cell; drew = 0; a = x>0 ? sub_0800E8CC(x-1,y) : 0; b = sub_0800E8CC(x,y); c = x < width-1 ? sub_0800E8CC(x+1,y) : 0; a &= b; b &= c; switch(a); if (drew && a == b) return; switch(b); then the ~20 pattern tests.",
    "`drew` (r10) is a REAL variable, not a jump-threading artefact -- it is read nine more times further down, which is why only one switch arm materialises the assignment.",
    "The four `?rN` spots in _symtrace.txt are not unknowns: `?r8` is always y*2 (set at _0800D3E4), so H[(ROWS)+?r8] reads ro[y]; `?r0` at _0800D2BC / _0800D8B8 / _0800DDAE / _0800E10A / _0800E4B8 is a cross-jumped tail whose two predecessors pass x and x-1, so the SOURCE has two separate calls there.",
    "tools/symtrace.py itself, and specifically its predecessor-state JOIN: without the join 44 of 187 blocks render with poisoned register state and transcribing from them silently produces wrong constants."
   ],
   "open_question": "The one thing genuinely undecided: how many local bindings the original used per pattern region. The ROM reloads `=gUnknown_08499590` 21 times, once per region, so the binding is per-region rather than once for the function -- but whether each region binds p/rows/tiles as three locals (the c_08001158.c shape) or fewer is ~20 independent register-allocation decisions, and that is where any attempts would go.",
   "budget_unit": "Budget this in BLOCKS TRANSCRIBED, not in try_match attempts. Attempts is the wrong unit above ~2KB -- this function cost zero attempts and still did not finish. It is 187 blocks; sub_08083EE0, which closed in one attempt, was 24. A fresh agent starting from _symtrace.txt should expect the cost to be reading and writing, with attempts spent only on the per-region binding question above.",
   "reason": "Volume, not codegen. It is the largest straight-line function left in the tree at 6,384 bytes -- 8.5% of the remaining straight-line band in one function -- and it is larger than a single agent-session, which is a different claim from being hard. Nothing about the C or the compiler blocks it: zero backward branches, four already-typed callees, one idiom, ten expression shapes, head verified exact. It is parked so waves stop rediscovering the derivation, NOT because the search space is exhausted. See docs/agbcc-codegen.md, 'ACTUALS for sub_0800CFDC', for the measurement this produced: shape count governs whether you can work a function out, byte count governs how long it takes to write down."
  },
  "sub_0800F8D4": {
   "bytes": 1136,
   "best": "11.3% identical, 1276 bytes against 1136 (+140). Two rounds: 6.2% then 11.3%.",
   "park_kind": "ORDINARY NEAR-MISS -- a real candidate exists and the axes below were actually probed. This is NOT the volume park sub_0800CFDC is.",
   "remaining_diff": "The candidate needs SIX callee-saved registers where the ROM needs five: `mov r7,sl; mov r6,sb; push {r6,r7}` against the ROM's `mov r7,r8; push {r7}`, and the extra spill/reload is the whole +140 bytes. The ROM keeps exactly three things live across the body -- `ip` = the address of this unit's -fforce-addr pool word, r5 = x, r4 = y. Instruction streams otherwise line up: same compare chains, same constant chain, same four pool words, same branch structure.",
   "why_it_is_close": "Zero callees and one data ref, so nothing in it depends on an unsettled prototype or global. The control-flow skeleton is fully derived: a 14-term dispatch on the centre cell, then six branches (322|354, 323|355, 353, 321, 352, 320), each two bounds guards plus two 7-term neighbour chains. The cell access is src/decomp/c_08001158.c's idiom. What moved the score from 6.2% to 11.3% was TWO things and both are recorded in the draft: fixing a transcription error in branch 322|354's second chain (`289, 288`, not `259, 258`) and splitting the value types (`u16` for the neighbour cell, `int` for the centre one).",
   "axes_ruled_out": [
    "function-scope p/rows/tiles/t/off shared by all 13 accesses -- 608 instructions against the ROM's 544, worse than block-scoping",
    "the same five bound in a BLOCK per access -- 600, and what the draft carries",
    "`n` block-scoped per branch rather than function-scope -- byte-neutral, so `n` is NOT the extra live value",
    "c_local `pp = &gUnknown_08499590` with `*pp` at each access -- 574 instructions, the BEST count seen and WRONG: it emits two indirections where the ROM has three, i.e. it buys 26 instructions by dropping a level of the pointer model",
    "declaring the pool word as `u8 **const gUnknown_0808D898` and dereferencing it -- 602 and FOUR indirections, because agbcc force-addrs the declared symbol's address on top. Do not declare the pool word; the honest `gUnknown_08499590` supplies the ROM's middle level",
    "`u16 v` for the centre cell -- 9.7%, worse than `int v` at 11.3%",
    "the guard spelling is NOT an open axis: the ROM's `subs r0,r5,#1; cmp r0,#0; bge` is literally the draft's `if (x - 1 < 0)`"
   ],
   "settled_by_this_attempt": [
    "`u16` neighbour value and `int` centre value, and the tell is in the listing: the neighbour range test carries `lsls #16; lsrs #16` and the centre one does not, so they are different types rather than one",
    "the ROM chains ONE live constant through the whole tail (0x143, +0x1d -> 352, +1 -> 353, +2 -> 355, -0x40 -> 291, -0x20 -> 259, -1 -> 258) and branch tails jump INTO that chain after an adjusting adds/subs -- so a chain's constants cannot be read off its own immediates, which is how a wrong transcription survived a round",
    "A CROSS-JUMPED TAIL IS NOT EVIDENCE THE CONSTANTS ARE RIGHT: the wrong `259, 258` scored BETTER (600 insns) than the correct `289, 288` (607) precisely because 259/258 are the shared tail's constants and merged where the correct code cannot"
   ],
   "reason": "Register allocation, not source semantics. One live value too many in a function whose instruction stream is otherwise derived. The next axis, untried, is the thirteen access blocks' row index: the draft computes `t = (y +/- 1) * 2` inside each block while the ROM hoists `lsls r2, r4, #1` for the centre row and derives neighbours from the shifted value in some branches -- bind the row index rather than the byte offset."
  },
  "sub_080815C0": {
   "bytes": 444,
   "best": "97.3% -- 432 of 444 bytes, size-exact, 12 differ, first at +0x73",
   "remaining_diff": "PURE REGISTER-ALLOCATION SWAP, r5 <-> r6, and nothing else. Every instruction, operand, offset, literal-pool word and relocation is correct. The ROM holds the long-lived `(5 - i) * 2` table-index pseudo (live from the Interpolate call through the trailing gUnknown_08616980 lookup) in r6, and the short-lived `k = i - 4` OAM-colour pseudo (dead within the two-DivRem block) in r5. The candidate assigns them the other way round. Affected sites: `movs r6,#5 / subs r6,r6,r7 / lsls r6,r6,#1`, `adds r0,r6,r4`, `subs r5,r7,#4`, the two `subs r0,r0,r5`, `adds r6,r6,r0` and `ldrsh r2,[r6,r4]`.",
   "why_it_is_close": "Size-exact at 444 bytes with the whole loop, both branch arms, the || chain, all four Div/DivRem clamps and the PutSprite argument assembly identified and byte-correct. This is 97.3% with zero outstanding source-level questions.",
   "axes_ruled_out": [
    "Declaration order -- swapping `int i, k;` has no effect; named-var regnos are not the lever.",
    "Explicit binding of the index -- giving `(j = 5 - i)` its own variable instead of relying on implicit CSE is byte-identical to the unbound form.",
    "Hoisting `k = i - 4;` out of the DivRem argument into its own statement -- moves the instruction's POSITION earlier, breaking the position match as well, and STILL leaves k in r6."
   ],
   "settled_by_this_attempt": [
    "The entire source shape is PROVED: struct Unk80815C0 layout (unk2c/unk48/unk4c/unk52), the `proc->unk4c <= 0x2d` guard, the `for (i = 0; i <= 4; i++)` bound, the Div(unk48,9) >= i && < i+1 split against the >= i+1 arm, the shared `5 - i` index into gUnknown_08616972 / gUnknown_08616980, the `((tbl[...] + 2) << 12) | 0x800 | (tbl[...] * 32 + 0x1D8)` PutSprite word, the unk4c <= 0x2c increment/Proc_Break tail and the sub_08084700 call. An unpark must not re-derive any of it.",
    "Same failure class as sub_0801A6C0: an allocation tie-break with no demonstrated source-level lever."
   ],
   "reason": "Wave 22 (W22-S3, Sonnet A/B arm). Not source-exhausted in the usual sense -- the source is settled and only the allocator disagrees. Next attempt: shift register pressure with an unrelated live value nearby, or check the toolchain axis per data/compiler-overrides.json before spending source iterations. Do NOT re-run the three ruled-out variants above. WAVE 28 (W28-B), unit-level oracle checked: `--unit` runs (single-function unit) and still fails, 12 of 444 bytes from +0x73. Static-helper axis ruled out structurally. Not one byte of the residual is at a `bl` -- all twelve are one register pair SWAPPED. The ROM builds `(5 - i) * 2` in r6 and `i - 4` in r5; the candidate builds them in r5 and r6 respectively, and every instruction that touches either differs only in that. This is exactly the wave-17 tie-break: two pseudos of equal priority take r5 and r6 in RTL-creation order, so the fix is the order the two expressions are first REFERENCED in the source, not anything about their arithmetic."
  },
  "sub_080344F0": {
   "bytes": 68,
   "best": "68 bytes (size EXACT), 80.9% identical, relocations correct",
   "remaining_diff": "13 bytes, and every one of them is the copy loop's counter running BACKWARDS plus the r3/r4 renumbering that follows. Original: `movs r4,#0` / `adds r4,#1` / `cmp r4,#3` / `ble`. Candidate: `movs r3,#3` / `subs r3,#1` / `cmp r3,#0` / `bge`. Identical instruction count and identical size, so `size: match` is not evidence the loop is right. Everything else -- the five straight-line stores, their offsets (+0/+6/+7/+0xc/+0xd), both literal pool words, both relocations, the `adds r1,#4` / `adds r2,#0xe` setup, the loop body's `ldrb`/`strb`/two `adds`, and the reload of the block pointer after the loop -- is byte-identical.",
   "why_it_is_close": "This is agbcc's check_dbra_loop. It reverses a counted loop once the index is dead, which happens as soon as every giv derived from it has been strength-reduced. The ROM has BOTH pointers reduced to bivs AND a live ascending counter, and no spelling tried produces both at once: every spelling that reduces both pointers kills the index and reverses, and the only spellings that keep the index alive do so precisely BECAUSE agbcc declined to reduce the destination, which then costs an `add` in the loop body the ROM does not have. sub_0803442C, parked beside this one last round, was the same mechanism seen from the other side and is now MATCHED: there a second address expression `dst[j + 1]` off a separately-bound `j = i * 2` stayed unreduced and kept the index alive on its own. The equivalent lever does not exist here -- 44F0's loop body is a bare byte copy with no second use of the index to hang it on.",
   "axes_ruled_out": [
    "`for (i = 0; i < 4; i++)` indexing both arrays off their bases -- reverses",
    "`for (i = 0; i <= 3; i++)` -- reverses, identical output to `< 4`",
    "`i = 0; do { ... } while (i <= 3);` with array indexing -- reverses",
    "`i = 0; do { ... } while (i <= 3);` with the pointer walk -- reverses",
    "explicit `*d = *s; s++; d++;` pointer bivs with a separate counter -- reverses (this is the kept draft)",
    "the same with a `u8 v;` temp carrying the copied byte -- bit-identical to the above, so the temp is not a lever",
    "`blk += 0xe;` advancing the block pointer in place instead of a separate `d` -- bit-identical, reverses",
    "`d[i] = gUnknown_03004490[4 + i]` (destination through a local, source indexed) -- does NOT reverse, but agbcc then leaves the destination unreduced (`add r1, r4, r3` in the body) and spends an extra `add` plus a third callee-saved register",
    "`blk[0xe + i] = s[i]` (source through a local, destination indexed) -- leaves BOTH unreduced, two `add`s in the body",
    "`(*p)[0xe + i]` with the block reached through the pointer-to-pointer at every use -- does not reverse, for the same reason as above, and additionally reloads `*p` before each of the five straight-line stores",
    "naming gUnknown_030044B0 directly instead of going through gUnknown_08090D84 -- also size-exact and scores 82.3%, but relocates against this unit's own .rodata instead of gUnknown_08090D84. HIGHER PERCENTAGE, WRONG RELOCATION; work/sub_080344F0/best.c holds that draft and should not be preferred over the kept one",
    "unsigned counter -- excluded without compiling: the ROM's exit test is `ble`, a SIGNED compare, so the index is a plain int"
   ],
   "settled_by_this_attempt": [
    "gUnknown_08090D84 is agbcc's -fforce-addr address constant for gUnknown_030044B0, not a global. The ROM word there is 0x030044B0 and the .text pool word relocates against gUnknown_08090D84 itself, so the block must be reached through a `u8 *const *` local -- the same idiom as c_080345C8.c's gUnknown_08090D88. Now declared in include/unknown-globals.h with this evidence.",
    "gUnknown_03004490 is a u8 array at least 8 long, not a scalar or a struct: the loop indexes it with a variable from [4].",
    "The five straight-line stores and their offsets are byte-exact, and their order is the source's -- +0, +6, +7, +0xc, +0xd.",
    "The parameter is `int`: the prologue is bare and r0 reaches `strb r0,[r2,#6]` with no masking.",
    "The loop is FOUR iterations copying gUnknown_03004490[4..7] to block[0x0e..0x11], and both pointers are strength-reduced bivs in the ROM.",
    "Loop-direction differences are SIZE-NEUTRAL. This cost a wave: the function reports `size: match` at 68 bytes while 13 bytes differ, and reads exactly like a missing statement.",
    "The fall-through / negated-condition axis does NOT apply here -- sub_080344F0 contains no `if` at all, so there are no two constants in two blocks to swap."
   ],
   "reason": "One optimiser decision, not source semantics. The type model, the relocation, the statement order and the loop body are all settled and recorded in the draft header. What remains is making agbcc keep the loop counter ascending while still strength-reducing both pointers, and eleven spellings say those two outcomes are mutually exclusive through the source. Worth one look from anyone who finds a third lever on check_dbra_loop -- or a permuter run, which has not been tried here."
  },
  "sub_08004D90": "19.1% by byte score, but 62 of 68 bytes are solved -- the low score is the cascade from one early difference; best spelling is in work/sub_08004D90/sub_08004D90.c. Remaining diff is PLACEMENT, not shape: the ROM materialises `ldr rN, =gUnknown_08499578` inside BOTH arms of the if (two pool words at 0x08004DA4/0x08004DD0 holding the SAME address) and dereferences once after the merge; every candidate emits one address load after the merge. Ruled out: source ordering, exhaustively -- putting the array reference in both arms drags the deref into the arms, or cross-jumping re-merges the whole suffix. It is a -fforce-addr placement artefact with no source-level lever found in wave 26. The arithmetic half is solved and reusable: stage `&g[(x+K)*32+3]` through an int local one step at a time (i = b*2; i += K; i = i*32+3) to block agbcc's tree-level refold; any two steps written as one expression refold. Next attempt: start at the pool-word duplication, not the arithmetic; re-test whenever a -fforce-addr duplication rule lands in docs/agbcc-codegen.md.",
  "sub_08071918": "NOT a decompilation failure and NOT a source problem -- the C in work/sub_08071918/sub_08071918.c is byte-exact for the whole compiler-generated body. The 48-byte 'function' is 8 bytes of `movs r0, r0` padding followed by a 40-byte C function: `if ((gpKeySt->unk00 & 0x200) && (gpKeySt->held & 2)) sub_0803DDF4();`. trymatch's diff is exactly those four leading nops and nothing else -- every later instruction, both branch targets and both relocations (R_ARM_THM_CALL sub_0803DDF4, R_ARM_ABS32 gpKeySt) agree once the 8-byte shift is applied. CAUSE: 0x08071918 is the trailing alignment of the ARM/THUMB interworking veneer table that runs up to sub_08071914 (a run of `bx pc; nop` thumb stubs paired with `b <arm addr>` arm stubs). upstream's thumb_func_start label sits 8 bytes early, so the padding was absorbed into the following function. The real function begins at 0x08071920. Ruled out: the source axis entirely -- no C emits leading nops, and the body already matches. Do not spend another wave's attempts on the C. FIX IS TOOLING, NOT SOURCE: the unit boundary would have to move to 0x08071920 with the 8 padding bytes carved out as data, the same way split_rodata.py carves pool words. Worth 40 bytes, so weigh it against the risk of moving a boundary upstream owns. SCOPE (scanned every asm/*.s): only THREE functions ROM-wide begin with movs r0,r0 padding -- this one (8B), sub_0802C62A (2B, already in asm-resident.json as the known .align counter-example) and sub_0806F73C (2B pad in a 4B stub). The class is closed.",
  "sub_08043630": {
   "bytes": 44,
   "best": "95.5% -- 2 of 44 bytes differ, size exact, relocs match",
   "remaining_diff": "ONE instruction pair, at +0xa. Original: `adds r1, r0, r2` then `ldrh r0, [r1, #36]`. Candidate: `adds r0, r0, r2` then `ldrh r0, [r0, #36]`. Same two instructions, same operands, different destination register for the record address: the ROM parks &gUnknown_085C77A0[gUnknown_03003FC0.unk02] in r1 and the candidate coalesces it into r0 because r0 is where the `ldrh` result -- which is also the return value -- has to end up. Every other byte of the function, both branch targets, the double `ldrb` of unk30 and both relocations agree.",
   "why_it_is_close": "The function is `if (g085C77A0[g03003FC0.unk02].unk24 != 0) return that; if (g03003FC0.unk30 != 0) return that; return 0;`. The second `ldrb r0, [r1]` off the same address register is NOT redundancy to optimise away -- it is what the two-read source compiles to, and the candidate reproduces it exactly, including the out-of-line placement of the non-zero arm.",
   "axes_ruled_out": [
    "Return type. Probed u16 and int; byte-identical, both give `adds r0, r0, r2`.",
    "Binding the record to a pointer local (`const struct Unk085C77A0 *r = &g[i]` and the `(g + i)->unk24` pointer-arithmetic spelling). Both fold to the same tree and the same register.",
    "Binding the index to an `int i` local first. Changes nothing about the address register and additionally SWAPS THE POOL ORDER to gUnknown_03003FC0 first, which the ROM does not have -- so the index is not a local.",
    "Declaring the value local `int` rather than `u16`. No effect.",
    "Reading unk24 TWICE instead of through a local. This DOES produce the ROM's `adds r1, r0, r2` -- it is the only spelling found that does -- but the second read then survives to the tail as an extra `b` plus `ldrh r0, [r1, #36]`, i.e. +4 bytes and a worse 90.9%. The register and the single read appear to be in tension and no spelling found so far gets both.",
    "The static-helper / unit-level axis: the unit is this function alone and it calls nothing at all, so there is no relocation to be wrong."
   ],
   "settled_by_this_attempt": [
    "struct Unk085C77A0 has a u16 at +0x24 (was inside filler_1c[0x0c]); now split out as unk24 in include/unknown-globals.h.",
    "The double `ldrb` of gUnknown_03003FC0.unk30 is ordinary agbcc output for two source reads, not a volatile tell -- the candidate reproduces it with a plain non-volatile global.",
    "The branch layout: the `unk30 != 0` arm is out of line and reloads, which requires the nested `if (unk24 == 0) { ... }` shape. Flattened to three sequential `if`s agbcc cross-jumps the two returns into one `bne` and loses the reload."
   ],
   "reason": "A pure register-allocation residual of 2 bytes with the instruction stream otherwise exact. Per the wave-17 note this is the class where decomp-permuter has been measured to give nothing and reading the allocation is the lever -- but at 44 bytes the function is far below the 256-byte threshold where a permuter run is even indicated. The lever is whatever makes the address pseudo lose the coalesce with r0; the double-read spelling proves such a lever exists."
  },
  "sub_0803550C": {
   "bytes": 44,
   "best": "40 bytes emitted vs 44 -- ONE 4-byte instruction pair short. Every other instruction, every branch target, the literal-pool position and both relocations agree.",
   "remaining_diff": "The original's dispatch is `cmp r0,#1; beq ->sub_080352B4` / `cmp r0,#1; ble ->end` / `cmp r0,#2; beq ->sub_080353E8` / `b end`, with both call blocks out of line after the literal pool. The candidate emits everything except the second pair (`cmp r0,#1; ble`).",
   "why_it_is_close": "The body is `switch (gUnknown_03003FC0.unk2c) { case 1: sub_080352B4(); break; case 2: sub_080353E8(); break; }`. That spelling is confirmed by LAYOUT, not guessed: a two-case switch is the only construct probed that puts BOTH call blocks after the pool, reaches each with a forward `beq`, ends the dispatch with `b default`, and gives the first block a trailing `b default` to skip the second. Every if/else rewrite puts one block inline instead and comes out 4 bytes short a different way. So the source shape is settled and the open question is narrow: what makes agbcc emit `emit_case_nodes`' low-bound range test for a TWO-case switch.",
   "axes_ruled_out": [
    "DO NOT re-read this as a prologue or parameter-list problem. The percentage is POSITIONAL (see the wave-27 note in docs/agbcc-codegen.md) and a 4-byte length deficit shifts every later byte, which is why it scores ~29% rather than ~90%. The reported 'first difference at +0x2' is the `ldr r0, [pc, #20]` vs `[pc, #16]` pool displacement -- a CONSEQUENCE of the missing 4 bytes. Both prologues are a bare `push {lr}` and are identical.",
    "Index type: probed the u8 member direct, and s8 / s16 / int locals. s8 adds `lsl #24; asr #24` and none of the four emits the low-bound test. The `ble` is a SIGNED compare, which independently rules out an unsigned index type.",
    "Case VALUES: probed {5,6} as well as {1,2}. Neither emits the test, so the value 1 is not special and agbcc simply never emits the low-bound pair at count 2.",
    "Extra case labels: `case 3:`/`case 4:` empty, and `case 0x80:`, DO reach the tree -- but balance_case_nodes then roots it at 2 and emits `cmp #2; beq / cmp #2; bgt / cmp #1; bne`, a different and wrong shape.",
    "An explicit `default: break;`: byte-identical to omitting it.",
    "`return` instead of `break` in each arm: byte-identical to `break`.",
    "if/else-if `if (v == 1) A(); else if (v > 1) { if (v == 2) B(); }` with an `int` v: produces the ROM's `cmp #1; ble` EXACTLY (agbcc folds `> 1` to `<= 1`), so that pair's provenance is understood -- but it puts the A block inline and reaches it with `bne`.",
    "The same chain with the arms inverted (`if (v != 1) { ... } else A();`): moves A out of line and gets `beq`, but then inlines B and loses the dispatch's trailing `b end`. Both halves of the ROM's layout have been reproduced, never together."
   ],
   "wave": 28,
   "agent": "W28-A"
  },
  "sub_0801C640": {
   "bytes": 60,
   "best": "60 bytes (exact), 88.3% identical -- 7 of 60 bytes",
   "remaining_diff": "Size, relocations and the instruction SEQUENCE are all exact. The only difference is that two quantities get each other's hard register in the second cursor chain: the ROM keeps the cursor in r0 and the one-use inner offset in r1 (`adds r0,r2,r0` / `ldrh r1,[r0]` / `adds r0,r0,r1`), ours is the other way round (`adds r1,r2,r0` / `ldrh r0,[r1]` / `adds r0,r1,r0`). The `adds rD,rD,rM` tie from `p += ...` is already right in both, so this is local-alloc's priority order between the longer-lived cursor and the one-use temp.",
   "why_it_is_close": "Everything else about the function is settled and measured. `lsrs #1; lsls #1` is `x / 2 * 2` and NOT `x & ~1` -- `& ~1` has to build -2 with `movs; rsbs` and hold it in a register, which gives this leaf a `push {r4, lr}` the ROM does not have, so the absence of a prologue is what proves the shift spelling. The +0x20 bit-1 test selecting a u16 self-relative encoding against a flat u32 pointer table is confirmed by sub_0801C4D4 doing the identical thing with an index. sub_0801C4D4 is parked on exactly the same register-allocation axis, so one probe aimed at agbcc's local-alloc priority would very likely unblock both.",
   "axes_ruled_out": [
    "x & ~1 for the shift pairs -- SETTLED THE OTHER WAY, it adds a push",
    "separate `p` and `v` locals -- v lands in r0 and p in r1, 8 bytes off",
    "one `p` reused for the +0x04 value and the cursor -- 7 bytes off, the current best",
    "one `v` reused across BOTH arms including the else arm -- 7 bytes off",
    "`v = p + ...` versus `p += ...` -- bit-identical output",
    "declaring the cursor u8 * / void * / u16 * -- bit-identical"
   ],
   "settled_by_this_attempt": "The script-data model: +0x00 is the installed script, +0x04/+0x08/+0x0c are three cursors derived from it, and handle +0x20 bit 1 picks the encoding. This is now recorded on struct Unk0801C210 in include/unknown-globals.h.",
   "wave": "30 (W30-B)"
  },
  "sub_0801C4D4": {
   "bytes": 72,
   "best": "72 bytes (exact), 86.1% identical -- 10 of 72 bytes",
   "remaining_diff": "Size, relocations and every instruction are exact. The handle and the script base simply get each other's register: the ROM has `adds r2, r0, #0` / `ldr r3, [r2]` (handle r2, base r3), ours has `adds r3, r0, #0` / `ldr r2, [r3]`. The ten differing bytes are exactly the ten instructions that name one of the two.",
   "why_it_is_close": "Both quantities are live over the same span and both have five references, so this is local-alloc's tie-break. Under gcc 2.9's `n_refs * floor_log2(n_refs) / (death - birth)` the base wins because it is born later and dies earlier, and no rewrite reached a shape where the handle wins -- lengthening the base's range by reusing one variable for the result raises its priority instead of lowering it. sub_0801C640 next door is parked on the same axis.",
   "axes_ruled_out": [
    "x & ~1 instead of / 2 * 2 -- worse, costs a register for -2",
    "`base` as a local versus `a1->unk00` spelled out at every use -- CSE gives one quantity either way",
    "u8 * / void * / u16 * for that local -- bit-identical",
    "a1->unk08 = v; a1->unk0c = v;  vs  a1->unk0c = a1->unk08 = v;  vs  a1->unk0c = a1->unk08; -- the reload is CSEd away so it adds no ref",
    "one variable for base and result so its range spans to the stores -- raises its priority, wrong direction",
    "guard as two early returns, as one `||`, and as a block-scoped `if (a1 && a1->unk00) { ... }`",
    "`p += ...` versus `v = p + ...` in the then-arm -- bit-identical"
   ],
   "settled_by_this_attempt": "Second parameter is the entry INDEX into the script's table (`lsls #1` on the u16 path, `lsls #2` plus a +4 displacement on the u32 path), which is why sub_0801C640 is the same body with the index pinned to 0.",
   "wave": "30 (W30-B)"
  },
  "sub_08080EB0": "96.2% by size -- 48 bytes against 52, every instruction in order and every pool word placed correctly. The ONLY difference is one register-allocation artefact: the ROM opens `adds r1, r0, #0` and does both `cmp` against r1, while every candidate keeps the parameter in r0 and compares r0 directly (2 bytes, plus the 2-byte alignment pad they push the pool past). The shape IS settled: else-if with a per-branch `return <cmp>`, and the fall-through returns the PARAMETER, not a literal 0 -- r0 is untouched when the argument is neither 1 nor 2, and a literal 0 costs a `movs r0, #0` the ROM does not have. Ruled out by probe, none of which produced the copy: `if (Proc_Find(x)) return 1;` falling through to a shared `return type` (makes the parameter live across the `bl`, so it lands in r4 and the prologue becomes `push {r4, lr}` -- this is what proves the per-branch return); an uninitialised `int result` assigned in the branches (gcc cross-jumps the two `bl Proc_Find` sites and uses r4); `int t = type;` comparing t and returning t (emits the copy INVERTED -- compares r0 and restores `add r0, r1, #0` at the tail -- and cross-jumps the calls); `int t = type;` comparing t and returning `type` (coalesced, no copy); `switch (type)` with and without a default (both compares hoisted to the top as a decision list, which the ROM does not do); a local ProcPtr holding the call result; and `?:` in place of `!=`. The ROM's shape needs the parameter's pseudo to LOSE r0 to the return value WITHOUT becoming live across a call, and no source spelling reproduces that. Same species as sub_0801A6C0 -- worth re-testing whenever a register-allocation rule lands in docs/agbcc-codegen.md. WAVE 30 (W30-D). Draft at work/sub_08080EB0/.",
  "sub_08087514": "92.3% by size -- 48 bytes against 52, every instruction and every register assignment identical to the ROM except the FIRST ARGUMENT. The ROM builds it in four instructions -- `movs r0,#5 / subs r3,r0,r3 / adds r0,r3,#0 / adds r0,#0xb8` -- computing (5 - a) and adding 0xB8 as a separate step. agbcc folds every spelling tried to the two-instruction `movs r0,#0xbd; subs r0,r0,r3`: `0xB8 + 5 - a`, `0xB8 + (5 - a)`, `(5 - a) + 0xB8`, `5 + 0xB8 - a`, `int t = 5 - a; t + 0xB8`, `int t = 5 - a; t += 0xB8;`, and the u32 version of the last two. The fold is tree-level in the front end for the literal forms and propagate-then-combine for the temporaries, so blocking it needs the subtraction's result to have a SECOND use, and nothing in this function gives it one. Everything else is settled and cost real probes: the clamp is UNSIGNED (`bhi`), hence `u32` on the first parameter; the row index must be written `b * 2 + 5 + c` and NOT `b * 2 + (c + 5)` -- agbcc reassociates the first into the ROM's `lsls r2,r1,#1; adds r1,r4,#5; adds r2,r2,r1` and the second into `lsl; add #5; add r5`, which also costs an extra callee-saved register; and the byte offset must sit INSIDE the pointer add, `p + (idx * 32 + 9)` rather than `p + idx * 32 + 9`, or the `adds #0x12` lands after the base add instead of before it. WAVE 30 (W30-D). Draft at work/sub_08087514/.",
  "sub_08073930": {
   "bytes": 68,
   "best": "68 bytes (exact), 85.3% identical -- 10 of 68 bytes",
   "remaining_diff": "Size and relocations are exact and the instruction sequence matches. The ROM holds a pool word in r2 where ours holds it in r3, first visible at +0xb (`ldr r2, [pc, #44]` vs `ldr r3, [pc, #44]`); the choice then propagates through every instruction naming either register.",
   "why_it_is_close": "Everything except the register assignment is already right, so this is the same local-alloc tie-break axis as sub_0801C4D4 and sub_0801C640 parked this wave -- but unlike those two it has had NO attempt on that axis yet, because the wave ran out of budget here.",
   "axes_ruled_out": [],
   "read_this_first": "include/unknown-globals.h already records two facts about this function that a fresh attempt will otherwise re-derive wrongly. (1) It reaches its slot through agbcc's own -fforce-addr word at 0x081CC02C, whose ROM contents are 0x0202FDE4, so the three-level load here is ONE indirection of compiler bookkeeping and NOT a pointer to a pointer -- a draft modelling it as the latter looks structurally right and allocates differently. (2) gUnknown_0202F8DC is read at stride FOUR with the halfword at +2 (two entries per scanline), which contradicts an older note above it claiming a one-halfword table; that reconciliation is still open and is the most likely source-level lever here. Settle the type model before touching allocation.",
   "wave": "30 (W30-A)"
  },
  "sub_0802FA64": {
   "bytes": 56,
   "best": "56 bytes (exact size), 87.5% identical -- 7 of 56 bytes differ, first at +0xa",
   "remaining_diff": "The instruction SEQUENCE is exact and so are the three pool words and their order (gUnknown_0202575C, gUnknown_0849B018, gUnknown_03003FC0). Two quantities have each other's hard register: the ROM keeps the gUnknown_0849B018 dereference in r3 and the gUnknown_03003FC0 address in r2, ours keeps them in r2 and r3. The one consequence is the tied operand of the index add -- ROM `adds r2, r2, r1` then `adds r2, #0x3e` (result in the BASE register), ours `add r1, r1, r3` then `add r1, r1, #0x3e` (result in the INDEX register). Nothing else in the function differs.",
   "why_it_is_close": "Everything about the address expression is already settled by the listing. The 0x3e is added to the address AFTER the index and the pool word is the bare `gUnknown_03003FC0`, which rules out every member-array spelling: `g.field[i]` folds `&g + 0x3e` into the pool word (measured, three ways). So the source is an address constant offset by a runtime index and then displaced by 0x3e, which is exactly what the parked draft writes. This is the same failure as sub_0801C4D4 and sub_0801C640 -- see 'OPEN: two simultaneously-live pointers can swap registers with no source lever' in docs/agbcc-codegen.md -- and it is a THIRD instance, which makes the minimal-probe experiment that section asks for worth more than another rewrite here.",
   "axes_ruled_out": [
    "member array `gUnknown_03003FC0.unk3e[i]` and every equivalent (`((u8 *)&g + 0x3e)[i]`, `((u8 *)&g)[i + 0x3e]`) -- all fold &g+0x3e into the literal pool, which the ROM does not do",
    "pointer local declared and assigned BEFORE the unk00/unk01 stores -- moves gUnknown_03003FC0's pool word to slot 0; the ROM has it in slot 2",
    "pointer local assigned after the unk01 store, as one statement (`p = (u8 *)&g + i; p[0x3E]`) -- correct pool order, wrong registers",
    "the same split into two statements (`p = (u8 *)&g; p += i;` and `p = p + i + 0x3E;`) -- identical output to the one-statement form",
    "binding the index to its own `int` or `s8` local first, so the add's operands are two plain pseudos -- identical",
    "writing the sum index-first (`*(g0849B018->unk06 + (u8 *)&g + 0x3E)`) to flip the tied operand -- gcc canonicalises, identical",
    "binding gUnknown_0849B018 to a local `struct Unk0849B018 *` -- moves ITS pool word to slot 0, still r2 for the deref",
    "binding gUnknown_0202575C to a local `struct Unk0202575C *` -- same, pool order breaks",
    "reaching the byte through a cast struct pointer with a member at 0x3e (`((struct S *)((u8 *)&g + i))->unk3e`), both inline and via a local -- identical",
    "the volatile double read of gUnknown_0849B018->unk06 is NOT the issue: the ROM's two `ldrb [r3,#6]` are reproduced exactly, and unk06 is already volatile in unknown-globals.h"
   ],
   "wave": "31 (W31-A)"
  },
  "sub_08029868": {
   "bytes": 24,
   "best": "24 bytes (exact size), 91.7% identical -- 2 of 24 bytes differ, first at +0xa",
   "remaining_diff": "One compare constant. ROM: `cmp r0, #2; bgt` then `cmp r0, #1; blt`. Ours: `cmp r0, #2; bgt` then `cmp r0, #0; ble`. Everything else -- the u8 prologue `lsls/lsrs #0x18`, the first compare, `movs r0, #0x50`, the call and the epilogue -- is byte-exact.",
   "why_it_is_close": "Two of the three levers are found and are in the parked draft. (1) The compares are SIGNED, which a plain `u8` parameter does not give: agbcc uses the 0..255 range and picks `bhi`/`beq`. Binding the parameter to an `int` local first is what loses the range and produces `bgt`. (2) That same binding is what stops the two bounds fusing into the `subs #1; cmp #1; bhi` range check a `&&` produces. What is left is that agbcc canonicalises BOTH `v < 1` and `v >= 1` to the zero form (`cmp #0` with `ble`/`bgt`), so the literal 1 in the ROM's second compare has to reach the back end some other way.",
   "axes_ruled_out": [
    "`u8` parameter with `a <= 2 && a >= 1` -- fuses to `subs #1; cmp #1; bhi`, 5 instructions",
    "`u8` parameter with nested ifs -- fuses identically",
    "`u8` parameter with two early returns -- unsigned `bhi` and `beq`, both compares wrong",
    "`s8` parameter -- prologue becomes `lsls/asrs`, so the entry narrowing is wrong before any compare",
    "`int v = a;` then two early returns (`v > 2`, `v < 1`) -- correct `bgt`, but `v < 1` canonicalises to `cmp #0; ble`",
    "`int v = a;` then `v > 2` early-out and `v >= 1` guarding the call -- correct `bgt`, `v >= 1` canonicalises to `cmp #0; bgt`, same two bytes wrong",
    "`int v = a;` with `v <= 2 && v >= 1` -- the range fusion comes back"
   ],
   "wave": "31 (W31-A)"
  },
  "sub_08029948": {
   "bytes": 48,
   "best": "48 bytes (exact size); exact instruction sequence and exact pool words, one instruction in the wrong slot",
   "remaining_diff": "The array's literal-pool load is INTERLEAVED into the sign-extension in the ROM: `lsls r0, r0, #0x18` / `ldr r2, =gUnknown_03001470` / `asrs r0, r0, #0x18`. Every candidate puts that `ldr` either before both shifts or after both. Nothing else differs -- the u16 narrowing of the parameter, the sub_080152C0 call, the `x * 3 << 5` stride chain and the `strh [r1, #0x22]` are all exact.",
   "why_it_is_close": "The two-instruction sign-extend is ONE RTL operation, so no source-level reordering can place another insn between its halves; the address pseudo is created either before the extend is expanded or after. That makes this an agbcc placement rather than a spelling, the same class as sub_0802FA64's register swap in this wave and sub_0801C4D4 / sub_0801C640 from wave 30. Worth revisiting only after the minimal register-allocation probe docs/agbcc-codegen.md asks for.",
   "axes_ruled_out": [
    "subscript written inline (`gUnknown_03001470[(s8)f()].unk22 = ...`) -- ldr BEFORE both shifts",
    "`s8 i = f();` then the subscript -- ldr BEFORE both shifts",
    "`struct Unk03001470 *p = &gUnknown_03001470[f()];` then `p->unk22` -- ldr BEFORE both shifts",
    "`int i = (s8)f();` then the subscript -- ldr AFTER both shifts",
    "the stored value spelled `(u16)a` against a bare `a` -- byte-identical, the parameter narrowing is unaffected either way"
   ],
   "wave": "31 (W31-A)"
  },
  "sub_08018100": {
   "bytes": 92,
   "best": "22.8%",
   "remaining_diff": "ONE extra instruction, `adds r5, r1, #0` at +0x06, and the register renaming it drags through the body. The ROM puts the promoted parameter straight into r5 (`lsls r0, r0, #0x10; lsrs r5, r0, #0x10`) and reads r5 at both uses; every candidate lands it in r1 and copies it to r5, then reads r1 before the call and r5 after.",
   "why_it_is_close": "Pool-word order, the .rodata address constant for gUnknown_0200C528 (which the disassembly calls gUnknown_0808E568), the whole if-body and the whole tail are instruction-for-instruction right. The percentage is low because best.json counts register fields and the extra copy shifts every following register number.",
   "axes_ruled_out": [
    "binding the node pointer to a local inside the if",
    "binding the halfword to a local and passing it to sub_0801815C",
    "`u16 a` parameter with `(s16)a` at both uses",
    "an extra unused second parameter (changes entry liveness, not the copy)",
    "folding the global assignment into the call argument",
    "inverting the test and duplicating the increment into both arms",
    "naming the node twice so CSE sees a third reference"
   ],
   "settled_by_this_attempt": "The copy tracks the CSE-HOISTED BASE POINTER, not the parameter. sub_08018464 -- matched, same block, same shape, but with only ONE reference to gUnknown_0200C528 so no hoist -- carries the identical copy in the ROM (`lsrs r1, r0, #0x10; adds r4, r1, #0`), and a probe with the base not hoisted drops it. So the ROM coalesced a parameter copy that agbcc declines to coalesce whenever that base is live across the call. Register allocation, not structure: do not rewrite the body.",
   "reason": "wave 32 (W32-A)"
  },
  "sub_08060110": {
   "bytes": 96,
   "best": "68.8%",
   "remaining_diff": "Size-exact. The ROM loads `ldr r3, =gUnknown_08499594` IMMEDIATELY after the `bl sub_08025E08`, ahead of gUnknown_08499590, and derefs it (`ldr r1, [r3]`) only after the `* 12`; the candidate loads that pool word just before the deref instead. Same instruction count, but it changes which register holds the 0x417A constant (the ROM needs r5, hence `push {r4, r5, lr}` against the candidate's `push {r4, lr}`) and renames the tail.",
   "why_it_is_close": "Every instruction is present and in the right order; only the placement of one pool load and the allocation that follows from it differ.",
   "axes_ruled_out": [
    "the three stores written as `gUnknown_08499594[cell].unkNN` with no bound element pointer -- agbcc then RELOADS the table pointer for each store, because a `strb` through a `struct Unk08499594 *` may alias the pointer global; 8 bytes worse",
    "splitting `cell` into its own statement ahead of the `u = &...` binding -- moves nothing"
   ],
   "settled_by_this_attempt": "The cell plane here is +0x12 (c_08008B70.c's idiom), not the +0x1432 one; the gUnknown_08499594 stride is 12; and unk09/unk0a/unk0b are three u8 members sharing ONE element address, now declared. The `u` binding is required and must stay. Its sibling sub_080601F0 wants the OPPOSITE lever -- an explicit `tbl = gUnknown_08499594;` statement to pull the deref FORWARD -- so try that here next.",
   "reason": "wave 32 (W32-A)"
  },
  "sub_08070478": "88.6% -- 44 bytes, SIZE-EXACT, and the entire residual is a two-register exchange: the ROM keeps &gUnknown_08242308 (the mplay table) in r2 and song->ms in r3; every candidate produces them the other way round. Instruction stream, literal-pool order and size are otherwise the ROM's. m4aSongNumStart. WAVE 32 (W32-B) ran the wave-31 allocation rule -- measured by this same agent -- against it, and THE RULE PREDICTS THE CANDIDATE, NOT THE ROM. gcc-2.9 local-alloc priority is floor_log2(n_refs) * n_refs * size / (death - birth), sorted descending: the table address has n_refs 2 and lives 8 insns (priority 0.25); ms has n_refs 3 and lives 2 insns (priority 1.50). ms is therefore allocated first and takes the lower register r2, which is exactly the candidate. THE THREE QUESTIONS, ANSWERED: (1) ref-count power-of-two crossing -- NOT REACHABLE; to overtake ms the table address needs floor_log2(n)*n > 12, i.e. 8 refs, and it has 2 in every spelling; crossing 3->4 only lifts it to 1.00, still under 1.50. (2) tie-break by declared local -- DOES NOT APPLY; 0.25 against 1.50 is not a tie, and measurement confirms declaration order moves the LITERAL POOL WORDS and never these two registers. (3) an index add whose operand order would have to change -- NO; both adds already carry the symbolic base as operand 2, in the ROM and in every candidate alike. All three levers absent means the difference is UPSTREAM OF STATEMENT SHAPE. RECORDED AS A TYPE-MODEL QUESTION, which is where the rule says to stop: something in struct MusicPlayer / struct Song / the two tables' declared types must give the table address either more references or a shorter live range than the current model does. W31-C's independent read was the same, naming struct Song / struct MusicPlayerInfo. RULED OUT: W31-C's twelve spellings (songTable/mplay locals present and absent, u16/u32/int ms local, &t[n] vs t+n, (p+i)->info vs p[i].info, info/header bound first, both tables fully inlined, const and non-const tables, both declaration orders) plus W32-B's three (fully inlined with no locals at all, the mplay ELEMENT bound instead of the table, index bound to a local declared before the tables). Fifteen spellings, one allocation.",
  "sub_08070544": "90.4% -- 52 bytes, SIZE-EXACT, and the entire residual is a two-register exchange: the ROM keeps &gUnknown_08242308 (the mplay table) in r2 and song->ms in r3; every candidate produces them the other way round. Instruction stream, literal-pool order and size are otherwise the ROM's. m4aSongNumStop. WAVE 32 (W32-B) ran the wave-31 allocation rule -- measured by this same agent -- against it, and THE RULE PREDICTS THE CANDIDATE, NOT THE ROM. gcc-2.9 local-alloc priority is floor_log2(n_refs) * n_refs * size / (death - birth), sorted descending: the table address has n_refs 2 and lives 8 insns (priority 0.25); ms has n_refs 3 and lives 2 insns (priority 1.50). ms is therefore allocated first and takes the lower register r2, which is exactly the candidate. THE THREE QUESTIONS, ANSWERED: (1) ref-count power-of-two crossing -- NOT REACHABLE; to overtake ms the table address needs floor_log2(n)*n > 12, i.e. 8 refs, and it has 2 in every spelling; crossing 3->4 only lifts it to 1.00, still under 1.50. (2) tie-break by declared local -- DOES NOT APPLY; 0.25 against 1.50 is not a tie, and measurement confirms declaration order moves the LITERAL POOL WORDS and never these two registers. (3) an index add whose operand order would have to change -- NO; both adds already carry the symbolic base as operand 2, in the ROM and in every candidate alike. All three levers absent means the difference is UPSTREAM OF STATEMENT SHAPE. RECORDED AS A TYPE-MODEL QUESTION, which is where the rule says to stop: something in struct MusicPlayer / struct Song / the two tables' declared types must give the table address either more references or a shorter live range than the current model does. W31-C's independent read was the same, naming struct Song / struct MusicPlayerInfo. RULED OUT: W31-C's twelve spellings (songTable/mplay locals present and absent, u16/u32/int ms local, &t[n] vs t+n, (p+i)->info vs p[i].info, info/header bound first, both tables fully inlined, const and non-const tables, both declaration orders) plus W32-B's three (fully inlined with no locals at all, the mplay ELEMENT bound instead of the table, index bound to a local declared before the tables). Fifteen spellings, one allocation.",
  "sub_08070578": "90.4% -- 52 bytes, SIZE-EXACT, and the entire residual is a two-register exchange: the ROM keeps &gUnknown_08242308 (the mplay table) in r2 and song->ms in r3; every candidate produces them the other way round. Instruction stream, literal-pool order and size are otherwise the ROM's. m4aSongNumContinue. WAVE 32 (W32-B) ran the wave-31 allocation rule -- measured by this same agent -- against it, and THE RULE PREDICTS THE CANDIDATE, NOT THE ROM. gcc-2.9 local-alloc priority is floor_log2(n_refs) * n_refs * size / (death - birth), sorted descending: the table address has n_refs 2 and lives 8 insns (priority 0.25); ms has n_refs 3 and lives 2 insns (priority 1.50). ms is therefore allocated first and takes the lower register r2, which is exactly the candidate. THE THREE QUESTIONS, ANSWERED: (1) ref-count power-of-two crossing -- NOT REACHABLE; to overtake ms the table address needs floor_log2(n)*n > 12, i.e. 8 refs, and it has 2 in every spelling; crossing 3->4 only lifts it to 1.00, still under 1.50. (2) tie-break by declared local -- DOES NOT APPLY; 0.25 against 1.50 is not a tie, and measurement confirms declaration order moves the LITERAL POOL WORDS and never these two registers. (3) an index add whose operand order would have to change -- NO; both adds already carry the symbolic base as operand 2, in the ROM and in every candidate alike. All three levers absent means the difference is UPSTREAM OF STATEMENT SHAPE. RECORDED AS A TYPE-MODEL QUESTION, which is where the rule says to stop: something in struct MusicPlayer / struct Song / the two tables' declared types must give the table address either more references or a shorter live range than the current model does. W31-C's independent read was the same, naming struct Song / struct MusicPlayerInfo. RULED OUT: W31-C's twelve spellings (songTable/mplay locals present and absent, u16/u32/int ms local, &t[n] vs t+n, (p+i)->info vs p[i].info, info/header bound first, both tables fully inlined, const and non-const tables, both declaration orders) plus W32-B's three (fully inlined with no locals at all, the mplay ELEMENT bound instead of the table, index bound to a local declared before the tables). Fifteen spellings, one allocation.",
  "sub_080704A4": "93.4% -- 76 bytes, SIZE-EXACT, and the entire residual is a two-register exchange: the ROM keeps &gUnknown_08242308 (the mplay table) in r2 and song->ms in r3; every candidate produces them the other way round. Instruction stream, literal-pool order and size are otherwise the ROM's. m4aSongNumStartOrChange; only FIVE bytes differ. NEW in wave 32: writing the song lookup INLINE rather than through a song local is what fixes the LITERAL POOL ORDER on this one and on sub_080704F0 -- with the local, the song table is referenced first and its pool word comes first, the reverse of the ROM. That halved the residual and brought both conditional members onto the identical single difference the other three carry. WAVE 32 (W32-B) ran the wave-31 allocation rule -- measured by this same agent -- against it, and THE RULE PREDICTS THE CANDIDATE, NOT THE ROM. gcc-2.9 local-alloc priority is floor_log2(n_refs) * n_refs * size / (death - birth), sorted descending: the table address has n_refs 2 and lives 8 insns (priority 0.25); ms has n_refs 3 and lives 2 insns (priority 1.50). ms is therefore allocated first and takes the lower register r2, which is exactly the candidate. THE THREE QUESTIONS, ANSWERED: (1) ref-count power-of-two crossing -- NOT REACHABLE; to overtake ms the table address needs floor_log2(n)*n > 12, i.e. 8 refs, and it has 2 in every spelling; crossing 3->4 only lifts it to 1.00, still under 1.50. (2) tie-break by declared local -- DOES NOT APPLY; 0.25 against 1.50 is not a tie, and measurement confirms declaration order moves the LITERAL POOL WORDS and never these two registers. (3) an index add whose operand order would have to change -- NO; both adds already carry the symbolic base as operand 2, in the ROM and in every candidate alike. All three levers absent means the difference is UPSTREAM OF STATEMENT SHAPE. RECORDED AS A TYPE-MODEL QUESTION, which is where the rule says to stop: something in struct MusicPlayer / struct Song / the two tables' declared types must give the table address either more references or a shorter live range than the current model does. W31-C's independent read was the same, naming struct Song / struct MusicPlayerInfo. RULED OUT: W31-C's twelve spellings (songTable/mplay locals present and absent, u16/u32/int ms local, &t[n] vs t+n, (p+i)->info vs p[i].info, info/header bound first, both tables fully inlined, const and non-const tables, both declaration orders) plus W32-B's three (fully inlined with no locals at all, the mplay ELEMENT bound instead of the table, index bound to a local declared before the tables). Fifteen spellings, one allocation.",
  "sub_080704F0": "94.0% -- 84 bytes, SIZE-EXACT, and the entire residual is a two-register exchange: the ROM keeps &gUnknown_08242308 (the mplay table) in r2 and song->ms in r3; every candidate produces them the other way round. Instruction stream, literal-pool order and size are otherwise the ROM's. m4aSongNumStartOrContinue; only FIVE bytes differ. See sub_080704A4 for the inline-lookup fix to the pool order. WAVE 32 (W32-B) ran the wave-31 allocation rule -- measured by this same agent -- against it, and THE RULE PREDICTS THE CANDIDATE, NOT THE ROM. gcc-2.9 local-alloc priority is floor_log2(n_refs) * n_refs * size / (death - birth), sorted descending: the table address has n_refs 2 and lives 8 insns (priority 0.25); ms has n_refs 3 and lives 2 insns (priority 1.50). ms is therefore allocated first and takes the lower register r2, which is exactly the candidate. THE THREE QUESTIONS, ANSWERED: (1) ref-count power-of-two crossing -- NOT REACHABLE; to overtake ms the table address needs floor_log2(n)*n > 12, i.e. 8 refs, and it has 2 in every spelling; crossing 3->4 only lifts it to 1.00, still under 1.50. (2) tie-break by declared local -- DOES NOT APPLY; 0.25 against 1.50 is not a tie, and measurement confirms declaration order moves the LITERAL POOL WORDS and never these two registers. (3) an index add whose operand order would have to change -- NO; both adds already carry the symbolic base as operand 2, in the ROM and in every candidate alike. All three levers absent means the difference is UPSTREAM OF STATEMENT SHAPE. RECORDED AS A TYPE-MODEL QUESTION, which is where the rule says to stop: something in struct MusicPlayer / struct Song / the two tables' declared types must give the table address either more references or a shorter live range than the current model does. W31-C's independent read was the same, naming struct Song / struct MusicPlayerInfo. RULED OUT: W31-C's twelve spellings (songTable/mplay locals present and absent, u16/u32/int ms local, &t[n] vs t+n, (p+i)->info vs p[i].info, info/header bound first, both tables fully inlined, const and non-const tables, both declaration orders) plus W32-B's three (fully inlined with no locals at all, the mplay ELEMENT bound instead of the table, index bound to a local declared before the tables). Fifteen spellings, one allocation.",
  "sub_0808B074": "32.8% -- 116 bytes, SIZE-EXACT, and the whole difference is ONE surplus instruction: an add rD, rMask, #0 copy in front of the first ands. The size still matches because the ROM carries 2 bytes of .short 0x0000 pool alignment and the extra instruction lands in that slot, so the low percentage is register renaming plus a 2-byte shift of everything after it, not 78 bytes of wrong code. CAUSE, MEASURED (wave 32, W32-B): the ROM writes ands r1, r6 -- destination is the freshly loaded REG_WAITCNT value, mask untouched in r6 across both uses and across the bl -- while agbcc makes the POOL CONSTANT operand 0 of the &, so a mask with a later use must be copied first. At the mask's LAST use, where it dies, agbcc emits no copy and the two agree; that asymmetry is what identifies the cause. The | in the same expression keeps its operand order perfectly (the ROM's orrs r1, r0 is reproduced), so this is specific to a commutative op whose second operand is an integer constant too large for a THUMB immediate. RULED OUT, eleven spellings, none removes the copy: (REG_WAITCNT & 0xfffc) | v; v | (REG_WAITCNT & 0xfffc); (REG_WAITCNT & ~3) | v; (0xfffc & REG_WAITCNT) | v; u16 t = REG_WAITCNT then t &= 0xfffc then t |= v; the same with int t; int t = REG_WAITCNT & 0xfffc then t |= v; u16 t with t &= ~3; a u16 m = 0xfffc mask local; and a non-volatile *(u16 *)0x04000204 read in place of the REG_WAITCNT macro. Compound assignment does NOT flip it, which is the surprising part. The ROM proves agbcc CAN emit ands rVal, rConst, so a lever exists and is somewhere other than the expression -- most likely in the surrounding register pressure. Written up in docs/agbcc-codegen.md. Sole difference on all three of sub_0808B074, sub_0808B0E8 and sub_0808B3C0.",
  "sub_0808B0E8": "42.3% -- 156 bytes, SIZE-EXACT. Identical single cause to sub_0808B074 (agbcc makes the 0xfffc pool constant operand 0 of the & and copies it, where the ROM writes ands r1, r5 into the loaded value); see that entry for the eleven-spelling measurement. Everything else is reproduced, including the r8 spill of the WAITCNT address, the 0x0E000000 + (sector << unk08) sector arithmetic and the shared epilogue the 0x80ff early return branches into.",
  "sub_0808B3C0": "23.2% -- 116 bytes against 112 (+4). Identical single cause to sub_0808B074; +4 rather than size-exact only because this one has no pool-alignment slot to absorb the surplus copy. This is sub_0808B074 with the trampoline copy hoisted to the top and the wait-state value taken from gUnknown_084856A4[0x12] rather than the chip descriptor.",
  "sub_080228D8": "78.2% -- 156 bytes, SIZE-EXACT, and the whole residual is ONE instruction's position, repeated in both arms. The ROM has bl __umodsi3; lsls r0,r0,#1; ldr r1,[pc,#36]; adds r0,r0,r1; agbcc emits the palette table's pool ldr BEFORE the shift, which also swaps the two pool words' order. Everything else is exact: the unk20 <= 7 guard, the 0x80-stride source address, the gUnknown_03001470 stride-0x60 index, the unsigned shift and __umodsi3, and both call argument sets. RULED OUT (wave 32, W32-B, six spellings): g + expr, &g[expr] and expr + g all hoist the table into a CALLEE-SAVED register ahead of the __umodsi3 call and swap the pool words -- two instructions wrong; binding the index to a local first (u32 i, int i, or via a u16 *p = g pointer local) FIXES the hoist and the pool order and is kept in the draft, but all three still place that ldr one instruction too early. So half the fix is known and the remainder is purely where agbcc schedules a pool load relative to the lsls that scales the index. NOT a type-model problem: sub_08013664's first parameter is u16 * on the promoted definition in c_080135F4.c, and the scaling by 2 is exactly that element size.",
  "sub_0803F908": "63.2% -- 132 bytes against 136 (-4). Everything is reproduced: the three-level gUnknown_08499590 read through agbcc's own -fforce-addr word at 0x08091308, the +0x417A rowOffset / +0x234A flag-plane lookup, the (u16)((a4 + 8) * 0x1000) shift pair, and the cross-jumped join. The residual is the two scaled coordinates: the ROM computes each into its own register and then copies it into the argument register (subs r4, r1, r0 ... adds r1, r4, #0, and likewise for r5/r2), where the candidate computes them straight into r1 and r2 -- two instructions shorter. RULED OUT: binding both to int locals, the lever that closed exactly this shape in sub_0803F550 the same wave, does NOT reintroduce the copies here (measured, both spellings) -- with only one call in the function agbcc has no reason to keep them off the argument registers, whereas sub_0803F550's two preceding calls forced it. NEXT THING TO TRY: compute both coordinates BEFORE the oam2 if/else rather than after it, so they span the branch and need real registers. That is one probe and it is where I would start.",
  "sub_0801914C": {
   "bytes": 100,
   "best": "87.0%",
   "remaining_diff": "SIZE-EXACT, and every one of the thirteen bytes is a register name. Two independent swaps: the parameter takes r5 and the CSEd table base r4 where the ROM has them the other way round, and `w` takes r1 with `v` in r0 where the ROM has `w` in r0 and `v` in r2. No instruction is added, removed or reordered.",
   "why_it_is_close": "The instruction stream is exact; best.json scores register fields, which is why a purely nominal difference reads as 87%.",
   "axes_ruled_out": [
    "`w` and `v` in either declaration order",
    "a bound `struct Unk0200C528Node *p` declared first -- the shape that matched sub_080188D0",
    "naming the node inline with no `p` at all",
    "a ternary for the fallback: WORSE, agbcc folds the `- 1` into both arms (`movs r2, #4`) and loses the shared `subs`"
   ],
   "settled_by_this_attempt": "Wave 31's three-question sort was run in full and ends at question 3. (1) The parameter and the table base BOTH have three references, so there is no power-of-two crossing to reach and no fourth reference exists. (2) They are not tied: the base is born later and dies at the same point, so it has the shorter live range and the higher priority -- which is what the formula predicts and what THIS candidate produces. The ROM is the one that disagrees with the formula. (3) Neither feeds an index add whose operands would have to move; the base is already operand 2 of `adds r0, r0, r2` in both. That is a type-model question, not a source-spelling one.",
   "reason": "wave 32 (W32-A)"
  },
  "sub_080193B0": {
   "bytes": 84,
   "best": "33.3%",
   "remaining_diff": "80 bytes against 84 -- ONE instruction short. The ROM keeps TWO extensions of sub_08019290's result off one `lsls` (`lsrs r2` for a zero-extended home, `asrs r0` for the `== -1` test) and re-derives the subscript from the zero-extended copy. This candidate keeps only the sign-extended form and indexes with it directly.",
   "why_it_is_close": "The three openers, the NULL return, all four stores, the shared `movs r4, #0` and the CSEd slot address are all exact; only the index variable's home differs.",
   "axes_ruled_out": [
    "`s16 i` -- agbcc keeps an s16 local SIGN-extended, giving the single `asr`",
    "`u16 i` with `(s16)i` at both uses -- gcc collapses it back to the same single `asr`, because it knows sub_08019290's result is already narrow",
    "retyping sub_08019290's return from `s16` to `int` to defeat that knowledge: TESTED with a stand-in `int`-returning prototype and it does NOT restore the second extension, so the retype would have been wrong as well as unnecessary -- and it would have broken c_08019850.c, whose `sub_08019290(...) != -1` depends on the `s16` re-narrow"
   ],
   "settled_by_this_attempt": "The local is a `u16` whose uses are all `(s16)` -- the exact idiom sub_08019290's OWN loop counter uses (`adds r0, r1, #1; lsls r0, r0, #0x10; lsrs r1, r0, #0x10; asrs r0, r0, #0x10; cmp r0, #9`), so the shape is right for this block. What is unsolved is what makes agbcc keep a zero-extended home it can prove is redundant; nothing on the source axis reached it.",
   "reason": "wave 32 (W32-A)"
  },
  "sub_08077214": {
   "bytes": 120,
   "best": "91.7%",
   "remaining_diff": "SIZE-EXACT, ten bytes, ORDER only. The ROM narrows the EIGHTH argument LAST of the three stack arguments (a6 `lsls`, a7 `lsls; lsrs` -> r7, then a8 `lsls; lsrs` -> r9); this candidate narrows a8 FIRST, because the `v = a8;` binding is the first statement. Every other instruction, the frame, both calls and the return are exact.",
   "why_it_is_close": "Only three instructions are in the wrong place; nothing is added or removed.",
   "axes_ruled_out": [
    "the eighth argument read straight off the stack at its use -- no narrowing at all, r9 never pushed, frame two registers smaller, 12 bytes wrong",
    "making the last three parameters `u16`: 8 bytes WORSE (128 against 120), because a narrow parameter's PROMOTE_MODE copy lives from the prologue to its last use and agbcc then needs a third high register (sl) and pushes it"
   ],
   "settled_by_this_attempt": "THREE corrections to wave 31's own declarations, all now in include/unknown-functions.h. (1) sub_08077214 RETURNS a value: wave 31 called it `void` from the call site, where sub_08077790 discards the result and gives no evidence; the body ends `adds r0, r7, #0` before an epilogue that pops into r1 and leaves r0, where a void function pops into r0 itself. (2) sub_08014740's FIRST TWO parameters are `s16`, not `u16` -- this is the first caller to hand them a variable rather than a literal and it sign-extends both with no conversion in front of the `bl`; the wave-29 note reasoned from that callee's prologue, but PROMOTE_MODE narrows `s16` and `u16` identically so a prologue never says unsigned. Both existing callers re-verified. (3) The eighth argument must be a bound `u16` local. What is left is only where that binding sits relative to the guard: it must be converted before the branch but after a6 and a7 are shifted, and statement order cannot reach that because the guard's operands are only evaluated inside the `if`.",
   "reason": "wave 32 (W32-A)"
  },
  "sub_08049C38": "97.9% -- 336 bytes, SIZE-EXACT, TWO instructions differ and both are inside `case 1`. ROM: ldrb r0,[r2] / cmp r0,#0 / beq / adds r1,r0,#0 / lsrs r1,r1,#1 ; candidate: ldrb r0,[r2] / lsls r0,r0,#24 / cmp r0,#0 / beq / lsrs r1,r0,#25. agbcc fuses the QImode zero-extension of the ldrb into both the compare and the shift. Wave 36 (W36-J), four attempts. RULED OUT: binding the value to a `u8` local in three placements (outside the `if`, inside the `if`, and with the final statement reading the member rather than the local) -- all three remove the redundant extend but destroy the address CSE (the ROM's `subs r2,#1` off the &unk27 pointer becomes a fresh `r4 + 0x26`) and all three score 96.4%, WORSE. The two effects are coupled. `int v` is worse still: a signed int `>>= 1` emits `asr` where the ROM has `lsr`. Everything else -- the 0..3 switch, the s16 widths, __divsi3, the *12, the tail call -- is byte-exact. See the W36-J section of docs/agbcc-codegen.md. Needs \"rodata\": [\"0x0812A16C\"] on promotion.",
  "sub_08047F70": "98.8% -- 488 bytes, SIZE-EXACT, SIX bytes: three instances of `mov r1, r8` emitted as `mov r3, r8`. r8 holds the -fforce-addr constant &gpKeySt, read six times; three of the six already use r1 and match, and the differing three are the 2nd, 4th and 6th -- a strict alternation, i.e. a least-recently-used pick inside reload with no source-level operand on it. Wave 36 (W36-J), four attempts. Every other byte, including all seven key arms, both branch polarities, the s16 local's extend pairs and the cross-jumped stores, is byte-exact. FIXED ON THE WAY (keep these): the 0x200 arm must write the `<= 5` arm FIRST (the arm reached by falling through is the one written first); the 0x80 arm's else needs `v = p->unk20 + 1` bound to its own local or fold reassociates it to `(unk1f - 1) - unk20`; and `limit` and `d` must be ONE s16 local, not two -- as two, the whole function's callee-saved assignment shifted by one and twelve further instructions differed. RULED OUT: local declaration order (byte-identical). NEXT STEP: a pure register-allocation residual with the instruction order already correct -- the one case the brief says to hand to decomp-permuter rather than a fifth hand spelling. Needs \"rodata\": [\"0x0812A13C\"] on promotion.",
  "sub_08009310": "PARKED wave 37 (W37-F) at 474 of 476 bytes. Every instruction present, in the ROM's order, with the ROM's operands. REMAINING DIFF: the switch `default:` arm lacks the ROM's `adds r6, r0, #0` after `bl sub_08008D70` (-2 bytes), and as a consequence `n` and the gUnknown_0808D814 pool-address pseudo swap r6/r7 and the two shared exit blocks come out in the opposite order (candidate `bgt -> return 1` with `return 0` falling through; ROM `ble -> return 0` with `return 1` falling through, which also swaps 0x1c8/0x1d4 in the second jump table) -- both zero-byte, both expected to follow once the copy is there. THE KNOT: the copy means the ROM binds the result to a pseudo global_alloc put in a callee-saved register, i.e. a variable referenced from more than one basic block. Using the function-scope `n` DOES produce the copy and then cross-jumping folds the arm's `cmp/ble/b` into one `b` to the shared tail (4 bytes short instead of 2); a block-local `int k` blocks the merge (different pseudos, rtx_equal_p fails) but is a local allocno, gets r0, and loses the copy. RULED OUT: tail written as `if (n <= 0) return 0; return 1;` (worse -- gcc then places the `return 1` block inline at 0x9c); `n = 0x16; break;` in the 2/4/6 arm (the store is jump-threaded away entirely). LOAD-BEARING, do not undo: `d = sub_08008D14(x,y); if (d & 6)` must be a bound local; the 2/4/6 membership test must be ONE `||` with a single `n = 0x16` store (the comma operators in the draft exist only because locals cannot be assigned inside a `||` operand and the exemplar's `rows`/`cells` bindings are required for the address arithmetic); the 0/8/9 arm has NO store at all, all three of its success paths are `return 1`. NEXT STEP: decomp-permuter from work/sub_08009310/best.c, not a fifth hand spelling. Needs \"rodata\": [\"0x0808D814\"] on promotion.",
  "sub_08009538": "PARKED wave 37 (W37-F). Control flow fully settled -- all four tile cases (0xFC/0xFD/0x11C/0x11D), every `||` neighbour pair and every guard match the ROM instruction for instruction. REMAINING DIFF is a single register-allocation inversion: the ROM has r5=x, r6=y, r7=ok, r8=the kept neighbour coordinate, sb=the gUnknown_0808D818 pool address; the candidate puts `ok` in r8 and the kept neighbour in r7, so every `ok = 1` costs `movs rN,#1; mov r8,rN` instead of `movs r7,#1` and every `if (ok == 0)` costs `mov r1,r8; cmp r1,#0` instead of `cmp r7,#0`. The two pseudos tie on allocno priority and the candidate loses the tie-break. RULED OUT (all four leave `ok` in r8): nx/ny at function scope; nx/ny declared per case; one function-scope kept variable plus a per-case scratch; `u8 ok` instead of `int ok`. CONFIRMED, do not re-derive: the tail is `r = 0; if (ok == 0) r = 1; return r;` and NOT `return ok == 0;` (do_store_flag would add an `adds r0, rN, #0` the ROM lacks); cases 0xFD and 0x11D share their last block at _080096DC purely by cross-jumping from symmetric source; the `if (ok == 0) { if (A) {...} if (ok == 0 && B) {...} }` nesting is forced because A's false edge goes to the next guard while the outer test's and B's both go to the exit. NEXT STEP: decomp-permuter -- it errored with 'exit_code' on this function and was not retried, and the instruction order is already correct, which is exactly the case the brief hands to the permuter. Needs \"rodata\": [\"0x0808D818\"] on promotion.",
  "sub_08009918": "PARKED wave 37 (W37-F) at 542 of 544 bytes; decomp-permuter reached 93.9% over 300 s from this start and matched nothing. This is sub_0800977C's twin (src/decomp/c_0800977C.c is the exemplar). SETTLED: `ok = sub_08009CF8(x, y) != 0;` gives the ROM's `rsbs; orrs; lsrs #0x1f`; `cnt` must be declared ONCE at the `terrain == 0xC` block scope (per-arm declarations give two pseudos and the shared `adds r6,#1; cmp r6,#1; ble` tail at _08009B06 is duplicated into the x-arm instead of cross-jumped); each row offset must be bound as its own statement (`t2 = (y-1)*2;`, `t3 = (y+1)*2;`) or CSE finds the common `rows + y*2` and emits `subs r0,r3,#2` / `ldrh r0,[r3,#2]`, which the ROM does not have; and `off = idx * 2;` must precede `tiles = p + 0xA22;`. REMAINING DIFF, y-branch only, -2 bytes net: the ROM keeps `rows` in r3 and spills the CELLS POINTER to r8 (`ldr r0,=0x1432; adds r0,r0,r1; mov r8,r0; add r8,r2; mov r0,r8; ldrb`) while holding `cnt` in r6; the candidate has `cnt` in r3, which leaves r6 free, so it keeps `rows` in r8 and builds the cells pointer in r0 two instructions cheaper -- the ROM is under one more unit of low-register pressure at that point. Visible as `adds r0, r1, r6` for the ROM's `adds r0, r0, r1` and `mov r4, r8; lsls r0, r4, #1` for `lsls r0, r3, #1`. NEXT STEP: the lever is whatever raises `cnt` above `rows` in allocno priority; hoisting `cnt` to function scope was NOT tried. Needs \"rodata\": [\"0x0808D820\"] on promotion.",
  "sub_08000CCC": "PARKED wave 37 (W37-E) at 97.6% -- SIZE EXACT (208 bytes), 5 bytes differ, and the residual is purely which register three instructions land in. ROM: movs r1,#192 / ands r1,r4 / movs r0,#63 / ands r0,r2 / orrs r0,r1 / strh r0,[r3,#36]. Candidate: movs r0,#192 / ands r4,r0 / movs r0,#63 / ands r2,r0 / orrs r2,r4 / strh r2,[r3,#36]. Same instructions, same order, same count: the ROM gives BOTH masks a fresh register and clobbers neither a1 (r4) nor the loaded gUnknown_0200B0D0 element (r2); agbcc coalesces each AND result into its dying operand. SETTLED and byte-exact everywhere else: two locals a and b (the ROM holds them in r1 and r2 simultaneously, so one variable is wrong); a = sub_08001D24(a1) - 4 written as ONE statement (the adds rD,rS,#0 / subs rD,#4 pair is not two statements -- see the K>7 note in docs/agbcc-codegen.md, and 4 is below that threshold only because the copy comes from the call result); and b = gUnknown_0200B0D0[b].unk04 as its OWN statement -- inlined into the store, the ldrh lands in the wrong register in BOTH arms. RULED OUT, each with the rest of the function byte-exact: operand order (b & 0x3F) | (a1 & 0xC0), which also breaks the ORDER by emitting the 0x3f mask first; binding c = a1 & 0xC0 to a local, which fixes the 0xC0 half (fresh register, r4 preserved) and leaves the 0x3f half coalescing; binding BOTH halves and spelling the or as d | c, same result; decomp-permuter, 21,468 iterations over 300 s from the 97.6% draft, nothing. Working hypothesis for the asymmetry: a1 is a PARAMETER pseudo (global allocno in callee-saved r4) and is therefore not a coalescing candidate, while the element is an ordinary local. Whatever makes the element non-coalescable is the one missing fact. Draft: work/sub_08000CCC/sub_08000CCC.c",
  "sub_08074AD0": "53.5% -- 144 bytes, candidate 140 (-4). TWO agents this wave (W38-B, W38-E), W38-E starting from scratch as the wave-37 inherited-draft rule requires. The residual is one source-level fact and all three symptoms follow from it: the original loads the gUnknown_081CC4D4 base ONCE into r5 and reuses it in both arms (`ldr r5,[r7,#0]` then `adds r0,r4,r5`), the candidate reloads it at the bottom; the original's pool word sits MID-FUNCTION at +0x54 with the code branching over it (`ldr r0,[r7,#0]; b.n 7e`) where the candidate dumps it after the epilogue -- that IS the -4 bytes; and allocation is shifted by one register throughout (r6 vs r5). Ruled out: binding the array base to a local before the loop, which is the wave-37 chapter that predicts exactly this pool movement -- it was tried and does not reproduce the mid-function dump. Not tried: the toolchain axis. WAVE 38.",
  "sub_0803E6C4": "90.6% -- 160 bytes, candidate 156 (-4), first difference at +0xa. TWO agents (W38-C, W38-F); the second was given the diagnosis below and did not move it. The original opens `adds r3,r2,#0` (copy param3 to a fresh register) then `adds r2,r1,#3` (param2+3 into a DIFFERENT register), keeping both incoming parameters live and putting +3 in a fresh local; the candidate does `adds r1,#3` in place. Everything downstream -- the pool-word register choice, the [pc,#104] vs [pc,#100] offsets, the -4 bytes -- follows from that one decision, so do not chase the pool separately. Wave 37 refuted the fixed 'reuse rather than a fresh name' rule (it inverts; decide by the exit test); the exit test here is `cmp <value>, r0; bge` against [[global]]->unk02. Both orderings were tried. Not tried: the toolchain axis. WAVE 38.",
  "sub_0801F2AC": "100.0% -- 160 bytes. THE SCORE IS NOT A ROUNDING ARTEFACT: every instruction is correct and only REGISTER FIELDS differ, which is precisely the case wave 37 restored to decomp-permuter after refuting 'the permuter gets nothing on allocation'. Two agents (W38-D, W38-G); W38-G was told explicitly to permute best.c rather than rewrite, and it still did not close. NOTE FOR THE NEXT WAVE: work/sub_0801F2AC/sub_0801F2AC.c is a LATER, WORSE attempt (164 bytes, 30.6%) -- the 100% candidate is in best.c beside it, and confusing the two wastes the lead. Ruled out: rewriting the C from the exemplar. WAVE 38.",
  "sub_08042864": "98.6% -- 140 bytes, first difference at +0x72. Carried a 98.57% draft into this wave; W38-K was instructed to delete the body and rewrite from the exemplar (the wave-37 rule: inherited drafts match at 9% against 83% for undrafted) and the from-scratch rewrite landed at 98.6%, essentially the same place. THAT IS THE FINDING: for this function the near-miss score reflects a genuinely hard residual rather than an exhausted search, which NARROWS the wave-37 rule rather than contradicting it -- a high score means 'the cheap paths are gone', and a rewrite recovers nothing when the cheap paths were never the problem. Ruled out: starting fresh from the exemplar. WAVE 38.",
  "sub_0801F19C": {
   "bytes": 80,
   "best": "76 bytes (-4), 51.2% identical (wave 39, W39-B; same figure reached in waves 34 and 36)",
   "remaining_diff": "EXACTLY TWO INSTRUCTIONS, both after `bl sub_0801F444`. The ROM parks that call's result (`adds r3, r0, #0`) and restores it (`adds r0, r3, #0`) around the argument setup, because it loads the 0x3FF mask into r0 -- the very register the result arrives in. The candidate loads the mask into r1 and leaves the result untouched in r0, so it never pays the pair. That is the whole -4. NOTE trymatch reports the first difference at +0x12 (`ldr r2,[pc,#52]` against `[pc,#48]`); that is the literal pool sliding by the 4 bytes lost later, NOT an independent error -- do not start there.",
   "why_it_is_close": "Everything from the prologue through `bl sub_0801F444` is byte-identical, including the r6/r8/r5/r4 assignment of a1/a2/a3/n, the gUnknown_0848B780 product and both call sequences. The statement structure is confirmed by the promoted caller src/decomp/c_0801F1EC.c, which fixes this function's arity and widths at its call site.",
   "axes_ruled_out": [
    "THE `lsls #5; lsrs #2` -> `lsls #3` FOLD IS NOT WHAT COSTS THE BYTES HERE, and three waves of drafts plus the docs/agbcc-codegen.md section \"`nonzero_bits` range analysis is ONE residual wearing three faces\" all say it is. Counted instruction by instruction: the ROM's `ands rX,rC; lsls rD,rX,#5; lsrs rD,rD,#2` is 3 insns and the folded `ands rX,rC; lsls rX,rX,#3; adds rD,rX,#0` is also 3 -- the saved shift is spent on a copy, because the folded form computes the count into a callee-saved register and must move it to the argument register while the ROM's two-shift form writes the first shift straight into it. Defeating nonzero_bits would therefore not close either function. Doc corrected in wave 39.",
    "Binding sub_0801F444's result to its own local before the CpuFastSet statement (`src = sub_0801F444(a, i); CpuFastSet(src, ...)`) -- BYTE-IDENTICAL to the nested form. gcc coalesces the copy, so this does not create the pseudo that would let the mask constant win r0. This was the obvious reading of the ROM's `adds r3,r0,#0` and it is wrong.",
    "Integer-space address arithmetic on the CpuFastSet destination (`(void *)((int)base + (v & 0x3FF) * 32)`) -- BYTE-IDENTICAL. This is the wave-39 lever that closed sub_0801FD9C, and it does nothing here: it only flips a commutative add whose operands are both non-constant registers, and these destination adds already have the ROM's operand order.",
    "old_agbcc with -fprologue-bugfix removed -- still -4 and WORSE at 42.5%, and it reverses the two gUnknown_0848B780 byte loads (+1 before +0) where the ROM and default agbcc both do +0 then +1. The toolchain is not the axis; probed by monkeypatching agbenv._OVERRIDES, leaving data/compiler-overrides.json untouched.",
    "Every spelling of the count arithmetic -- retracted twice already (waves 33 and 36) and now shown to be byte-neutral anyway. Do not re-measure it.",
    "Wave 38 (W38-D) separately refuted the `reg_n_sets` / second-assignment route to defeating nonzero_bits."
   ],
   "settled_by_this_attempt": [
    "`adds r1, r0, #0` after `bl sub_0801F3D4` is NOT a dead copy, which the wave-39 assignment brief asserted. It is sub_0801F444's SECOND ARGUMENT: nothing writes r1 between that copy and `bl sub_0801F444`, so the value is live across all the intervening gUnknown_0848B780 address arithmetic. include/unknown-functions.h already records the call as `sub_0801F444(a1, sub_0801F3D4(a1))`.",
    "The -4 is ONE fact, not two. Earlier drafts listed the shift fold and the result parking as separate 2-byte residuals; the fold is byte-neutral and the parking pair is the entire difference.",
    "`u32 n` -- the count shifts with `lsrs`, not `asrs`.",
    "Statement order is forced and correct: sub_0801F3D4 first, then the product, then the CpuFastSet whose first argument holds the sub_0801F444 call. gcc evaluates the call-bearing argument first, which is why the product must be its own statement ahead of it."
   ],
   "reason": "Register allocation, not source semantics. The question is why the 0x3FF constant's quantity beats the call result's to r0 in local-alloc: in the candidate the result never becomes a pseudo at all -- it flows from one hard r0 to the next via a copy suggestion -- so r0 is simply unavailable when the constant is allocated. Park with sub_0801F234, which carries the identical residual plus one more. Draft at work/sub_0801F19C/sub_0801F19C.c."
  },
  "sub_0801F234": {
   "bytes": 120,
   "best": "124 bytes (+4), 31.7% identical (wave 39, W39-B; same figure reached in waves 33 and 36)",
   "remaining_diff": "TWO INDEPENDENT REGISTER-ALLOCATION FACTS, which is why this scores worse than its sibling despite the same shape. (1) `n` LANDS IN r8 INSTEAD OF r7. Four values are live across both calls (a, e, tile, n); the ROM puts them in r5, r4, r7, r6 and pushes {r4,r5,r6,r7,lr}, while the candidate takes r4, r5, r6 and then reaches PAST r7 for r8, paying `mov r6,r8; push {r6}` in the prologue, `mov r8,r3` after the multiply, `mov r2,r8` at the use and `pop {r3}; mov r8,r3` in the epilogue. It also forces the extra copy after `muls`: THUMB `muls` cannot target a hi register, so the product takes a lo scratch and is moved out, where the ROM multiplies straight into r6. (2) THE SAME MISSING RESULT-PARKING PAIR as sub_0801F19C (`adds r3,r0,#0` / `adds r0,r3,#0`), which cancels 4 of the bytes fact (1) adds.",
   "why_it_is_close": "Every statement is present and in the ROM's order, and the whole tail -- the unk05 cursor bump and the three separate re-reads of it -- reproduces. Its sibling sub_0801F19C is byte-exact up to the second call from the same derivation, and the promoted src/decomp/c_0801F1EC.c supplies the type model.",
   "axes_ruled_out": [
    "THE `lsls #5; lsrs #2` -> `lsls #3` FOLD IS NOT WHAT COSTS THE BYTES HERE, and three waves of drafts plus the docs/agbcc-codegen.md section \"`nonzero_bits` range analysis is ONE residual wearing three faces\" all say it is. Counted instruction by instruction: the ROM's `ands rX,rC; lsls rD,rX,#5; lsrs rD,rD,#2` is 3 insns and the folded `ands rX,rC; lsls rX,rX,#3; adds rD,rX,#0` is also 3 -- the saved shift is spent on a copy, because the folded form computes the count into a callee-saved register and must move it to the argument register while the ROM's two-shift form writes the first shift straight into it. Defeating nonzero_bits would therefore not close either function. Doc corrected in wave 39.",
    "Binding sub_0801F444's result to its own local before the CpuFastSet statement (`src = sub_0801F444(a, i); CpuFastSet(src, ...)`) -- BYTE-IDENTICAL to the nested form. gcc coalesces the copy, so this does not create the pseudo that would let the mask constant win r0. This was the obvious reading of the ROM's `adds r3,r0,#0` and it is wrong.",
    "Integer-space address arithmetic on the CpuFastSet destination (`(void *)((int)base + (v & 0x3FF) * 32)`) -- BYTE-IDENTICAL. This is the wave-39 lever that closed sub_0801FD9C, and it does nothing here: it only flips a commutative add whose operands are both non-constant registers, and these destination adds already have the ROM's operand order.",
    "old_agbcc with -fprologue-bugfix removed -- READ THIS BEFORE CHASING IT. It reports `size: match (120 bytes)`, which looks like a breakthrough and is not: it scores WORSE (28.3% against 31.7%), it still pays the r8 round trip, and it is size-exact only because it CSEs away the third `ldrb r0,[r4,#5]` re-read that the ROM actually performs after the cursor bump. Default agbcc reproduces that re-read and is the correct compiler for this function. The toolchain is not the axis.",
    "Every spelling of the count arithmetic -- retracted twice (waves 33 and 36) and byte-neutral regardless."
   ],
   "settled_by_this_attempt": [
    "sub_0801F3D4 is called ONCE and its result bound: r1 stays live across all the address arithmetic and is sub_0801F444's second argument. Arguments are evaluated LEFT to right.",
    "`tile` and `n` are computed as statements BEFORE the CpuFastSet call because both are live across it AND across sub_0801F444; that placement is what the ROM's instruction order requires, not a stylistic choice.",
    "`tile` must be a local, not a re-read: the ROM loads `e->unk08[e->unk05].unk00` once into r7, and a second source read would land on a different entry because `e->unk05` is incremented in between.",
    "The ROM genuinely re-reads `e->unk05` from memory three times in the tail. Default agbcc reproduces this; old_agbcc does not, which is an independent confirmation of the compiler choice.",
    "The frame is NOT one live value too many, which was the wave-39 orchestrator's hypothesis for the +4. Both the ROM and the candidate keep exactly four values live across the calls; they differ only in WHICH registers, r7 against r8."
   ],
   "reason": "Register allocation, not source semantics, and it shares residual (2) with sub_0801F19C. Park and unpark the two together. Residual (1) looks like a REG_ALLOC_ORDER/cost effect rather than anything source-reachable: `n`'s uses are all mov/add-copyable so its preferred class is GENERAL_REGS, and gcc pays copies to keep it in a hi register rather than restricting it to LO_REGS. Draft at work/sub_0801F234/sub_0801F234.c."
  },
  "sub_080242B0": {
   "bytes": 128,
   "best": "97.7% -- 128 of 128, SIZE EXACT, 3 bytes differ, first at +0x5a",
   "remaining_diff": "ONE branch pair: the case-14 leaf of the switch decision tree. original `cmp r2,#14 / bne _default / b _true`; candidate `cmp r2,#14 / beq _true / b _default`. Same two instructions, opposite polarity. Everything else is byte-for-byte -- prologue, all three map planes, both masks, the +0x12 unit test, the gUnknown_03004084 test, the root range test, the case-6 leaf, the sub_08043050 bit test and both return blocks. NOT A RELOCATION: try_match prints no reloc line at all for this function, so it is not the 'different symbols resolve to the same address' false mismatch -- that hypothesis was raised and checked in wave 39 and is dead. The two differing branch instructions plus one byte of the following `b` are the whole 3 bytes.",
   "why_it_is_close": "The tree is gcc's balanced {6, [10,11], 14} and the candidate reproduces it exactly down to the register (r2) and the root's `cmp #0xb / bgt` + `cmp #0xa / bge` range test. Note the tree contains BOTH emission forms: the left leaf {6} comes out `beq label / b default` in the ROM and the candidate agrees there; only the right leaf {14} differs. So the disagreement is specifically about how emit_case_nodes treats a single-valued RIGHT child, not about the tree shape, the case list or the index register.",
   "axes_ruled_out": [
    "`case 10: case 11: case 14:` sharing one body -- this draft (wave 35, W35-C).",
    "`case 10: case 11:` plus a SEPARATE `case 14:` with its own `return TRUE;` -- cross-jumping merges the bodies again and the branch shape is identical (wave 35, W35-C; re-probed wave 39, W39-C, same).",
    "An explicit `default: break;` arm inside the switch -- emits the IDENTICAL tree down to the register. W35-C listed this as the promising untried lead; it is dead (wave 39, W39-C).",
    "Padding the node list so the right child gains a child of its own. This is IMPOSSIBLE here and the reason is structural, not a question of which terrain codes are real: any fourth node in 0..14 makes the value range dense enough that agbcc abandons the decision tree for a JUMP TABLE. Probed twice -- `case 0: break;` below the lowest gives a 15-entry table after `cmp #0xe; bhi`, and `case 12: case 13: break;` (semantically free, their body is the default's) gives a 9-entry table after `subs #6; cmp #8; bhi`. Either way the tree the residual lives in stops existing (wave 39, W39-C).",
    "Reordering the shared labels as `case 14: case 10: case 11:` -- bit-identical, so case source order is irrelevant (wave 39, W39-C).",
    "An `s16` switch index instead of `int` -- leaf polarity unchanged, and it perturbs the register allocation of the already-matching part (r1 instead of r2), so it is strictly worse (wave 39, W39-C)."
   ],
   "settled_by_this_attempt": [
    "The map planes MUST be members of a local struct cast onto gUnknown_08499590 (the wave-34 W34-F rule), not pointer arithmetic; this gets all three planes (+0x12 unit, +0x1432 terrain, +0x417A rowOffset) right at once.",
    "THE TWO MASKS MUST BE LOCALS. Inline at their uses agbcc sinks `cell & 0xe0` and `cell & 0x1f` below the unit test: 73.4% inline against 97.7% bound. Binding also fixes the `cmp` operand order for free -- the ROM has `cmp <mask>, <global>` here, the OPPOSITE of sub_080253B0's `cmp <global>, <mask>` on the same two values, so operand order is a real per-site lever and not a house style.",
    "The case list is NOT reachable as an axis: neither adding, removing, reordering nor defaulting a case moves this branch, and adding one destroys the tree entirely."
   ],
   "reason": "Branch emission inside gcc's emit_case_nodes, not source semantics. Every source-level handle on a switch has now been tried. The remaining untested axis is the one that decides between the two leaf emissions -- whether the node is considered bounded above/below (node_has_high_bound / node_has_low_bound), which consults the index TYPE's maximum and the ancestor chain's values. No ordinary C type has a maximum of 14, so if this is reachable at all it is not through the index type either; the next real lead is the toolchain axis (data/compiler-overrides.json), which has never been tried on this function. Draft at work/sub_080242B0/sub_080242B0.c."
  },
  "sub_08024720": {
   "bytes": 132,
   "best": "42.4% by byte identity -- but that score is POSITIONAL and badly understates it: 128 bytes against 132, and ONE missing instruction",
   "remaining_diff": "(1) The only real difference: original `adds r0,r5,#0 / cmp r0,#0 / blt / ldr r1,=sym / lsls r0,r0,#1`, candidate `cmp r5,#0 / blt / ldr r1,=sym / lsls r0,r5,#1`. The ROM materialises idx into a scratch and BOTH the compare and the shift read that copy; the candidate reads r5 directly twice. That is -2 bytes, and the other -2 is the `.short 0x0000` literal-pool alignment pad, which only exists because the ROM's code length before the pool is 2 mod 4. (2) Zero-byte ordering: in the else block the candidate emits `ldr r0,=gUnknown_0809139C` BEFORE the second argument's computation, the ROM after it (the ordinary argument grouping -- parameter copies, then pool `ldr`s, then `mov #imm8`). THE PUSH LIST ALREADY MATCHES. `push {r4, r5, r6, lr}` is a context line in the diff, not a difference. The wave-39 orchestrator diagnosed this as 'the original keeps one more callee-saved register live than your C does' and that is refuted by the diff itself -- the same wrong 'one live value too many' hypothesis it raised for sub_0801F234 in the same wave. The frame is correct; do not go looking for an extra live value.",
   "why_it_is_close": "Every other instruction matches: the idx computation, the u16 loop counter and its `bls` exit, the 0x3c-byte record stride, the unk1b test, the sub_0804415C truth test, both argument setups and both calls, and the epilogue. The residual is one register copy.",
   "axes_ruled_out": [
    "`&gUnknown_0809139C[idx]` vs `gUnknown_0809139C + idx` -- identical code.",
    "`continue` on the unk1b test instead of a nested if -- identical.",
    "nested `if (sub_0804415C(i))` with the else body written out twice -- identical, cross-jumping merges it back.",
    "`?:` on the pointer argument -- blocks the LICM hoist but merges the two calls into one, and the ROM has two.",
    "binding the array base to a local before the loop -- strictly worse, hoists base AND shift and costs a third register.",
    "`(j = idx) >= 0` inside the `&&`, placing the copy exactly where the ROM has it -- gcc coalesces j into idx and the copy vanishes; bit-identical to no copy at all.",
    "a nested `if (sub_0804415C(i) != 0) { j = idx; if (j >= 0) ... }` -- cross-jumping merges the two calls' argument setup into one shared tail, which the ROM does not have.",
    "passing the bare symbol in the else (fixes ordering (2) but restores the +8 LICM hoist), and assigning p AFTER the call in the else (keeps the dead store AND still hoists: 21.2%)."
   ],
   "settled_by_this_attempt": [
    "THE FRAME IS SOLVED, and the lever is a new codegen rule -- see the 'LICM hoists a conditionally-executed loop invariant' chapter added to docs/agbcc-codegen.md. The natural spelling costs EIGHT BYTES because LICM hoists `idx * 2` and the array base into the preheader, needing two more callee-saved registers. Being guarded by two conditionals AND preceded by a call does not protect an invariant -- `maybe_never` and `call_passed` are both set and agbcc hoists anyway. What blocks it is assigning the SAME pointer local in BOTH arms of the if/else: two sets of one pseudo inside the loop means scan_loop never records the address computation as a movable at all.",
    "`idx = ((u32)gUnknown_03004008 >> 2) & 0xf` -- the shift is LOGICAL; gUnknown_03004008 is s32 so a plain `>> 2` emits `asrs`.",
    "`for (i = 1; i <= 4; i++)` with a u16 counter, and `gUnknown_08499598[i].unk1b` reached by the honest subscript, which reproduces the ROM's three-level chain through the -fforce-addr word 0x08090A30 with no help.",
    "Second argument `(i + 0xb) * 32 + 0x1e`, truncated to u16 at the call by sub_0801368C's declared `u16` parameter."
   ],
   "reason": "One register copy. docs/agbcc-codegen.md already carries the pattern: 'An unexplained `adds rD, rS, #0` that survives to a LATER use is a SECOND LOCAL -- and it must be a copy of the first, not of the expression' (wave 27, W27-A). The obvious spellings of that second local are ruled out above because gcc coalesces the copy away once the value is provably the same pseudo. What has NOT been tried is making the second local survive coalescing -- a different TYPE (so the copy is a conversion rather than a copy), or a use of idx after the loop that keeps both live. Draft at work/sub_08024720/sub_08024720.c."
  },
  "sub_08012B70": {
   "bytes": 88,
   "best": "87.5% -- 11 of 88 bytes differ, size EXACT (wave 39, W39-A)",
   "remaining_diff": "The 43-instruction stream is 1:1 with the ROM: same opcodes, same order, same immediates, same branch structure, same `mov ip, r0`. EVERY differing byte is a register NUMBER, in two linked pairs. (1) dst and src are swapped -- draft r5/r4, ROM r4/r5. (2) the row base and the row pointer are swapped -- the draft keeps the base in callee-saved r5 (it is the SAME pseudo as dst, which is reassigned) and puts p in scratch r2, where the ROM puts the base in scratch r2 and recycles dst's now-dead r4 for p. w->r6, h->r7, i->r1, j->r3 and add->ip already match exactly, as does everything from `lsrs r7, r0, #8` onward apart from the register numbers.",
   "why_it_is_close": "The type model and the statement order are settled and independently corroborated rather than guessed: the split PROMOTE_MODE pair fixes the parameter widths (and the retype was confirmed by re-verifying all three callers byte-for-byte), and the position of the src prologue copy fixes parameter 2 as a directly-walked pointer. No statement-order or type error remains to find.",
   "axes_ruled_out": [
    "old_agbcc with -fprologue-bugfix removed -- MEASURED, not assumed, via a throwaway data/compiler-overrides.json entry: 85.2%, WORSE than the default toolchain's 87.5%. It keeps the same wrong register assignment AND additionally emits `ldrb` before `ldrh`. Consistent with the wave-38 discriminator, which locates the two allocators' divergence at a CALL inside a loop body -- this function has no calls at all.",
    "A separate local for the row base (`d = dst + x; d = d + y * 0x20;`). This DOES produce the ROM's fresh-pseudo base and would free dst's register for p, but it always costs the `adds r4, r0, #0` prologue copy: with dst read-only, gcc loads the stack parameter into r1 rather than r0, dst never leaves r0, and the function comes out 42 instructions / 4 bytes SHORT. Probed in five spellings -- own local in one statement and in two, base assigned back into dst, the mixed `dst = dst + x; d = dst + y * 0x20;`, and declaration order both d-before-p and p-before-d -- all 42 instructions. THIS IS THE CORE TENSION: the prologue copy needs dst long-lived, the base register needs dst dead.",
    "A `void *` second parameter with a `u16 *s = src;` local -- puts the src prologue copy seven instructions later, after the stack argument's narrowing, instead of at instruction 3. Not reachable by statement reordering, because parameter setup always precedes a body statement.",
    "`int` parameters 3-5 with explicit `(u16)` casts at the uses -- loses both prologue `lsls`, because the narrowing is then emitted whole at the use.",
    "decomp-permuter, FOUR runs totalling roughly 43,000 iterations: 60.2 -> 72.7 -> 86.4 -> 87.5%, and then a full 600 s run starting from 87.5% produced nothing better at all. It has CONVERGED; do not simply re-run it longer. Its two surviving edits are already in the draft and are worth keeping -- a zero-trip `do { } while (0)` around the second offset add plus `src++`, and `y * 0x20` bound to an `int` local -- together worth 14.8 points.",
    "`*p = add + *src` operand order, and the `p++` / `src++` order inside the inner loop -- byte-neutral."
   ],
   "settled_by_this_attempt": [
    "Parameters 3, 4 and 5 are u16, NOT the `int` they were declared with. A prologue `lsls rN, rN, #0x10` whose matching `lsrs` is MISSING -- it reappears at the use fused with the scale, `lsrs #0xf` for `dst + x` (*2) and `lsrs #0xa` for `y * 0x20` (*0x40) -- is a sub-word PARAMETER, whereas an `int` with an explicit `(u16)` cast emits the narrowing whole at the use and leaves no prologue `lsls`. Retyped in include/unknown-functions.h; sub_080399F8, sub_0807FE90 and sub_08080498 were all re-run through try_match and are still byte-identical.",
    "Parameter 2 is a directly-walked `u16 *`, not a `void *`: `adds r5, r1, #0` is a PROLOGUE copy sitting ahead of the `ldr r0, [sp, #0x14]`, which only happens when the parameter itself is the walking pointer.",
    "gUnknown_080A31A4 is `u16 []`, not `u8 []` -- sub_08012B70 is its only reader and walks it as halfwords. Byte-neutral at all three call sites, which pass only the base address.",
    "w and h come from ONE halfword, not two byte loads: `hdr = *src` (ldrh), `w = (u8)hdr` (agbcc re-loads it as `ldrb` off the same address), `h = hdr >> 8`. The ldrh-BEFORE-ldrb order is what distinguishes this from `w = *(u8 *)src; h = *src >> 8;`.",
    "The destination offset is `dst + x` plus `y * 0x20` in u16 units (a 0x40-byte row stride), and the row pointer is `p = dst + i * 0x20` -- a MULTIPLY per outer iteration, not an accumulate: the ROM has `lsls r0, r1, #6; adds r4, r2, r0` every time round.",
    "Both loop counters are u16 tested unsigned; the outer counter's increment being hoisted above the inner loop is the compiler's doing, not the source's."
   ],
   "reason": "Register allocation, not source semantics. The instruction stream, the type model and every read form are settled; what remains is which hard register the row base and the row pointer land in, and that is coupled to whether dst survives as a pseudo of its own. Both sides of that coupling have been probed to exhaustion from source, the toolchain axis is measured and worse, and the permuter has converged. Unparking needs a construct that keeps dst alive across the stack-parameter load WITHOUT making it the row base."
  },
  "sub_080253B0": "97.6% -- 252 bytes, size-exact, 6 bytes / THREE instructions differ, all inside `gUnknown_03004084 == (cell & 0xe0)` and nowhere else (first difference +0x4a). original: `ldr r0,=gUnknown_03004084 / movs r1,#0xe0 / ldrh r0,[r0] / ands r3,r1 / cmp r0,r3`; best candidate: `ldr r0,=gUnknown_03004084 / ldrh r1,[r0] / movs r0,#0xe0 / ands r3,r0 / cmp r1,r3`. The ROM interleaves two independent chains -- A = (address load, halfword load), B = (constant, and) -- as A1 B1 A2 B2, filling the load-use stall between `ldr` and `ldrh` with the constant. It can only do that because the constant is allocated to r1: with the constant in r0, the address's own register, hoisting it above `ldrh r0,[r0]` would clobber the address, so the scheduler cannot fill the stall. THE REGISTER CHOICE IS THE CAUSE, THE ORDERING IS THE SYMPTOM -- chasing the order directly is what every failed attempt did. NOTE for anyone re-reading the diff: `mov r8, r2` and the whole gUnknown_08090A48 record load ahead of it are BYTE-EXACT and appear only as unchanged context; r8 is not implicated. RULED OUT: (1, w34) terrain bound to its own local -> 90.5%; (2, w34) `mask == global` operand order -> 96.4%; (3, w34) `global == mask`, this draft -> 97.6%; (4, w34) `cell & 0xe0` bound to a local -> 96.8%, which fixes the cmp operand order AND the in-place ldrh but hoists the whole B chain above the whole A chain; (5, w34) decomp-permuter 300 s / 4 threads, best score 215, never interleaved the chains (wave-17's exception: useless on a pure allocation residual with instruction order already correct); (6, w39) `cell &= 0xe0;` as its own statement, W34's nominated next step -- NOT worth an attempt and not spent, because a statement boundary emits the B chain complete before the `if`, which is exactly the movs/ands/ldr/ldrh shape already measured at 96.8% in (4); (7, w39) `cell & ~0x1f` as the mask, probed: does NOT fold to 0xe0, agbcc materialises 0xffffffe0 as `movs #0x20; negs` and the function gets one instruction LONGER. No source-level statement ordering can separate the `ldr` from its `ldrh`, so the interleave is reachable only through allocation. AT PROMOTION this also needs \"rodata\": [\"0x08090A48\"] placed; that reloc line in the diff is the wave-18 honest-spelling flow and is NOT part of the 6-byte residual. Draft at work/sub_080253B0/.",
  "sub_08025BE0": "83.1% -- 124 bytes, size-exact, and the entire residual is ONE MISSING INSTRUCTION (first difference +0x52). original: `strb r5,[r3,#7] / strb r5,[r3,#8] / movs r0,#0 / strb r0,[r3,#9] / strb r5,[r3,#10]`; candidate: the same without the `movs r0,#0`, storing r5 to +9 as well. The 21 differing bytes are that one 2-byte instruction plus the 2-byte shift it imposes on everything after it -- the trailing instructions are identical, just relocated, and the `<_08025C50>` vs `<sub_08025BE0+0x70>` branch targets in the diff are symbolisation, not byte differences. NOTE for anyone re-reading the diff: the u16 bitfield surgery at +4 (`ldrh / ldr =0xFFFFF87F / ands / orrs / strh`) and the whole gUnknown_085D5ABC table section are BYTE-EXACT and appear only as unchanged context; they are produced by ordinary C bitfield member assignment (`a1->unk04_7 = ...`), which is already the right convention and must not be respelled as explicit mask/or. The ROM materialises a SECOND zero register for unk09 alone, while r5 (`movs r5,#0` in the prologue) serves unk01, unk07, unk08, unk0a and the switch's `unk0b = 0`. r5 is live across unk09 -- used again for unk0a on the very next instruction -- so this is not the allocator extending a live range; two zero pseudos in the RTL means two distinct source expressions. The `switch` in the draft is load-bearing and worth 19 of the 21 bytes: both `if` spellings of the 1..2 range fold to the unsigned `(a2-1) <= 1` trick because a2 is u8, and expand_case does not apply that fold, so a two-label case range is the only spelling that survives. RULED OUT for the unk09 zero: (1, w34) assigning the group in source order and reordering it; (2, w39) chained assignment `a1->unk09 = a1->unk08 = 0;` -- the inner assignment's QImode value temp is CSEd straight back to r5, full try_match, still 83.1%; (3, w39) THE BITFIELD GUESS W34 LEFT AS 'the obvious next step' IS WRONG -- controlled probe, one body and two struct declarations differing only in `u8 unk09;` versus `u8 unk09:8;` (the latter declared locally in the probe), both emit `strb r5,[r3,#9]`, because agbcc's bitfield store path degenerates to the plain store for a byte-wide byte-aligned field. There is NO evidence for reshaping struct Unk08499594.unk09 and it was left alone (recorded beside the member in include/unknown-globals.h); (4, w39) `a1->unk09 = a1->unk01;` reading the sibling zeroed earlier -- CSE keeps mem[r3+1] == r5 and folds it, probed; (5, w39) unk09 and unk0b sharing one int local that changes value 0 -> 1, probed, also folds to r5. The two constructs that most plausibly create a second constant pseudo have now both been measured and neither does; the next idea should question whether unk09's initialiser is a literal 0 at all. Draft at work/sub_08025BE0/.",
  "sub_080353E8": {
   "bytes": 168,
   "best": "97.6% -- 168 of 168, SIZE EXACT, 4 bytes / TWO ADJACENT INSTRUCTIONS differ (first difference +0x3e)",
   "remaining_diff": "The second loop's bound, and nothing else. original: `lsls r0,r0,#4 / adds r0,#0x10` (n * 16 + 16); candidate: `adds r0,#1 / lsls r0,r0,#4` ((n + 1) * 16). Every other instruction, every register assignment and all four pool words are the ROM's, including the `bge` guard, the `adds r7,r0,#0` bound copy and the `blt` at the loop bottom.",
   "why_it_is_close": "The honest spelling `j < n * 16 + 16` SHARES the subexpression `n * 16` with the loop's own init `j = n * 16`, and agbcc then hoists that multiply alone to the TOP of the preheader, leaving the `+0x10` behind at the comparison: `lsl r1,r0,#4 / lsl r5,r0,#0x14 / asr r0,r5,#0x10 / add r1,r1,#0x10`. That is the same four instructions the ROM has, in a different order with r0/r1 swapped -- a strictly WORSE diff than the `(n + 1) * 16` form kept in the draft, which shares nothing with the init, is hoisted as a UNIT and lands in exactly the ROM's slot. So the shape, the slot and the hoist are all already right; only the bound's two-operation arithmetic is not.",
   "axes_ruled_out": [
    "bound as `n * 16 + 16`, as `16 + n * 16`, and with `n` declared `s16` instead of `int` -- all three give the split hoist described above, byte-for-byte the same wrong output",
    "init as `n << 4` with the bound left `n * 16 + 16` -- LSHIFT_EXPR and MULT_EXPR are the same `ashift` at RTL, so they still share and still split",
    "`n = (gUnknown_03004008 & 1) * 16;` with `for (j = n; j < n + 16; j++)` -- keeps n live through the loop and RECOMPUTES the bound at the loop bottom, where the ROM hoists it into r7",
    "the global inlined twice instead of a local `n` -- the `bl` in the body kills it, so gUnknown_03004008 is RE-READ inside the loop; much worse",
    "`n * 16 + 16 > j` with the operands swapped -- keeps the bound's two instructions adjacent but computes it BEFORE the init and inverts the branches to `ble`/`bgt`",
    "the second loop reusing `i` rather than a fresh `j`; a `while` loop instead of a `for`; the comma form `for (n = gUnknown_03004008 & 1, j = n * 16; ...)` -- all three bit-identical to the plain `n * 16 + 16` spelling, so neither variable reuse nor the comma lever reaches this",
    "300 s of decomp-permuter from the 97.6% draft, ~19,800 iterations across 4 threads -- nothing scored better than the starting point"
   ],
   "settled_by_this_attempt": [
    "gUnknown_02027DE8 is an array of 12-byte records, declared as struct Unk02027DE8 in include/unknown-globals.h. The ROM's `lsls #1; adds; lsls #2` is i * 12, an ordinary array index and NOT a shift idiom",
    "All five members it touches are u16: the accumulate loop reads both operands with `ldrh` (`unk00 += unk04`, `unk02 += unk06`) and the position reads use `lsrs #8`, never `asrs`",
    "`& 0xff` is in the SOURCE, not a parameter narrowing: 0xff is materialised once into r6 and LICM hoists it out of the loop, and sub_0801BDB4's parameters are `s32` per the promoted src/decomp/c_0801BD00.c, so no narrowing is owed at the call",
    "The camera origin is `*(s16 *)(gUnknown_08499590 + 4)` -- the register-offset `ldrsh` an s16 forces is what the ROM has. The subtraction is SImode here, unlike src/decomp/c_08035B80.c where the SAME two fields load `ldrh` because the result narrows to an s16 parameter. One member declaration serves both",
    "sub_0801BDB4, sub_08042F5C and sub_08042FA4 were the PROMOTED-BUT-NEVER-DECLARED trap -- all three already matched, now declared in include/unknown-functions.h from their definitions",
    "`mov r7, r8` in the prologue is five-plus live values, not a loop signal"
   ],
   "reason": "One expression shape, not register allocation and not semantics. What is wanted is a spelling of `n * 16 + 16` whose multiply is NOT a common subexpression with the loop init; every spelling tried either shares it (and splits the hoist) or changes the arithmetic. Unpark when such a spelling is found. Draft at work/sub_080353E8/."
  },
  "sub_08061A40": {
   "bytes": 132,
   "best": "44 bytes (-88), 0.0% identical at +0x0 -- the candidate is a WORD/ldm copy where the ROM is a BYTE copy, so no instruction lines up despite the statement structure being right.",
   "remaining_diff": "GRANULARITY ONLY. The statement structure is settled and reproduces the ROM's layout exactly: prologue-free entry (`bx lr`, no push), the same `adds r2,r0,#0 / adds r2,#0x10 / adds r1,#0x10` preheader, the same `movs r3,#0x17` down-counter and the same `subs r3,#1; cmp r3,#0; bge` bottom. The ROM moves BYTES (`ldrb`/`strb` pairs -- 16 unrolled at offsets 0x00..0x0f, then 12 per trip for 24 trips); every agbcc configuration tested moves WORDS. 0x10 + 24 * 0xc == 0x130, which independently agrees with the record size sub_08061788 proves via its `* 0x130` stride, so the TWO-STATEMENT reading (a 0x10-byte copy, then a 24-trip loop of 0xc bytes) is solid.",
   "ruled_out": [
    "STRUCT ASSIGNMENT (`*dst = *src`) -- this was the wave-44 brief's assertion and it is WRONG. Refuted by controlled probe, W44-H. agbcc has exactly two block-move paths and neither can emit an unrolled byte copy.",
    "thumb `movstrqi`: gated on `align == 4 && size <= 48`, moves 12 bytes at a time via ldmia/stmia triples. Measured: 48 bytes -> four ldm/stm pairs; 52 bytes -> `bl memcpy`; 0x130 -> `bl memcpy`.",
    "`move_by_pieces` (the only path that emits byte moves) is switched off entirely by MOVE_RATIO: `memcpy(u8 *, const u8 *, N)` is a libcall for N = 2, 3, 4, 8 and 16 alike. There is no N at which it unrolls.",
    "No struct type can present alignment 1 to either path -- structs here are always at least 4-aligned (`struct { u8 v[3]; }` copies as a single WORD; a member declared after `u8 pad[1]` lands at offset 4).",
    "PER-FILE COMPILER AXIS (data/compiler-overrides.json) -- checked and NEGATIVE, do not re-run. Four configurations via the `agbenv.flags` monkeypatch: default, `-mstructure-size-boundary=8`, `old_agbcc`, and `old_agbcc` with `-mstructure-size-boundary=8`. ALL FOUR emit byte-for-byte the same 44-byte ldm/stm version."
   ],
   "next": "Do NOT re-attempt from the source axis without a new mechanism -- four axes are closed above. The open question is what produces an UNROLLED BYTE copy at all: either a compiler/flag combination not in compiler-overrides.json, or the source wrote the byte moves explicitly in a form that survives to -O2. See the 'Block moves' chapter in docs/agbcc-codegen.md (wave 44, W44-H) for the full measurement before spending anything here.",
   "wave": 44,
   "agent": "W44-H"
  },
  "sub_0808AE54": {
   "bytes": 124,
   "best": "124 bytes (size-exact), 26.6% identical at +0x4",
   "remaining_diff": "ONE CAUSE, THREE FUNCTIONS -- do not work these separately. `mov r3, sp; adds r3, #1` (the entry address of the relocated copy) is emitted AFTER the copy loop in the ROM and BEFORE it by every candidate: agbcc hoists it into the loop preheader, under the `while`'s own line note immediately ahead of the guard, which is where LICM writes. The value is then live across the loop and the whole function is allocated one register to the left -- a1 lands in r5 instead of r4 and the last parameter is pushed out to `ip`. Symptom 2 (loop entry): the ROM `b`s to a bottom test SHARED by the entry path and the back edge with one `lsrs r1, r0, #0x10` serving both; candidates get `duplicate_loop_exit_test` instead. Explicit `goto`s DO suppress the duplication and produce the `b`, but the two `lsr`s then fail to cross-jump because the hoisted `mov rN, sp; adds rN, #1` sits between the entry path's `lsr` and its `b` and breaks the common tail -- so symptom 2 is DOWNSTREAM of the hoist. Symptom 3: the ROM loads the copied function's pool word TWICE (once for the THUMB-bit XOR, once for the size subtraction) and XORs in place; candidates load once and keep it live. Same family -- one value fewer live in the ROM.",
   "siblings": [
    "sub_0808AF00",
    "sub_0808AF74"
   ],
   "ruled_out": [
    "`for (n = k; n != 0; n--)` vs `while (n != 0) { ...; n--; }`: byte-identical output. The loop spelling is not the lever.",
    "a nested `{ }` block around the loop: block notes do not reach the region `duplicate_loop_exit_test` scans.",
    "`while (n--)`: materialises the 0xFFFF sentinel in its own pool word and costs a register. Visibly wrong.",
    "a `volatile` destination pointer in the copy loop: identical output, so the hoist is NOT blocked by a volatile store inside the loop.",
    "reusing the source-pointer local as the call target instead of a fresh name: agbcc splits the live range and hoists anyway.",
    "binding the call target to a declared `void (*fp)(...)` local: same.",
    "CONTROL: with NO loop at all, a probe puts `sp + 1` at the call exactly where the ROM has it -- so the hoist is caused by the LOOP, not by how the call is spelled."
   ],
   "next": "1. Anything that stops agbcc hoisting a FRAME-ADDRESS constant into a loop preheader. Everything else falls out of it. 2. The permuter -- order-wrong and slot-wrong are precisely its cases and this residual is instruction placement plus register slots, not a missing instruction. Read the 'Self-relocating code' chapter in docs/agbcc-codegen.md (wave 44, W44-G) first: the idiom itself (THUMB-bit XOR, inter-function size arithmetic, the (u16)(size/2) halfword counter) is FULLY DERIVED and is NOT the open question.",
   "wave": 44,
   "agent": "W44-G"
  },
  "sub_0808AF00": {
   "bytes": 116,
   "best": "116 bytes (size-exact), 23.3% identical at +0x0",
   "remaining_diff": "ONE CAUSE, THREE FUNCTIONS -- do not work these separately. `mov r3, sp; adds r3, #1` (the entry address of the relocated copy) is emitted AFTER the copy loop in the ROM and BEFORE it by every candidate: agbcc hoists it into the loop preheader, under the `while`'s own line note immediately ahead of the guard, which is where LICM writes. The value is then live across the loop and the whole function is allocated one register to the left -- a1 lands in r5 instead of r4 and the last parameter is pushed out to `ip`. Symptom 2 (loop entry): the ROM `b`s to a bottom test SHARED by the entry path and the back edge with one `lsrs r1, r0, #0x10` serving both; candidates get `duplicate_loop_exit_test` instead. Explicit `goto`s DO suppress the duplication and produce the `b`, but the two `lsr`s then fail to cross-jump because the hoisted `mov rN, sp; adds rN, #1` sits between the entry path's `lsr` and its `b` and breaks the common tail -- so symptom 2 is DOWNSTREAM of the hoist. Symptom 3: the ROM loads the copied function's pool word TWICE (once for the THUMB-bit XOR, once for the size subtraction) and XORs in place; candidates load once and keep it live. Same family -- one value fewer live in the ROM.",
   "siblings": [
    "sub_0808AE54",
    "sub_0808AF74"
   ],
   "ruled_out": [
    "`for (n = k; n != 0; n--)` vs `while (n != 0) { ...; n--; }`: byte-identical output. The loop spelling is not the lever.",
    "a nested `{ }` block around the loop: block notes do not reach the region `duplicate_loop_exit_test` scans.",
    "`while (n--)`: materialises the 0xFFFF sentinel in its own pool word and costs a register. Visibly wrong.",
    "a `volatile` destination pointer in the copy loop: identical output, so the hoist is NOT blocked by a volatile store inside the loop.",
    "reusing the source-pointer local as the call target instead of a fresh name: agbcc splits the live range and hoists anyway.",
    "binding the call target to a declared `void (*fp)(...)` local: same.",
    "CONTROL: with NO loop at all, a probe puts `sp + 1` at the call exactly where the ROM has it -- so the hoist is caused by the LOOP, not by how the call is spelled."
   ],
   "next": "1. Anything that stops agbcc hoisting a FRAME-ADDRESS constant into a loop preheader. Everything else falls out of it. 2. The permuter -- order-wrong and slot-wrong are precisely its cases and this residual is instruction placement plus register slots, not a missing instruction. Read the 'Self-relocating code' chapter in docs/agbcc-codegen.md (wave 44, W44-G) first: the idiom itself (THUMB-bit XOR, inter-function size arithmetic, the (u16)(size/2) halfword counter) is FULLY DERIVED and is NOT the open question.",
   "wave": 44,
   "agent": "W44-G"
  },
  "sub_0808AF74": {
   "bytes": 116,
   "best": "120 bytes (+4), 24.1% identical at +0x0 -- the +4 is the last parameter displaced to a fifth callee-saved register by the same hoist",
   "remaining_diff": "ONE CAUSE, THREE FUNCTIONS -- do not work these separately. `mov r3, sp; adds r3, #1` (the entry address of the relocated copy) is emitted AFTER the copy loop in the ROM and BEFORE it by every candidate: agbcc hoists it into the loop preheader, under the `while`'s own line note immediately ahead of the guard, which is where LICM writes. The value is then live across the loop and the whole function is allocated one register to the left -- a1 lands in r5 instead of r4 and the last parameter is pushed out to `ip`. Symptom 2 (loop entry): the ROM `b`s to a bottom test SHARED by the entry path and the back edge with one `lsrs r1, r0, #0x10` serving both; candidates get `duplicate_loop_exit_test` instead. Explicit `goto`s DO suppress the duplication and produce the `b`, but the two `lsr`s then fail to cross-jump because the hoisted `mov rN, sp; adds rN, #1` sits between the entry path's `lsr` and its `b` and breaks the common tail -- so symptom 2 is DOWNSTREAM of the hoist. Symptom 3: the ROM loads the copied function's pool word TWICE (once for the THUMB-bit XOR, once for the size subtraction) and XORs in place; candidates load once and keep it live. Same family -- one value fewer live in the ROM.",
   "siblings": [
    "sub_0808AE54",
    "sub_0808AF00"
   ],
   "ruled_out": [
    "`for (n = k; n != 0; n--)` vs `while (n != 0) { ...; n--; }`: byte-identical output. The loop spelling is not the lever.",
    "a nested `{ }` block around the loop: block notes do not reach the region `duplicate_loop_exit_test` scans.",
    "`while (n--)`: materialises the 0xFFFF sentinel in its own pool word and costs a register. Visibly wrong.",
    "a `volatile` destination pointer in the copy loop: identical output, so the hoist is NOT blocked by a volatile store inside the loop.",
    "reusing the source-pointer local as the call target instead of a fresh name: agbcc splits the live range and hoists anyway.",
    "binding the call target to a declared `void (*fp)(...)` local: same.",
    "CONTROL: with NO loop at all, a probe puts `sp + 1` at the call exactly where the ROM has it -- so the hoist is caused by the LOOP, not by how the call is spelled."
   ],
   "next": "1. Anything that stops agbcc hoisting a FRAME-ADDRESS constant into a loop preheader. Everything else falls out of it. 2. The permuter -- order-wrong and slot-wrong are precisely its cases and this residual is instruction placement plus register slots, not a missing instruction. Read the 'Self-relocating code' chapter in docs/agbcc-codegen.md (wave 44, W44-G) first: the idiom itself (THUMB-bit XOR, inter-function size arithmetic, the (u16)(size/2) halfword counter) is FULLY DERIVED and is NOT the open question.",
   "wave": 44,
   "agent": "W44-G"
  },
  "sub_08063698": {
   "bytes": 116,
   "best": "97.4% -- 116 bytes, SIZE-EXACT, three bytes differ at +0x47 (unchanged, wave 46 W46-K)",
   "remaining_diff": "The loop's 0x100 stride is built in the wrong scratch register. The ROM has `movs r1,#0x80; lsls r1,#1; adds r4,r4,r1`; every candidate emits the same three instructions on r0, which is free at that point because it is sub_08011E54's just-dead return value. Nothing else in the function differs -- the fill block, the a1 guard, both pool orders, the argument setup and the loop bottom are byte-exact.",
   "why_it_is_close": "THE WAVE-44 DIAGNOSIS WAS WRONG AND THE FIX WAS ONE EDIT. That park read the residual as a single LICM hoist of the 0x1FFE0 mask into a callee-saved register and spent the wave trying to spell the mask so combine would build it after loop-opt. The mask was never the axis: the ROM's loop rebuilds THREE invariants every iteration -- 0x1FFE0, 0x06000000 (`movs r0,#0xc0; lsls r0,#0x13`) and the 0x100 stride -- and three un-hoisted constants in one body is the signature of a loop loop.c never processed. THE LOOP IS A GOTO LOOP. A label plus a conditional `goto` carries no NOTE_INSN_LOOP_BEG, so LICM, strength reduction and check_dbra_loop all skip it. That removed the hoist, the preheader `ldr`, the r7 push/pop pair and the +2 bytes at once. See the goto-loop chapter in docs/agbcc-codegen.md.",
   "axes_ruled_out": [
    "`u32 v` vs `int v` -- BIT-IDENTICAL output",
    "declaration order of i, v and p relative to each other -- BIT-IDENTICAL, and this is the lever that worked twice elsewhere in the same batch (sub_0806384C, sub_08063528), so it was the first thing tried",
    "binding 0x100 to its own local before the add -- BIT-IDENTICAL",
    "240 s of decomp-permuter from the 97.4% draft -- 16,000 iterations, nothing better than the starting point; it sits on a score-20 plateau, which is this one register",
    "everything the wave-44 park lists about the mask spelling, which is now moot: there is no hoist left to spell away",
    "wave 46 (W46-K): `u8 *v` with `(u32)v & 0x1FFE0` at the use -- BIT-IDENTICAL",
    "wave 46 (W46-K): the 0x100 bound to a temp declared at the top of its own block after the call, i.e. W46-A lever 3 in its literal form -- BIT-IDENTICAL",
    "wave 46 (W46-K): the destination address bound to a `void *dst` local in its own statement before the call, i.e. W46-A lever 2 in its literal form -- BIT-IDENTICAL",
    "wave 46 (W46-K): decomp-permuter 300 s on 8 threads, 34,638 iterations, nothing better than the starting point. With wave 44's 240 s that is ~50,000 iterations on a score-20 plateau. DO NOT RUN THE PERMUTER ON THIS FUNCTION AGAIN.",
    "wave 46 (W46-K): W46-A's levers 1 and 3 CANNOT APPLY HERE AT ALL -- they are LICM and strength_reduce levers and this is a goto loop, so neither pass runs on it. Any future instruction to fix this with a preheader/giv rule is wrong for the same structural reason."
   ],
   "settled_by_this_attempt": [
    "The loop is a GOTO loop, not a `for` or a `do`/`while`. This is the whole finding and it generalises -- it also explains sub_08063528's 0xE7/0xE8 arm.",
    "The 0x1FFE0 mask needs no special spelling. Write `v & 0x1FFE0` honestly; combine narrows it from the source constant and the pool word comes out right.",
    "`sub_08011E54(p, dst, 0xC0)` with `u16 *p` advanced by 0x60 is correct -- the argument setup is byte-exact.",
    "The counter is `i = 5` with `if (--i >= 0) goto loop;` -- six passes, and the loop bottom matches exactly.",
    "wave 46 (W46-K), FIRST POSITIVE READOUT ON THE RESIDUAL: a probe placed the same `v += 0x100` in two basic blocks of one function (an if/else with identical arms). In the arm that is a fall-through continuation of the call's block the constant landed in r0; in the arm that is a BRANCH TARGET it landed in r1, the ROM's register. The residual is therefore a basic-block/liveness effect, not an expression-order one: agbcc picks r1 exactly when the block holding the constant has a predecessor edge other than the call's straight-line fall-through. The open question is narrow -- what source gives the increment section its own incoming edge at zero instruction cost? Stop respelling expressions."
   ],
   "reason": "Register allocation, and only that -- now localised. One constant lands in r0 where the ROM puts it in r1, in a function otherwise byte-for-byte correct at the right size. It is a BASIC-BLOCK EDGE question, not an expression-spelling one (see the W46-K chapter in docs/agbcc-codegen.md). Do not re-run the permuter and do not re-try LICM/giv levers: this is a goto loop, so those passes never run."
  },
  "sub_0806412C": {
   "bytes": 232,
   "best": "52.2% -- 232 bytes, SIZE-EXACT",
   "remaining_diff": "TWO facts. (a) check_dbra_loop REVERSES the first loop's counter here and did not in the ROM. The ROM keeps `movs r1,#0` in the preheader with `adds r1,#1; cmp r1,#7; ble` at the bottom ALONGSIDE the 0x18 giv; the draft emits `movs r3,#7` with `subs r3,#1; cmp r3,#0; bge` and no separate init. Same instruction count. It also costs the second loop's `adds r3,r1,#1` / `adds r1,r3,#0` shuffle, which is that loop reusing r1 for both the counter and the row pointer. (b) The second loop's base comes out as ONE pool word relocated `gUnknown_0202F110+0x2`, where the ROM materialises the bare symbol and biases it (`ldr r0,=g; adds r4,r0,#2`) so that the one pool word is shared with the six halfword stores after the loop -- the ROM has six pool words, the draft seven.",
   "why_it_is_close": "Everything else is exact: the eight-parameter prologue with four u16s spilled to a 0x10 frame and read back with `ldrh`, both `stm`/`strb` copy loops, the `ldrsh` walk with `* 0x1000`, the six halfword stores and the two 20.12 globals. The size already matches.",
   "axes_ruled_out": [
    "LOOP FORM does not move the reversal: `for (i = 0; i <= 7; i++)`, `for (i = 0; i < 8; i++)` and `do { } while (++i <= 7);` all reverse, with the inner loop written ascending or descending, and with `i = 0` placed before or after the v7/v8 statements",
    "writing the outer loop as a GOTO loop DOES keep the counter ascending -- loop.c never sees it -- but it also loses strength reduction, so the 0x18 stride must be an explicit source variable, and agbcc then hoists that variable's increment ABOVE the inner loop where the ROM has it at the bottom next to the counter. The ROM needs BOTH the giv and the un-reversed counter and nothing tried gives both.",
    "binding gUnknown_0202F110's base to a `u8 *` local to force the ROM's two-instruction preheader is much WORSE, not better: agbcc emits a `.LC` rodata word holding the address and loads through it indirectly at every use, costing four instructions and a rodata entry",
    "wave 46 (W46-K) re-confirmed W46-B by probe: hoisting `i = 0` into a statement of its own placed BEFORE the v7/v8 statements, with the loop written `for (; i <= 7; i++)`, still reverses; so does the same init with `do { ... i++; } while (i <= 7);`. The ROM's `movs r1,#0` sitting ahead of the v7/v8 insns is NOT reachable by moving the source init."
   ],
   "settled_by_this_attempt": [
    "BINDING `a7 * 0x1000` AND `a8 * 0x1000` TO LOCALS IS LOAD-BEARING and was the whole first half of the fix. Written inline at their two stores, the address of gUnknown_0202F140 wins ip, a7 spills to a fifth frame slot (`sub sp,#0x14` instead of `#0x10`, every stack argument +4) and the address is LICM-hoisted out of the first loop -- where the ROM rematerialises it every iteration. With v7/v8 bound, ip and r8 hold them, the frame is 0x10 and the hoist is gone. This is the wave-17 'two address constants tie and the loser is rematerialised' rule seen from the winning side.",
    "All eight parameters are u16 and the first four really are spilled to the frame as whole words and read back with `ldrh` -- that is PROMOTE_MODE plus pressure, not a narrower local.",
    "gUnknown_0858089C is `const s16[24]` read with `ldrsh` and scaled `* 0x1000`; gUnknown_085808CC is `const u8[24]`. Both are walked by ONE pointer that is never reset between rows, so the counts are totals, not strides.",
    "gUnknown_0202F140 is eight 0x18-byte entries of two three-word vectors, and gUnknown_0202F110 is six 8-byte rows of `u16 + u8[4]`. Both layouts are corroborated independently by sub_08064214, which is MATCHED.",
    "wave 46 (W46-K): the counter reversal is the SOLE cause of the first loop's diff -- the giv itself is already correct. The candidate emits `adds rD, r5, r2` with the same operand order as the ROM's `adds r3, r5, r2` and differs only in the destination register, which follows from r3 being consumed by the reversed counter. Fixing the reversal is expected to fix the second loop's `adds r3,r1,#1` / `adds r1,r3,#0` shuffle with it. Defect (b), the `gUnknown_0202F110+0x2` pool word, is worth ONE pool word and is independent of (a). The draft is SIZE-EXACT; its 52.2% is positional and must not be read as a wrong shape."
   ],
   "reason": "Two loop-optimiser facts, not source semantics. The type model, the statement order and the size are settled; what remains is one check_dbra_loop reversal that no loop form reaches and one address-constant biasing. Re-test whenever a rule about suppressing check_dbra_loop lands in docs/agbcc-codegen.md."
  },
  "sub_08073228": {
   "bytes": 220,
   "best": "29.1% (wave 46, W46-H; was 12.7%)",
   "remaining_diff": "Size-EXACT at 220 bytes and the residual is confined to the inner search loop. Byte-exact already: the whole prologue, the 0x20 frame, the ENTIRE stack-slot map ([sp,#0]=a1, [sp,#4]=a2, [sp,#8]=a4, [sp,#0xc]=acc, [sp,#0x10]=proc+0x2a, [sp,#0x14]=a3*0x20, [sp,#0x18]=proc+0x46, [sp,#0x1c]=i*2), every callee-saved assignment (r5=i, r6=i*0x100, r8=i*0x10, sb=acc<<16, sl=i+1, r4=j*8), the CpuFastSet call, both proc stores, the acc update and the whole epilogue. TWO residuals remain. (1) LOOP ROTATION: the ROM's inner search is UNROTATED -- exit test at the top, unconditional `b` at the bottom, with a dead `adds r0, r4, #0` ahead of the test -- and it strength-reduces j*8 to `adds r4, #8`. The candidate comes out ROTATED (peeled test, bottom-tested `bne`) and recomputes `lsls r4, r3, #3` each iteration. (2) The ROM's compare is `cmp r3, r7`, i.e. `c` on the LEFT, and the VRAM destination groups as (i*0x100 + 0x06010000) + a3*0x20 with the 0x06010000 pool word loaded INSIDE the loop; the candidate lets LICM hoist `0x06010000 + a3*0x20` together as one invariant. NEXT LEVER -- one question only: make agbcc leave the inner search loop UNROTATED. `while (tbl[j].unk00 != c) j++;`, `for (;;) { if (...) break; j++; }` and the for(;;) with a leading `k = j * 8;` all rotate. The ROM's dead `adds r0, r4, #0` ahead of the exit test is the tell that a source statement sits there. Fixing rotation is expected to fix the compare operand order and the j*8 strength reduction with it, because both moved together in every probe.",
   "why_it_is_close": "Everything outside the inner search loop is byte-identical, including the frame size and every stack slot -- the two things the previous 12.7% attempt got wrong. The score is positional and understates it.",
   "axes_ruled_out": [
    "PROTOTYPE. `void sub_08073228(const void *, void *, u16, ProcPtr)` is correct; two callers of sub_08073304 matched byte-for-byte this wave corroborate the chain. Do not re-derive it.",
    "`u16 acc` with an explicit (s16) cast at the read -- convert_to_integer distributes the narrowing over the `+` and deletes it. Not the spelling.",
    "gUnknown_08614028 as a second object -- it is this table's `unk04` member (include/unknown-globals.h:15243). The candidate's `.word gUnknown_08614024+4` against the ROM's `.word gUnknown_08614028+0` is the documented same-address false mismatch, NOT a real difference.",
    "The flat `*((u8 *)gUnknown_08614024 + k)` table spelling: it gets the compare operand order right but DROPS the frame back to 0x1c and puts a1 in sl. The struct-index spelling is required for the frame.",
    "Swapping the compare to `c == tbl[j].unk00` on its own: same effect -- correct `cmp` operand order but the frame collapses to 0x1c (216 bytes, 20.0%). The compare order and the frame are COUPLED; do not chase them separately.",
    "TOOLCHAIN. Not the axis: sub_08073714 and two block neighbours matched on the default toolchain this wave, and data/compiler-overrides.json has no entry anywhere in 0x08073.",
    "The wave-46 orchestrator's advice to 'aim for eight distinct stack-resident locals' -- REFUTED, see settled_by_this_attempt."
   ],
   "settled_by_this_attempt": [
    "`short acc` -- a SIGNED short LOCAL -- is what produces the ROM's `lsls #16; asrs #16` on every read and `lsls #16; lsrs #16` on every write. agbcc's PROMOTE_MODE forces sub-word LOCALS (not just parameters) to zero-extended SImode, so a signed short local sign-extends at each read and truncates at each write. This closes the previous attempt's 'defect 2' outright, and it explains for free why the ROM computes acc<<16 at the TOP of the loop body and the `asrs` at the BOTTOM: the two halves straddle the CpuFastSet call because the value must be carried in a callee-saved register.",
    "Using the PARAMETERS DIRECTLY with casts at each use (`((const u8 *)a1)[i]`, `((struct Unk73228Proc *)a4)->unk58`) rather than through aliasing locals `str`/`proc` is what produces the ROM's 0x20 frame with a1, a2 and a4 spilled to [sp,#0]/[sp,#4]/[sp,#8] in PARAMETER ORDER. With the aliasing locals, a1 stays in sl and the frame is 0x1c. The casts are byte-neutral; the aliasing locals are not.",
    "Adding `k = j * 8;` as the first statement of a `for (;;)` inner loop, with k used after the loop for the `.unk04` access, is what puts a real insn ahead of the exit test and makes j*8 survive the loop in r4.",
    "The stack-slot map, the frame size and every callee-saved register assignment are now exactly the ROM's, so the remaining work is ONLY the inner loop's shape, not allocation.",
    "[sp,#0x10], [sp,#0x14], [sp,#0x18] and [sp,#0x1c] are LICM hoists and strength_reduce giv temporaries and must NOT be authored as source locals. The 'eight distinct stack-resident locals' reading of the slot map counted four compiler-generated temporaries as source variables; only a1, a2, a4 and acc are author-level."
   ],
   "reason": "2 try_match attempts spent; W46-H had a single round and three other functions."
  },
  "sub_08073304": {
   "bytes": 156,
   "best": "97.4% (wave 46, W46-A; reviewed by W46-H, no new attempt)",
   "remaining_diff": "FOUR bytes, size-exact, and they are one fact: `src` and `a6` hold each other's high register. ROM: src->r9, dst->r8, a6->sl. Candidate: src->sl, dst->r8, a6->r9. Everything else is byte-exact. NEXT LEVER: break the regmove coalesce of `v = a6;` without adding an instruction, or otherwise get a6 back to 2 references. The do/while(0) currently in the draft is a permuter artefact rather than plausible original source, so a spelling that reaches dst->r8 honestly may reach the whole allocation.",
   "why_it_is_close": "A clean from-the-assembly rewrite landed on the SAME residual as the inherited wave-35 draft it replaced, which is evidence the residual belongs to the function rather than to any author's structure.",
   "axes_ruled_out": [
    "PROTOTYPE. The seven-argument declaration is corroborated by two callers matched byte-for-byte this wave (sub_08065238, sub_0806540C), including that three arguments arrive on the stack. Changing it would break both.",
    "`proc->unk64 = a6 ? -1 : 0;` -- 76.9%.",
    "Declaration order of `v` and `proc` -- byte-identical either way.",
    "Moving `v = a6;` inside the do/while(0) head block -- WORSE (96.2%, a6 lands in r8). The lever must lower a6's REFERENCE COUNT, not shorten its live range.",
    "PERMUTER -- exhausted. 22k iterations took it 96.2% -> 97.4% (that is where the do/while(0) came from); a further 300 s run from the improved start found nothing."
   ],
   "settled_by_this_attempt": [
    "The model: agbcc's allocno_compare ranks roughly floor_log2(n_refs) * n_refs / live_length. dst has 3 refs, src 2, a6 2 with a longer range, which gives the ROM's order dst, src, a6. The candidate gets dst, a6, src because agbcc COALESCES the plain copy `v = a6;`, making v and a6 one 4-reference allocno whose extra references outweigh a6's longer range and lift it above src.",
    "The four entry narrowings, the (u16)((a4 + 0x10) * 0x20) palette index, the walking +0x5c/+0x5e/+0x5f/+0x68 store run and the fall-through tail are all exact."
   ],
   "reason": "Permuter exhausted and the residual is 4 bytes of pure allocation; W46-H spent its single round on the three functions with structural room."
  },
  "sub_08073480": {
   "bytes": 244,
   "best": "76.2% (wave 46, W46-H permuter run; was 50.4%)",
   "remaining_diff": "First difference now at +0x52, inside the per-scanline loop. The ROM keeps the `sin * unkNN >> 20` chain in a FRESH register -- hence an `adds rD, r0, #0` copy before each `muls`, since the THUMB multiply ties its destination to operand 1 -- and loads gUnknown_0300200C / gUnknown_03002000 into r0. The candidate coalesces the multiply into the sin value's register and puts the globals in the scratch instead. NEXT LEVER: re-run the permuter from the new 76.2% best.c. The first run was still improving when its 300 s expired, and a better starting point is what the tool itself says helps most.",
   "why_it_is_close": "Size-exact at 244 bytes with the instruction order already correct.",
   "axes_ruled_out": [
    "Binding the sin value to a named local (`s = gSinLut[...]`) -- byte-identical, agbcc coalesces it anyway.",
    "TOOLCHAIN. data/compiler-overrides.json has no entry in 0x08073 and sub_08073714 matched on the default toolchain this wave.",
    "'The permuter cannot fix this' -- REFUTED, see settled_by_this_attempt."
   ],
   "settled_by_this_attempt": [
    "THE PERMUTER DOES HELP HERE. The previous attempt parked this as 'two instructions SHORT rather than slot-wrong, which the brief says the permuter cannot fix' and never ran it. One 300 s / 4-thread run took it from 50.4% to 76.2% and moved the first difference from +0xe to +0x52. A register-allocation residual that manifests as MISSING COPIES is still a register-allocation residual; the brief's exclusion is for a residual of one EXTRA instruction, which this is not.",
    "The head reaches gUnknown_0202FDE4 through agbcc's own -fforce-addr .rodata word at 0x081CC024 while the loop and the DMA tail use a plain inline pool word; the honest spelling reproduces both levels. PROMOTION MUST CARRY \"rodata\": [\"0x081CC024\"].",
    "`dst = gUnknown_0202FDE4; dst += i * 2;` as two separate statements is required -- one expression sinks the `ldr` past the index computation.",
    "`*((b & 0xff) + gSinLut)` rather than `gSinLut[b & 0xff]`: the index-first spelling creates the 0xff pseudo before gSinLut's, which is what decides that 0xff wins r8 and gSinLut gets ip."
   ],
   "reason": "Permuter run improved it substantially but did not close it; W46-H's single round ended."
  },
  "sub_08087040": {
   "bytes": 120,
   "best": "116 bytes (-4), 23.3% identical, first difference at +0xa. NOTE work/sub_08087040/best.c holds a permuter artefact scored 55.8% at size 120 which is NOT a real candidate -- it reads two locals before either is assigned and reaches 120 bytes through undefined behaviour. 23.3% is the honest state. Wave 46 W46-J re-ran the draft to confirm this; do not quote 55.8% as this function's score and reset best.c before any further permuter run.",
   "remaining_diff": "ONE coupled fact worth 4 bytes. The ROM spends `sub sp, #8` and keeps the 0x54 x-giv SPILLED at [sp, #4] (`movs r0,#84; str r0,[sp,#4]` in the preheader, `ldr r0,[sp,#4]` at each use, `ldr r0,[sp,#4]; adds r0,#8; str r0,[sp,#4]` at the bottom). That leaves r7 free as a low scratch, so the ROM emits `mov r7, sl; ldr r3, [r7]` LAST in each call's argument setup, after r0/r1/r2. The candidate fits all seven live values in registers (`sub sp, #4`, 0x54 in r7, `adds r7, #8`), needs no callee-saved low scratch, and gcc therefore schedules the `ldr r3` to the FRONT of each iteration where r0-r3 are free. Spill and load position are one decision, not two. Also cosmetic and downstream of it: the 0x2000 is built in r7 in the ROM and r0 here.",
   "why_it_is_close": "Every structural choice is forced by controlled probe and the instruction stream is otherwise complete: the preheader is in the ROM's exact order (p, q, 0x2000, 0x54, 0x50, 0x97, counter), all three coordinates are strength-reduction givs, the reversed counter and its `subs/cmp/bge` are exact, both call bodies and both pool words are exact. The mechanism is understood: FOUR of the seven live values demand LOW registers (the counter and all three givs are updated with `adds rN,#imm8` / `subs rN,#1`) and there are exactly four low callee-saved registers, so a FIFTH low demand -- the reload scratch for the high-register reads -- is what tips the ROM into spilling one giv. What is missing is an EIGHTH live value; nothing in the ROM's instruction stream accounts for one, so it is probably a source local this reading folds away.",
   "axes_ruled_out": "Waves 44, 46 (W46-C) and 46 (W46-J): accumulators instead of givs; `x` / `x + 4` instead of two x givs (fewer live values, moves the wrong way); volatile on the pointer objects and on the deref; binding the OAM word to a local before each call (reorders the whole body); naming gUnknown_081D9440/44 directly rather than binding their addresses to `u16 **` locals (agbcc routes the symbol through force_const_mem and emits a .rodata word plus a DOUBLE indirection per use, where the ROM has a single `ldr r3, [r7]` off a plain `.4byte` TEXT pool word); a source-level descending loop (a source init would sit FIRST in the preheader, ahead of the LICM-hoisted 0x2000, and the ROM has no for-init in the preheader at all); and swapping the declaration order of the two pointer locals. DECOMP-PERMUTER WAS RUN AND FAILED: 900 seconds over two runs, 4 threads. That is a fair test of the brief's \"the permuter is not useless on register allocation\" rule and it came out NEGATIVE here even though the residual looks exactly like the advertised case (slot-wrong, stream otherwise right).",
   "settled_by_this_attempt": "W46-J's only contribution is verification: the draft was re-run from scratch and scores 23.3% at 116 bytes, confirming W46-C's analysis and establishing that the 55.8% carried in best.json and quoted in the wave-46 brief is a permuter artefact. No new lever was found. The next reader should attack the missing eighth live value directly and should not re-run the permuter without resetting best.c first.",
   "reason": "Two independent agents (W46-C, W46-J) reached the same 4-byte spill residual with the same source shape; the permuter has been run and failed."
  },
  "sub_0808AAF4": {
   "bytes": 152,
   "best": "28.3% (wave 46, W46-D then W46-I)",
   "remaining_diff": "Size-EXACT at 152 bytes; first difference at +0x0, i.e. the push itself -- the ROM opens `push {r4, r5, lr}` and the candidate `push {r4, r5, r6, lr}`, one callee-saved register too many. THIS IS SELF-RELOCATING CODE: it calls sub_0808AD6C, which copies four bytes of a SIBLING'S MACHINE CODE onto the stack, then computes r5 = sp+1 (the THUMB entry point of those bytes) and calls it twice via `bl _call_via_r5`, holding r5 live across both calls and two delay loops. That lifetime is what forces a callee-saved register. The ROM also emits its literal pool MID-FUNCTION, jumped over by a `b.n`, holding 0x0E005555 / 0x0E002AAA / 0x00004E20 (flash unlock command addresses and a delay count).",
   "ruled_out": "TOOLCHAIN AXIS RULED OUT (wave 46, W46-I): the candidate C was held constant and compiled under five configurations -- default, -fprologue-bugfix off, -fforce-addr off, old_agbcc, and old_agbcc + no force-addr -- with .text compared against work/<fn>/_target.bin each time. old_agbcc is byte-for-byte IDENTICAL to the default on sub_0808AAF4 and sub_0808AC7C (it does not change one instruction) and is strictly WORSE on sub_0808A3DC (64 -> 71 differing bytes). Dropping -fforce-addr is worse on three of four. The wave-46 brief predicted this block needed old_agbcc on the strength of compiler-overrides.json's 'the sound and flash libraries came out of the SDK prebuilt with their own settings' plus four of six functions differing at +0x0; that prediction is REFUTED. Do not re-run it. Full table in docs/agbcc-codegen.md, 'The flash driver at 0x0808A000 is NOT a toolchain-axis block'. ALSO RULED OUT: reading `_call_via_r5` as an argument count. It is called with ONE argument each time; for callee-saved N the index says only that the pointer had to survive a call. See docs/agbcc-codegen.md, '`_call_via_rN` for a CALLEE-SAVED N is not an argument count at all'."
  },
  "sub_0808AB8C": {
   "bytes": 148,
   "best": "55.4% (wave 46, W46-I; was 5.4%)",
   "remaining_diff": "First difference at +0x4. Calls sub_0808AAF4, which is itself unmatched and parked, so its prototype is not yet settled by a matching definition -- expect this one to move once sub_0808AAF4 closes. 7 data_refs.",
   "ruled_out": "TOOLCHAIN AXIS RULED OUT (wave 46, W46-I): the candidate C was held constant and compiled under five configurations -- default, -fprologue-bugfix off, -fforce-addr off, old_agbcc, and old_agbcc + no force-addr -- with .text compared against work/<fn>/_target.bin each time. old_agbcc is byte-for-byte IDENTICAL to the default on sub_0808AAF4 and sub_0808AC7C (it does not change one instruction) and is strictly WORSE on sub_0808A3DC (64 -> 71 differing bytes). Dropping -fforce-addr is worse on three of four. The wave-46 brief predicted this block needed old_agbcc on the strength of compiler-overrides.json's 'the sound and flash libraries came out of the SDK prebuilt with their own settings' plus four of six functions differing at +0x0; that prediction is REFUTED. Do not re-run it. Full table in docs/agbcc-codegen.md, 'The flash driver at 0x0808A000 is NOT a toolchain-axis block'."
  },
  "sub_0808AC7C": {
   "bytes": 168,
   "best": "47.0% (wave 46, W46-D then W46-I)",
   "remaining_diff": "First difference at +0xf -- later than its block-mates, so the prologue is right and the residual is in the body. 0 calls, 6 data_refs, so it is a table-reader with no prototype dependencies. Called by sub_0808ADA4.",
   "ruled_out": "TOOLCHAIN AXIS RULED OUT (wave 46, W46-I): the candidate C was held constant and compiled under five configurations -- default, -fprologue-bugfix off, -fforce-addr off, old_agbcc, and old_agbcc + no force-addr -- with .text compared against work/<fn>/_target.bin each time. old_agbcc is byte-for-byte IDENTICAL to the default on sub_0808AAF4 and sub_0808AC7C (it does not change one instruction) and is strictly WORSE on sub_0808A3DC (64 -> 71 differing bytes). Dropping -fforce-addr is worse on three of four. The wave-46 brief predicted this block needed old_agbcc on the strength of compiler-overrides.json's 'the sound and flash libraries came out of the SDK prebuilt with their own settings' plus four of six functions differing at +0x0; that prediction is REFUTED. Do not re-run it. Full table in docs/agbcc-codegen.md, 'The flash driver at 0x0808A000 is NOT a toolchain-axis block'. Note old_agbcc is byte-IDENTICAL to the default here -- it does not change one instruction, so the axis is not merely unhelpful but inert."
  },
  "sub_0808ADA4": {
   "bytes": 140,
   "best": "14.3% (wave 46, W46-D then W46-I)",
   "remaining_diff": "First difference at +0x2 -- the second register-save instruction. Uses `bl _call_via_r1` against gUnknown_03000F6C's declared `u8 (*)(u8 *)`, ONE argument, which is consistent with the scratch-register reading of _call_via_rN. Also calls sub_0808AC7C (parked) and sub_0808AD24.",
   "ruled_out": "TOOLCHAIN AXIS RULED OUT (wave 46, W46-I): the candidate C was held constant and compiled under five configurations -- default, -fprologue-bugfix off, -fforce-addr off, old_agbcc, and old_agbcc + no force-addr -- with .text compared against work/<fn>/_target.bin each time. old_agbcc is byte-for-byte IDENTICAL to the default on sub_0808AAF4 and sub_0808AC7C (it does not change one instruction) and is strictly WORSE on sub_0808A3DC (64 -> 71 differing bytes). Dropping -fforce-addr is worse on three of four. The wave-46 brief predicted this block needed old_agbcc on the strength of compiler-overrides.json's 'the sound and flash libraries came out of the SDK prebuilt with their own settings' plus four of six functions differing at +0x0; that prediction is REFUTED. Do not re-run it. Full table in docs/agbcc-codegen.md, 'The flash driver at 0x0808A000 is NOT a toolchain-axis block'."
  },
  "sub_0808A3DC": {
   "bytes": 160,
   "best": "60.0% (wave 46, W46-D then W46-I)",
   "remaining_diff": "First difference at +0xa. Calls CpuFastSet, CpuSet and Decompress -- all hub callees with settled prototypes, so the residual is not a prototype problem. The usual trap in this shape is the CpuSet/CpuFastSet mode word (count OR'd with the fill/copy flag), which is a constant expression at the call site.",
   "ruled_out": "TOOLCHAIN AXIS RULED OUT (wave 46, W46-I): the candidate C was held constant and compiled under five configurations -- default, -fprologue-bugfix off, -fforce-addr off, old_agbcc, and old_agbcc + no force-addr -- with .text compared against work/<fn>/_target.bin each time. old_agbcc is byte-for-byte IDENTICAL to the default on sub_0808AAF4 and sub_0808AC7C (it does not change one instruction) and is strictly WORSE on sub_0808A3DC (64 -> 71 differing bytes). Dropping -fforce-addr is worse on three of four. The wave-46 brief predicted this block needed old_agbcc on the strength of compiler-overrides.json's 'the sound and flash libraries came out of the SDK prebuilt with their own settings' plus four of six functions differing at +0x0; that prediction is REFUTED. Do not re-run it. Full table in docs/agbcc-codegen.md, 'The flash driver at 0x0808A000 is NOT a toolchain-axis block'. This is the function old_agbcc makes strictly WORSE (64 -> 71 differing bytes), which is the strongest single piece of evidence against the axis for this block."
  },
  "sub_080705AC": {
   "bytes": 44,
   "best": "44 bytes (+0), 97.7% identical -- 1 of 44 bytes differ",
   "remaining_diff": "First and only difference at +0x24, the literal-pool word. The loop bound 11 is NOT a literal, and three controlled probes refute every literal spelling. The ROM does `ldr r0, =0x0000000B; lsls r0,#0x10; lsrs r0,#0x10; cmp r0,#0; beq <end>` and only THEN loads gUnknown_08242308. Probed in wave 47 with compile_probe: (a) `u16 n = 11;`, (b) `n = (u16)(sizeof(arr)/sizeof(arr[0]))` with the array declared `[11]`, (c) `n = (u16)0x1000000B`. ALL THREE emit `mov r4, #0xb`, DELETE the `cmp #0` zero-trip guard entirely, and hoist the table pointer ABOVE the loop -- three separate divergences, not one. So the compiler provably did not know the value was 11; it is a symbol resolved outside the compiler.",
   "why_it_is_close": "W47-A's `n = (u16)(u32)&gUnknown_0000000B` reproduces EVERY INSTRUCTION exactly -- zero-trip guard kept, table pointer loaded below the guard, countdown counter, 0xc stride, epilogue. The ONLY remaining difference is the literal-pool word: original `.word 0x0000000b` with NO relocation, candidate `.word 0x00000000` plus `R_ARM_ABS32 gUnknown_0000000B`. That is an ABSOLUTE symbol resolved at LINK time, which is the AGB sound-tool convention -- the generated music-player table ships with its entry count as an absolute. The LINKED bytes are identical either way; only tools/trymatch.py's unit-level relocation check rejects it, and there is no C spelling that makes the assembler resolve a symbol in-unit. THIS IS A HARNESS LIMIT, NOT A DECOMPILATION FAILURE.",
   "axes_ruled_out": "Literal 11 as a u16 local; literal 11 via sizeof/ARRAY_COUNT on a sized array; literal 11 via a wider constant truncated with (u16). All three refuted by compile_probe in wave 47 -- each emits `mov r4, #0xb`, drops the zero-trip guard AND hoists the table load, so each fails on three counts, not one. Eleven-iteration loops elsewhere in the m4a block share the fingerprint.",
   "settled_by_this_attempt": "The instruction stream is SOLVED and should not be re-derived. What is open is a harness-policy question: whether an R_ARM_ABS32 against a symbol the linker resolves to the same word should count as a match in tools/trymatch.py. That is a change to the matching criterion and needs the user, not a wave. sub_080703F4 (m4aSoundInit, 120B) contains the same eleven-iteration construct and is blocked identically. TWO concrete changes are needed and W47-B established the first: (1) `tools/gen_lds.py` invents no symbol for 0x0000000B, so the SPLIT build cannot link a promoted reader -- one line, `gUnknown_0000000B = 0x0000000B;` among the linker script's absolute definitions, fixes that and cannot move any address because it defines a symbol rather than placing anything. (2) `reloc_equivalent()` in tools/trymatch.py requires `len(t_rel) == len(c_rel)` and returns False immediately when the candidate carries a relocation the original does not. Accepting an EXTRA candidate R_ARM_ABS32 whose symbol resolves (via sym_addr's gUnknown_<addr> fallback, which already yields 11 here) to exactly the original's literal word would close it. That is a change to the matching ORACLE and was deliberately NOT made in wave 47 -- the oracle is the project's correctness guarantee and a wave should not weaken it unilaterally. Both builds reproducing SHA1 14dd0b22c894865867aff89e8116b2dffae25605 would verify the change end to end if it is ever made. Full write-up in docs/agbcc-codegen.md under 'A POOLED SMALL INTEGER IS AN ABSOLUTE SYMBOL'.",
   "reason": "m4aMPlayAllStop. Blocked on a link-time absolute symbol that trymatch's relocation check cannot accept; the C is otherwise byte-exact. Wave 47, W47-A plus orchestrator probes."
  },
  "sub_080705E4": {
   "bytes": 44,
   "best": "44 bytes (+0), 97.7% identical -- 1 of 44 bytes differ",
   "remaining_diff": "Identical to sub_080705AC in every respect. overlap_screen.py reports the two as a byte-identical CALLEE-NORMALISED pair differing only in `bl sub_08070C90` vs `bl sub_080703B8`. Same first difference at +0x24, same pool word. The loop bound 11 is NOT a literal, and three controlled probes refute every literal spelling. The ROM does `ldr r0, =0x0000000B; lsls r0,#0x10; lsrs r0,#0x10; cmp r0,#0; beq <end>` and only THEN loads gUnknown_08242308. Probed in wave 47 with compile_probe: (a) `u16 n = 11;`, (b) `n = (u16)(sizeof(arr)/sizeof(arr[0]))` with the array declared `[11]`, (c) `n = (u16)0x1000000B`. ALL THREE emit `mov r4, #0xb`, DELETE the `cmp #0` zero-trip guard entirely, and hoist the table pointer ABOVE the loop -- three separate divergences, not one. So the compiler provably did not know the value was 11; it is a symbol resolved outside the compiler.",
   "why_it_is_close": "W47-A's `n = (u16)(u32)&gUnknown_0000000B` reproduces EVERY INSTRUCTION exactly -- zero-trip guard kept, table pointer loaded below the guard, countdown counter, 0xc stride, epilogue. The ONLY remaining difference is the literal-pool word: original `.word 0x0000000b` with NO relocation, candidate `.word 0x00000000` plus `R_ARM_ABS32 gUnknown_0000000B`. That is an ABSOLUTE symbol resolved at LINK time, which is the AGB sound-tool convention -- the generated music-player table ships with its entry count as an absolute. The LINKED bytes are identical either way; only tools/trymatch.py's unit-level relocation check rejects it, and there is no C spelling that makes the assembler resolve a symbol in-unit. THIS IS A HARNESS LIMIT, NOT A DECOMPILATION FAILURE.",
   "axes_ruled_out": "Same three refuted spellings as sub_080705AC. Transcribing the sibling was correct and was done; the sibling is blocked by the same relocation, so the transcription inherits the block rather than the match.",
   "settled_by_this_attempt": "The instruction stream is SOLVED and should not be re-derived. What is open is a harness-policy question: whether an R_ARM_ABS32 against a symbol the linker resolves to the same word should count as a match in tools/trymatch.py. That is a change to the matching criterion and needs the user, not a wave. sub_080703F4 (m4aSoundInit, 120B) contains the same eleven-iteration construct and is blocked identically. Whatever unblocks sub_080705AC unblocks this one with no further work. TWO concrete changes are needed and W47-B established the first: (1) `tools/gen_lds.py` invents no symbol for 0x0000000B, so the SPLIT build cannot link a promoted reader -- one line, `gUnknown_0000000B = 0x0000000B;` among the linker script's absolute definitions, fixes that and cannot move any address because it defines a symbol rather than placing anything. (2) `reloc_equivalent()` in tools/trymatch.py requires `len(t_rel) == len(c_rel)` and returns False immediately when the candidate carries a relocation the original does not. Accepting an EXTRA candidate R_ARM_ABS32 whose symbol resolves (via sym_addr's gUnknown_<addr> fallback, which already yields 11 here) to exactly the original's literal word would close it. That is a change to the matching ORACLE and was deliberately NOT made in wave 47 -- the oracle is the project's correctness guarantee and a wave should not weaken it unilaterally. Both builds reproducing SHA1 14dd0b22c894865867aff89e8116b2dffae25605 would verify the change end to end if it is ever made. Full write-up in docs/agbcc-codegen.md under 'A POOLED SMALL INTEGER IS AN ABSOLUTE SYMBOL'.",
   "reason": "m4aMPlayAllContinue. Same link-time-absolute block as its callee-normalised twin sub_080705AC. Wave 47."
  },
  "sub_08070BAC": {
   "bytes": 228,
   "best": "228 bytes (SIZE-EXACT), 90.4% identical -- 22 bytes differ. 89.5% by hand, 90.4% after a 17,970-iteration permuter run.",
   "remaining_diff": "Six spots and NOTHING ELSE. Every one is the same phenomenon: the ROM puts a value in r1 where the candidate puts it in r0, and materialises a `movs rD, #imm` one instruction EARLIER than the candidate does. (1) +0x20 ROM `ldr r1,[r5,#0x2c]; movs r0,#0x40; ldrb r1,[r1]; ands r0,r1` -- the constant is emitted BETWEEN the pointer load and the byte load, and the pointer reuses r1; candidate `ldr r0,[r5,#0x2c]; ldrb r1,[r0]; movs r0,#0x40; ands r0,r1`. (2) +0x38 ROM copies songHeader->priority into r2 BEFORE loading mplayInfo->priority (`ldrb r0,[r7,#2]; adds r2,r0,#0; ldrb r0,[r5,#9]; cmp r0,r2`); candidate loads both then copies. (3) +0x64 loop preheader, ROM `ldrb r1,[r7]; cmp r6,r1`; candidate uses r0. (4) +0x7a ROM `mov r1,r8; str r1,[r4,#0x20]`; candidate uses r0. (5) +0xa2 ROM `movs r1,#0; mov r8,r1`; candidate uses r0. (6) +0xc2 ROM `movs r0,#0x80` then `ldrb r1,[r7,#3]`; candidate loads first. The prologue (including `mov r7,r8; push {r7}`), every branch target, the loop rotation, the eleven-store body in its exact order, both loop bottoms, the tail and the single pool word are already byte-exact.",
   "why_it_is_close": "The shape is settled. The three-term entry guard, the fact that the body is the guard's `if` rather than a set of early returns, the ident reload for the `++`, the ONE source-level `while` with a two-term condition whose exit test agbcc duplicates at the top, and the second `while` continuing from the same i and track -- all confirmed by the diff staying silent on every one of them. What is left has no source-level lever found so far.",
   "axes_ruled_out": [
    "Constant-operand order: `0x40 & x` / `0x80 & x` instead of `x & 0x40` / `x & 0x80`. fold canonicalises the constant to the right for commutative ops, so the emitted code is character-for-character identical. This was the most promising reading of the movs-before-ldrb order and it is refuted.",
    "Local declaration order: `struct MusicPlayerTrack *track; s32 i;` instead of `s32 i; struct MusicPlayerTrack *track;`. Identical output -- the allocno-order lever does not reach these pseudos.",
    "Binding mplayInfo->status to a u32 local before the guard. WORSE, not neutral: it collapses the ROM's `ldr r1,[r5,#4]; ldrh r0,[r5,#4]` pair into `lsl r0,r3,#0x10`, so the ROM genuinely reads the member twice, once as a word and once as a halfword, and the two-load spelling in the draft is correct.",
    "decomp-permuter, 300 s / 4 threads / 17,970 iterations from the 89.5% draft. Reached 90.4% (one of the six spots) and stalled. Consistent with the wave-46 finding that the permuter is unreliable on register-slot residuals.",
    "Struct layout: every offset the function touches was newly derived in this wave and each is independently confirmed by a matched sibling -- unk_0b/clock/memAccArea/tempoC by sub_080703F4 and sub_08070B34, tracks/trackCount/priority/ident by the already-promoted sub_08071420 group."
   ],
   "settled_by_this_attempt": [
    "struct SongHeader: trackCount/blockCount/priority/reverb at +0..+3, tone at +4, part[] at +8. All five members are read by this function and the offsets are byte-exact in the candidate.",
    "struct MusicPlayerInfo +0x0b unk_0b, +0x0c clock, +0x18 memAccArea, +0x22 tempoC -- all four written or read here with the widths in the header.",
    "The tempo block's SOURCE ORDER is tempoD, tempoI, tempoU, tempoC, fadeOI, which is NOT offset order (0x1c, 0x20, 0x1e, 0x22, 0x24). Byte-exact, so the order is proved rather than guessed.",
    "MPT_FLG_START == 0x40: tested in the entry guard and stored together with MPT_FLG_EXIST as 0xc0 on every started track.",
    "`(u16)mplayInfo->status` and `mplayInfo->status & 0x80000000` are two reads of the same member in one condition, emitting ldr then ldrh; binding either to a local destroys it.",
    "ORCHESTRATOR DIFF (wave 47), read this before the next attempt: all 24 differing bytes are ONE root cause and it is not the loops. The candidate keeps ONE MORE VALUE LIVE than the ROM across the entry guard, and every later difference is register renumbering downstream of that. Three sites, in order. (1) At +0x20 the ROM does `ldr r1,[r5,#0x2c]; movs r0,#0x40; ldrb r1,[r1]; ands r0,r1` -- the constant is materialised BETWEEN the pointer load and its dereference, and the pointer and the loaded byte share r1. The candidate does `ldr r0,[r5,#0x2c]; ldrb r1,[r0]; movs r0,#0x40`, using two registers where the ROM chains through one. Constant-operand order is NOT the lever -- W47-B measured that `0x40 & x` and `x & 0x40` fold identically -- so what to try is whether the dereference is written chained (`mplayInfo->tracks->flags & 0x40`) or through a named temporary. (2) At the priority compare the ROM reads `songHeader->[2]` a SECOND time: `ldrb r0,[r7,#2]; adds r2,r0,#0; ldrb r0,[r5,#9]; cmp r0,r2` reloads r0 rather than allocating a new register, while the candidate keeps the first read in r1 and never re-reads. The source therefore reads that field TWICE, in two different places, and the draft hoists it into one variable. This single change should fix sites (2) and (3) together. (3) `ldrb r1,[r7,#0]` vs `ldrb r0,[r7,#0]` is pure renumbering with no independent cause. Note r2 is live into `strb r2,[r5,#9]` on BOTH paths through the guard, which is what forces the copy-then-reload shape."
   ],
   "reason": "Pure register allocation on a size-exact candidate. Worth re-testing whenever a register-allocation rule lands in docs/agbcc-codegen.md; the six spots are one phenomenon, so a single lever should close all of them at once."
  },
  "sub_080703F4": {
   "bytes": 120,
   "best": "120 bytes (SIZE-EXACT), 99.2% identical -- ONE byte differs, and it is a relocation artefact rather than wrong code.",
   "remaining_diff": "The pool word at +0x6c. The ROM has a bare `.word 0x0000000b`; the candidate has `.word 0x00000000` plus `R_ARM_ABS32 gUnknown_0000000B`. Every instruction, every branch target, the whole loop and all eight other pool words are byte-exact and the relocations on them all match.",
   "why_it_is_close": "The C is believed correct and complete. The loop bound is the ABSOLUTE SYMBOL gUnknown_0000000B (link-time value 11), spelled `(u16)(u32)&gUnknown_0000000B` and read AFTER the four setup calls rather than in the declaration's initialiser. W47-A derived the same symbol independently on sub_080705AC; see the declaration's comment in include/unknown-globals.h and the wave-47 chapter in docs/agbcc-codegen.md.",
   "axes_ruled_out": [
    "The bound as an integer literal, in six spellings: 11, (u16)11, a u16 local, a const u16 local, sizeof(t)/sizeof(*t), and (u16) of that. All six emit `mov rN, #0xb` with no lsls/lsrs pair and no zero-trip guard, and all six are 12 bytes SHORT because they also cost a second defect (see next).",
    "The `mplayInfo->memAccArea = &gUnknown_03005BE0` store as a separate problem. It is not one. With a literal bound the loop carries one more live value, -fforce-addr builds a .rodata address-constant word, LICM hoists it, the store becomes `ldr r7,=.LC` in the preheader plus `ldr r0,[r7]` per iteration, and r7 joins the push list. With the symbol bound it is the plain `ldr r0,=sym` the ROM has. Spelling gUnknown_03005BE0 as an array vs as a scalar taken by address changes nothing on its own.",
    "Reading the bound in the declaration's initialiser. Also size-exact, but it moves the lsls/lsrs pair into the prologue and the pool word two slots earlier -- 75.0%."
   ],
   "settled_by_this_attempt": [
    "gUnknown_0000000B is real and is read here exactly as W47-A found it read in sub_080705AC -- two independent derivations on different functions.",
    "struct MusicPlayer: info at +0, track at +4, trackCount at +8, unk_0a at +0x0a as a u16 (ldrh loaded, strb stored into MusicPlayerInfo::unk_0b).",
    "CpuSet's first argument is `(void *)((s32)sub_0806F7C8 & ~1)`; agbcc materialises -2 as `movs r1,#2; rsbs r1,r1,#0`.",
    "sub_080706B0 takes the gUnknown_030057D0 CGB channel array; sub_080707F4 takes &gUnknown_03004790, agreeing with its already-promoted definition."
   ],
   "reason": "BLOCKED ON THE LINKER SCRIPT, not on the source. gUnknown_0000000B is declared but defined nowhere: tools/gen_lds.py invents no symbol at 0x0000000B and no data blob carries that address. Per-function trymatch cannot paper over it either -- sym_addr()'s gUnknown_<addr> fallback resolves the candidate's relocation, but the original's pool word is a bare 0x0000000b that no symbol database maps, so it is never masked and the two words compare as one differing byte. Adding `gUnknown_0000000B = 0x0000000B;` among the linker script's absolute definitions unblocks this function, sub_080705AC and sub_080705E4 together; it defines a symbol rather than placing anything, so it cannot move an address. Unpark all three the moment that line exists."
  },
  "sub_0808B430": {
   "bytes": 132,
   "best": "132 bytes, MATCH by exit status",
   "remaining_diff": "NONE. The compiled .text is byte-for-byte identical to the original. MATCHES PER-FUNCTION BUT CANNOT BE PROMOTED -- a state this file has not carried before, so read the whole entry before treating it as an ordinary near-miss. `tools/verify_batch.py` reports OK by exit status and `tools/trymatch.py` prints 'MATCH -- byte-for-byte identical to the original'. The blocker is at INTEGRATION. Wave 47 (W47-F) established that the flash library is built at -O1 rather than -O2 and these functions match on that flag alone with no source change. But at -O1 agbcc ALSO emits dead `.rodata` address constants -- `.section .rodata / .LC0: .word gUnknown_084856A4` -- ALONGSIDE the same words in the function's own inline literal pool, and it never eliminates them. Confirmed by reading build/src/decomp/c_0808B430.s: lines 126/130, 200 and 384/388 are the real inline pool words the ROM has, and .LC0-.LC4 are unreferenced duplicates. Removing -fforce-addr does NOT suppress them (tested: the .rodata survives a forced recompile with the flag filtered out). THE MEASURED CONSEQUENCE: the SPLIT build links a ROM whose first 8,388,608 bytes are BYTE-IDENTICAL to baserom.gba and which is 28 BYTES LONGER -- seven unplaced .rodata words (2 from c_0808B31C.o, 5 from c_0808B430.o) swept past the end of the image by the linker script's catch-all. There is no harmless address to place dead words at in a fixed-layout ROM, and data/promoted.json's `rodata` mechanism cannot help because the five words in c_0808B430.o would need to land at five NON-CONTIGUOUS addresses (0x0808B4A0, 0x0808B4B0, 0x0808B4F0, 0x0808B5A4, 0x0808B5B4) from one section. Wave 47 backed all four out rather than weaken the both-builds-reproduce-the-SHA1 rule, and removed their entries from data/compiler-overrides.json so the build does not carry rules for objects that no longer exist.",
   "why_it_is_close": "Flash library EraseFlashSector inner routine. Shares src/decomp/c_0808B430.c with sub_0808B4B4 and sub_0808B540; that file emits 5 dead .rodata words between the three.",
   "axes_ruled_out": "-O2 (the default) leaves a 4-9 byte register-assignment residual; twenty source spellings were measured against that residual across W47-D and W47-F and every one failed, plus a 38,000-iteration permuter run. -fforce-addr removal does not suppress the dead .rodata. old_agbcc is NOT the axis here -- wave 46 measured five configurations on the neighbouring 0x0808A block and found it byte-identical on two functions and strictly worse on a third.",
   "settled_by_this_attempt": "WHAT WOULD UNBLOCK THIS, in increasing order of invasiveness: (1) find a source spelling that does not make agbcc materialise the dead address constant at -O1 -- untested, and the cheapest thing to try first; (2) teach tools/gen_lds.py to send named objects' unreferenced .rodata to /DISCARD/ -- provably safe for these two because the ROM's first 8MB already reproduce without them, but it is a build-system change and a wave should not make it unilaterally; (3) check whether a different optimisation level (-Os was not tried) gives the same .text without the dead constants. NOTE the -O1 finding itself is SOUND and general -- see the chapter 'OPTIMISATION LEVEL is a third per-file axis' in docs/agbcc-codegen.md -- it is only the integration that is open. Do not re-derive the flag; it is not the question.",
   "reason": "Wave 47: matched at -O1, backed out at integration because the dead .rodata adds 28 bytes past the end of the ROM. The decompilation is DONE; the build integration is not."
  },
  "sub_0808B4B4": {
   "bytes": 140,
   "best": "140 bytes, MATCH by exit status",
   "remaining_diff": "NONE. The compiled .text is byte-for-byte identical to the original. MATCHES PER-FUNCTION BUT CANNOT BE PROMOTED -- a state this file has not carried before, so read the whole entry before treating it as an ordinary near-miss. `tools/verify_batch.py` reports OK by exit status and `tools/trymatch.py` prints 'MATCH -- byte-for-byte identical to the original'. The blocker is at INTEGRATION. Wave 47 (W47-F) established that the flash library is built at -O1 rather than -O2 and these functions match on that flag alone with no source change. But at -O1 agbcc ALSO emits dead `.rodata` address constants -- `.section .rodata / .LC0: .word gUnknown_084856A4` -- ALONGSIDE the same words in the function's own inline literal pool, and it never eliminates them. Confirmed by reading build/src/decomp/c_0808B430.s: lines 126/130, 200 and 384/388 are the real inline pool words the ROM has, and .LC0-.LC4 are unreferenced duplicates. Removing -fforce-addr does NOT suppress them (tested: the .rodata survives a forced recompile with the flag filtered out). THE MEASURED CONSEQUENCE: the SPLIT build links a ROM whose first 8,388,608 bytes are BYTE-IDENTICAL to baserom.gba and which is 28 BYTES LONGER -- seven unplaced .rodata words (2 from c_0808B31C.o, 5 from c_0808B430.o) swept past the end of the image by the linker script's catch-all. There is no harmless address to place dead words at in a fixed-layout ROM, and data/promoted.json's `rodata` mechanism cannot help because the five words in c_0808B430.o would need to land at five NON-CONTIGUOUS addresses (0x0808B4A0, 0x0808B4B0, 0x0808B4F0, 0x0808B5A4, 0x0808B5B4) from one section. Wave 47 backed all four out rather than weaken the both-builds-reproduce-the-SHA1 rule, and removed their entries from data/compiler-overrides.json so the build does not carry rules for objects that no longer exist.",
   "why_it_is_close": "Flash library EraseFlashSector. Was the function that cracked the -O1 finding: W47-D had it at 96.4% with 5 differing bytes and documented the residual as an UNSOLVED register-tie; it is byte-exact at -O1 with no source change.",
   "axes_ruled_out": "-O2 (the default) leaves a 4-9 byte register-assignment residual; twenty source spellings were measured against that residual across W47-D and W47-F and every one failed, plus a 38,000-iteration permuter run. -fforce-addr removal does not suppress the dead .rodata. old_agbcc is NOT the axis here -- wave 46 measured five configurations on the neighbouring 0x0808A block and found it byte-identical on two functions and strictly worse on a third.",
   "settled_by_this_attempt": "WHAT WOULD UNBLOCK THIS, in increasing order of invasiveness: (1) find a source spelling that does not make agbcc materialise the dead address constant at -O1 -- untested, and the cheapest thing to try first; (2) teach tools/gen_lds.py to send named objects' unreferenced .rodata to /DISCARD/ -- provably safe for these two because the ROM's first 8MB already reproduce without them, but it is a build-system change and a wave should not make it unilaterally; (3) check whether a different optimisation level (-Os was not tried) gives the same .text without the dead constants. NOTE the -O1 finding itself is SOUND and general -- see the chapter 'OPTIMISATION LEVEL is a third per-file axis' in docs/agbcc-codegen.md -- it is only the integration that is open. Do not re-derive the flag; it is not the question.",
   "reason": "Wave 47: matched at -O1, backed out at integration because the dead .rodata adds 28 bytes past the end of the ROM. The decompilation is DONE; the build integration is not."
  },
  "sub_0808B31C": {
   "bytes": 164,
   "best": "164 bytes, MATCH by exit status",
   "remaining_diff": "NONE. The compiled .text is byte-for-byte identical to the original. MATCHES PER-FUNCTION BUT CANNOT BE PROMOTED -- a state this file has not carried before, so read the whole entry before treating it as an ordinary near-miss. `tools/verify_batch.py` reports OK by exit status and `tools/trymatch.py` prints 'MATCH -- byte-for-byte identical to the original'. The blocker is at INTEGRATION. Wave 47 (W47-F) established that the flash library is built at -O1 rather than -O2 and these functions match on that flag alone with no source change. But at -O1 agbcc ALSO emits dead `.rodata` address constants -- `.section .rodata / .LC0: .word gUnknown_084856A4` -- ALONGSIDE the same words in the function's own inline literal pool, and it never eliminates them. Confirmed by reading build/src/decomp/c_0808B430.s: lines 126/130, 200 and 384/388 are the real inline pool words the ROM has, and .LC0-.LC4 are unreferenced duplicates. Removing -fforce-addr does NOT suppress them (tested: the .rodata survives a forced recompile with the flag filtered out). THE MEASURED CONSEQUENCE: the SPLIT build links a ROM whose first 8,388,608 bytes are BYTE-IDENTICAL to baserom.gba and which is 28 BYTES LONGER -- seven unplaced .rodata words (2 from c_0808B31C.o, 5 from c_0808B430.o) swept past the end of the image by the linker script's catch-all. There is no harmless address to place dead words at in a fixed-layout ROM, and data/promoted.json's `rodata` mechanism cannot help because the five words in c_0808B430.o would need to land at five NON-CONTIGUOUS addresses (0x0808B4A0, 0x0808B4B0, 0x0808B4F0, 0x0808B5A4, 0x0808B5B4) from one section. Wave 47 backed all four out rather than weaken the both-builds-reproduce-the-SHA1 rule, and removed their entries from data/compiler-overrides.json so the build does not carry rules for objects that no longer exist.",
   "why_it_is_close": "Flash library ProgramFlashSector. Emits 2 dead .rodata words (gUnknown_03005C78, gUnknown_03005C7C).",
   "axes_ruled_out": "-O2 (the default) leaves a 4-9 byte register-assignment residual; twenty source spellings were measured against that residual across W47-D and W47-F and every one failed, plus a 38,000-iteration permuter run. -fforce-addr removal does not suppress the dead .rodata. old_agbcc is NOT the axis here -- wave 46 measured five configurations on the neighbouring 0x0808A block and found it byte-identical on two functions and strictly worse on a third.",
   "settled_by_this_attempt": "WHAT WOULD UNBLOCK THIS, in increasing order of invasiveness: (1) find a source spelling that does not make agbcc materialise the dead address constant at -O1 -- untested, and the cheapest thing to try first; (2) teach tools/gen_lds.py to send named objects' unreferenced .rodata to /DISCARD/ -- provably safe for these two because the ROM's first 8MB already reproduce without them, but it is a build-system change and a wave should not make it unilaterally; (3) check whether a different optimisation level (-Os was not tried) gives the same .text without the dead constants. NOTE the -O1 finding itself is SOUND and general -- see the chapter 'OPTIMISATION LEVEL is a third per-file axis' in docs/agbcc-codegen.md -- it is only the integration that is open. Do not re-derive the flag; it is not the question.",
   "reason": "Wave 47: matched at -O1, backed out at integration because the dead .rodata adds 28 bytes past the end of the ROM. The decompilation is DONE; the build integration is not."
  },
  "sub_080607E8": {
   "bytes": 172,
   "best": "164 bytes (-8), 32.0% identical (positional -- the candidate is 8 bytes short, so every byte after +0xa shifts)",
   "remaining_diff": "TWO SPOTS, one cause. Everything else is byte-exact: the prologue, the sub_0803E354 guard, the a/b setup, the whole gUnknown_08499590 row+plane address chain, both (s16) conversions, the sub_08025CC8 and sub_08061178 calls, the unk0a/unk0b stores, the loop bottom and the tail. (1) The ROM recomputes `band * 3` INSIDE the loop -- `mov r3, r9; lsls r0, r3, #1; add r0, r9; adds r0, r0, r7; adds r1, r1, r0` with `band` itself living in sb/r9 -- while agbcc LICM-hoists `band * 3` into the preheader and parks THAT in r9, leaving `band` in the low r4. That is where the 8 bytes go, and it also costs the ROM's mask shuffle `movs r1,#0x1f; mov r9,r1; mov r2,r9; ands r2,r0; mov r9,r2`, which collapses to `movs r4,#0x1f; ands r4,r0` when band is low. (2) Knock-on: with band dead after the hoist, r4 is free for `u` and the known-zero `c` in r6 is still live, so cse substitutes r6 for the literal 0 of `unk09 = 0` where the ROM materialises `movs r0, #0`. Spot (2) cannot be attacked on its own -- it disappears the moment (1) does. Relocations already match; there is nothing extra to place at promotion.",
   "why_it_is_close": "The shape is fully derived and exactly ONE compiler decision is wrong. The lever is known and half-demonstrated: casting the table to a 3-byte-row STRUCT (`((struct Row3 *)base)[band].v[i]`) stops the hoist dead and reproduces the ROM's sb allocation AND its mask shuffle exactly, first try -- but agbcc pads `struct { u8 v[3]; }` to 4, so the stride comes out `lsls #2` and the association becomes `(base + band * 4) + i` instead of the ROM's `base + (band * 3 + i)`. What that measures is the underlying rule: agbcc's LICM hoists a TWO-instruction invariant (the *3 synthesis, lsl + add) but leaves a ONE-instruction one (the *4 shift) in the loop. So the job is a spelling that keeps a 3-byte stride while making the multiply part of an addressing tree rather than a standalone MULT_EXPR in the index -- or a way to get agbcc to emit the *3 as one instruction.",
   "ruled_out": [
    "SEVEN index spellings, all reassociating to the identical tree and ALL hoisting: `[band * 3 + i]`, `[i + band * 3]`, `[band * 2 + band + i]`, `[(band << 1) + band + i]`, `*(base + band * 3 + i)`, `((u8 (*)[3])base)[band][i]`, and `((struct T *)base)->rows[band][i]` (the struct-wrapped-array lever from the .rodata chapter). The struct wrapper only flipped the operand order of one `add`.",
    "u8 / int for band (u8 band also breaks the `ldrh` of gUnknown_03004080 down to a `ldrb`), for c, and for d -- d must stay u8, because the third sub_08025CC8 argument is a bare `adds r2, r5, #0` with no sign extension, which an int would need.",
    "the `continue` form against the nested-if form; `0x1f & g` against `g & 0x1f`; merging the two guards into one `&&`.",
    "Register pressure as the cause: the ROM and every candidate use exactly seven callee-saved registers and the identical push list, so the hoist is not agbcc running out of registers."
   ],
   "notes": "The gUnknown_08499590 access is settled and must not be re-litigated: the file-local `struct Map7E8` cast reproduces the ROM's `adds r0,r1,r3(=0x417A); adds r0,r0,r2` and `adds r1,#0x12; adds r1,r1,r0` associations exactly, per the wave-34 rule in include/unknown-globals.h. Byte arithmetic on the u8 * does not."
  },
  "sub_0808B5B8": {
   "bytes": 192,
   "best": "192 bytes SIZE-EXACT, 40.1% identical (positional -- one block moved, so most later bytes shift). NEEDS -O1; the entry is in data/compiler-overrides.json and is flagged as not yet evidence-grade because this function does not match under it.",
   "remaining_diff": "ONE block placement, in the OUTER loop, and nothing else. Every instruction is present and correct: the prologue including `mov r7,r8; push {r7}` and `sub sp,#0x40`, the 0x0f guard and its 0x80FF pool word, the sub_0808AD6C call on the 64-byte local, BOTH REG_WAITCNT read-modify-writes with the `ands` tied to the LOADED value's register (the -O1 signature), `lsls r0,r4,#0x15; lsrs r5,r0,#0x10`, the gUnknown_08485678 +0x18 seed store, `adds r0,r1,#0; mov r8,r0`, the whole retry loop, the step block, and the epilogue. The difference: the ROM enters the outer loop with `b _0808B624` INTO the middle, so the back-edge target is the STEP block at _0808B610 and the counter test is reached only by that entry jump; the candidate has a COPY of the counter test hoisted in front of the loop as a guard, the head (`movs r4,#2`) as the back-edge target, and the counter test again at the bottom. agbcc's exit-test duplication (guard + do/while) against the ROM's un-duplicated `jump to the test`. Both are 192 bytes; there is no instruction to add or remove. Relocations: the ROM's second gUnknown_084856A4 pool word is present and correctly placed, so nothing extra is needed at promotion.",
   "why_it_is_close": "The lever is KNOWN and already demonstrated on the other loop in this same function. The INNER retry loop had the identical defect and a `goto` into the loop body -- which puts a CODE_LABEL inside the loop and blocks agbcc from copying the exit test to the front -- fixed it exactly, taking this function from -12 bytes / 31.8% to SIZE-EXACT / 40.1% in one attempt. The outer loop needs the same suppression and the same lever does not reach it: spelling the outer loop with a goto into its body relocates the step block instead of stopping the copy. So the question is narrow and answerable: what ELSE blocks agbcc's exit-test duplication for a loop whose body already contains a label and a call.",
   "ruled_out": [
    "old_agbcc, at -O1 and with -fprologue-bugfix removed: STRICTLY WORSE, 31.2% and -12 bytes, and it adds a spurious `mov r3,r8` in the step block. The compiler binary is not the axis here (consistent with W47-F's note that the flash trio matches under -O1 with or without it).",
    "-O2, i.e. no override at all: wrong on both REG_WAITCNT read-modify-writes -- the `ands` ties to the mask's register instead of the loaded value's, which is W47-F's documented -O2 invariant and was probed twenty ways in wave 47. -O1 is right and is not in question; it is just not sufficient.",
    "`while (gUnknown_03005C7C != 0) {...}` with the step at the end of the body -- the natural spelling. -12 bytes / 31.8%, both loops duplicated.",
    "`for (; gUnknown_03005C7C != 0; step...)` with the step as a comma expression in the third clause: byte-identical output to the `while`, to the instruction. A for-update clause and a statement at the end of the body are the same RTL.",
    "`goto test;` into a `for(;;)` whose body starts with the step and has the counter test after the label: the step block moves to AFTER the inner loop and the counter test to the bottom, r8 is abandoned entirely and the high-register push disappears. 184 bytes (-8), 10.4%.",
    "The same goto entry with the loop spelled `do { step; test: ...; inner } while (result == 0)`, matching the ROM's conditional back edge: byte-for-byte identical output to the previous one -- agbcc normalises the two.",
    "volatile on gUnknown_03005C7C, which WOULD block the duplication: refuted by the ROM itself, not by a probe. Per W47-E's chapter a volatile narrow store emits a dead load in front of it, and the ROM's seed store `ldr r0,[r0,#0x18]; strh r0,[r1]` has no such load. Do not add it -- it would also change the already-matched sub_0808B31C, which reads the same global."
   ],
   "notes": "The two globals are settled and typed in include/unknown-globals.h: gUnknown_08485678 +0x18 is a 32-bit SLOT size (seeded into the counter) and gUnknown_084856A4 +0x18 is the 32-bit SECTOR size (subtracted from it each iteration) -- the same offset in two different records 0x2C apart, not one record read twice. Both are reached through a local `const u8 *`, one local per site: array-subscript spellings and pointer arithmetic on the address constant both fold the offset into the pool word, and a single shared local would give one long-lived pseudo where the ROM rematerialises the address per statement. sub_0808B540, the callee, MATCHED this wave."
  },
  "sub_0808B540": {
   "bytes": 120,
   "best": "120 bytes, MATCH by exit status",
   "remaining_diff": "NONE. The compiled .text is byte-for-byte identical to the original. MATCHES PER-FUNCTION BUT CANNOT BE PROMOTED -- a state this file has not carried before, so read the whole entry before treating it as an ordinary near-miss. `tools/verify_batch.py` reports OK by exit status and `tools/trymatch.py` prints 'MATCH -- byte-for-byte identical to the original'. The blocker is at INTEGRATION. Wave 47 (W47-F) established that the flash library is built at -O1 rather than -O2 and these functions match on that flag alone with no source change. But at -O1 agbcc ALSO emits dead `.rodata` address constants -- `.section .rodata / .LC0: .word gUnknown_084856A4` -- ALONGSIDE the same words in the function's own inline literal pool, and it never eliminates them. Confirmed by reading build/src/decomp/c_0808B430.s: lines 126/130, 200 and 384/388 are the real inline pool words the ROM has, and .LC0-.LC4 are unreferenced duplicates. Removing -fforce-addr does NOT suppress them (tested: the .rodata survives a forced recompile with the flag filtered out). THE MEASURED CONSEQUENCE: the SPLIT build links a ROM whose first 8,388,608 bytes are BYTE-IDENTICAL to baserom.gba and which is 28 BYTES LONGER -- seven unplaced .rodata words (2 from c_0808B31C.o, 5 from c_0808B430.o) swept past the end of the image by the linker script's catch-all. There is no harmless address to place dead words at in a fixed-layout ROM, and data/promoted.json's `rodata` mechanism cannot help because the five words in c_0808B430.o would need to land at five NON-CONTIGUOUS addresses (0x0808B4A0, 0x0808B4B0, 0x0808B4F0, 0x0808B5A4, 0x0808B5B4) from one section. Wave 47 backed all four out rather than weaken the both-builds-reproduce-the-SHA1 rule, and removed their entries from data/compiler-overrides.json so the build does not carry rules for objects that no longer exist.",
   "why_it_is_close": "Flash library, the byte-copy twin of sub_0808B430.",
   "axes_ruled_out": "-O2 (the default) leaves a 4-9 byte register-assignment residual; twenty source spellings were measured against that residual across W47-D and W47-F and every one failed, plus a 38,000-iteration permuter run. -fforce-addr removal does not suppress the dead .rodata. old_agbcc is NOT the axis here -- wave 46 measured five configurations on the neighbouring 0x0808A block and found it byte-identical on two functions and strictly worse on a third.",
   "settled_by_this_attempt": "WHAT WOULD UNBLOCK THIS, in increasing order of invasiveness: (1) find a source spelling that does not make agbcc materialise the dead address constant at -O1 -- untested, and the cheapest thing to try first; (2) teach tools/gen_lds.py to send named objects' unreferenced .rodata to /DISCARD/ -- provably safe for these two because the ROM's first 8MB already reproduce without them, but it is a build-system change and a wave should not make it unilaterally; (3) check whether a different optimisation level (-Os was not tried) gives the same .text without the dead constants. NOTE the -O1 finding itself is SOUND and general -- see the chapter 'OPTIMISATION LEVEL is a third per-file axis' in docs/agbcc-codegen.md -- it is only the integration that is open. Do not re-derive the flag; it is not the question.",
   "reason": "Wave 47: matched at -O1, backed out at integration because the dead .rodata adds 28 bytes past the end of the ROM. The decompilation is DONE; the build integration is not."
  }
 }
}