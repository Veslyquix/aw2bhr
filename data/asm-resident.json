{
 "_comment": [
  "Functions that will never be matched by writing C, because they were not",
  "produced by a C compiler. They are excluded from the work queue: without",
  "this the scheduler keeps offering them, and they look like ideal targets",
  "-- tiny, high fan-in, adjacent to easy matches. A wave-5 agent spent its",
  "budget proving out this list, which is the only reason it exists.",
  "",
  "Adding to this list is a claim that needs evidence in the reason field.",
  "'I could not match it' is not evidence; the tells are in the",
  "'not compiler output' section of docs/agbcc-codegen.md.",
  "",
  "Cheap pre-filter when scoping a new batch:",
  "  grep -n 'arm_func_start\\|non_word_aligned_thumb_func_start' asm/<file>.s",
  "",
  "The strongest tell is the CALLING CONVENTION, but it is corroborating",
  "evidence and not proof, and the paragraph near the end of this comment",
  "explains why: a dead leading parameter is invisible in the body, so from",
  "the assembly alone you CANNOT distinguish 'r0 was skipped' from 'r0 held",
  "an argument nothing reads'. Both look identical. Any entry resting on the",
  "parameter convention alone is therefore weaker than it reads.",
  "",
  "Two things are still decisive on their own. A value RETURNED in a register",
  "other than r0 is one -- dead arguments cannot explain a return register, so",
  "sub_0807031C stands unaided. Two callable entry points inside one",
  "instruction stream is the other.",
  "",
  "What actually carries the nine ply_* handlers below is not that they take",
  "the track pointer in r1. It is that register allocation DIFFERS across",
  "structurally identical bodies within the block (sub_08071840 uses",
  "r0-for-address / r2-for-byte and sub_08071868 the reverse), which one C",
  "source shape cannot produce, plus their entry from a shared jump table.",
  "Cite that, not the register, if you ever need to defend the block.",
  "",
  "The m4a/MP2K driver spans roughly 0x0806F734-0x080718E4, with the linker",
  "veneers immediately after. This comment used to say 'treat that whole span",
  "as lift-from-another-decomp rather than re-derive'. That blanket claim is",
  "WRONG and wave 8 disproved it: MPlayFadeOut (sub_080703D4) and",
  "m4aMPlayFadeOutPause (sub_08070620) both matched as ordinary C. The span is",
  "m4a.c (compiler output, matchable) interleaved with m4a_asm.s (hand-written,",
  "never matchable) -- the same split every other GBA decomp has. Inside it:",
  "a function the FE signature matcher has NAMED is almost certainly the m4a.c",
  "side and is among the cheapest functions available, needing only that public",
  "source be reconciled with this ROM's struct offsets; a function taking its",
  "context in r1 is the m4a_asm.s side and belongs in this file.",
  "",
  "Those two halves of the rule are not equally strong, and wave 9 found the",
  "case where they collide: an FE NAME is not evidence of the m4a.c side, and",
  "the calling convention still overrules it. sub_08070300 is FE-named",
  "clear_modM and is hand-written; its compiled twin sits at sub_08071564 under",
  "the FE name MP_clear_modM. The driver keeps BOTH copies of some routines, so",
  "read the name as 'which routine is this', never as 'which half is it in'.",
  "Check r0 first, every time.",
  "",
  "Wave 10 measured how often that collides, on a batch of four FE-named",
  "functions inside the span picked without regard to convention. THREE of the",
  "four were hand-written -- sub_0806FC34 (ply_patt), sub_08070090",
  "(ChnVolSetAsm) and sub_080717F8 (ply_xwave) -- and one, sub_08070AF8",
  "(SoundVSyncOn_rev01), was ordinary C that matched first try. So an FE name",
  "inside this span is not weak evidence of the m4a.c side, it is no evidence",
  "at all; treat the name as free documentation of what the routine DOES and",
  "settle the side from the registers. Two of the three announce themselves if",
  "you read the name properly: ChnVolSetAsm says 'Asm' outright, and ply_* is",
  "the jump-table handler family of which nine were already listed here.",
  "",
  "Two tells beyond the calling convention showed up in that batch and are",
  "worth adding to the list in docs/agbcc-codegen.md. A body that READS a",
  "callee-saved register it never writes is taking an argument there:",
  "sub_08070090 opens `ldrb r1,[r4,#0x12]` with no push at all, and",
  "sub_080717F8 does `push {r4, lr}` and then `ands r4, r0` before ever",
  "setting r4, so the pushed register is simultaneously an inbound operand.",
  "And control flow that LEAVES the function -- sub_0806FC34 ends in two `b`s",
  "to labels inside two other functions, and is itself the target of a `b` from",
  "a jump-table stub. Neither is something a compiler emits, and both are",
  "readable without knowing what the routine computes.",
  "",
  "The calling-convention tell is about the FIRST parameter, and reading it as",
  "'a context pointer in a register above r0' is too strong. sub_08020680 takes",
  "its byte-stream pointer in r2 and never reads r0 or r1, which looks like the",
  "ply_* shape and is not: it is void f(int, int, u8 *) with two dead leading",
  "parameters, and it matched byte-for-byte. Dead arguments are ordinary C. The",
  "tell only fires when the first LIVE parameter arrives somewhere r0 could have",
  "held it -- i.e. when r0 is skipped with nothing occupying it.",
  "",
  "The span's lower bound is real but tight. sub_0806F710, twenty bytes below",
  "0x0806F734, is an ordinary Proc_StartBlocking wrapper: it takes its parent",
  "in r0 like every other member of that family and matched byte-for-byte in",
  "wave 9. Proximity to the driver is not evidence -- check the calling",
  "convention before assuming a neighbour is resident.",
  "",
  "Wave 18 (W18-C) added two tells and REFUTED a third. The decisive new one is",
  "`mov ip, lr` ... `bx ip`: agbcc returns from a THUMB function that calls",
  "anything by pushing lr and popping it into a LOW register, and across the 959",
  "promoted units it writes ip exactly ten times, always `mov ip, rLOW` holding a",
  "value under register pressure. There is no `mov ip, lr` and no `bx ip` in any",
  "agbcc output. asm/ has exactly 15 of each, they pair 1:1, and all 15 are m4a",
  "track-command handlers -- 13 in 0x0806FBD4-0x0806FD97 and 2 at 0x08070328 /",
  "0x0807033C. The weaker new one is THUMB `tst`, which agbcc has no pattern for:",
  "zero occurrences in 959 promoted units, eight probed spellings of",
  "AND-test-discard all emitting `and` + `cmp #0`, and 31 of the 37 occurrences in",
  "asm/ inside the m4a span. Treat `tst` as corroborating, never as sufficient.",
  "",
  "The REFUTED one matters more than either. `bl` to a mid-function local label is",
  "NOT a tell: of the 19 such sites in the tree, 11 are agbcc's own long-branch",
  "expansion inside ONE function (THUMB `b` reaches +-2 KB, `bl` reaches +-4 MB,",
  "and lr is already on the stack, so a goto past 2 KB becomes a `bl`), and 4",
  "target labels that are real function entries the SPLITTER merged into the unit",
  "above -- sub_08079B04 is two bytes of `b _08079B38` in front of the PREVIOUS",
  "function's literal pool, nothing ever calls 0x08079B04, and the real function",
  "starts at _08079B38. Only 4 sites are genuine second entry points, all of them",
  "`bl _0806FBEE`. Resolve the target against data/functions.json before reading",
  "anything into it, and check whether the label is preceded by `.4byte` words."
 ],
 "functions": {
  "sub_08000234": "Wave 47 (orchestrator). ARM code, `arm_func_start sub_08000234` -- agbcc compiles this tree entirely as THUMB, the same argument that carries the six ARM veneer halves below and the m4a ARM mixer. It is one of the SIX ARM ROUTINES THE LINKER VENEERS POINT AT: sub_080718EC branches here, and the other five veneers target sub_08000304, sub_0800043C, sub_080003E0, sub_08000540 and sub_08000554. Wave 18 recorded all six THUMB veneer halves, wave 33 recorded all six ARM halves, and nobody recorded the ARM TARGETS -- so the veneer chain was documented at both ends and left open in the middle. Found by sweeping asm/*.s for THUMB `tst`, which turned up five ARM functions in this block; the `tst` is incidental (in ARM it is an ordinary conditional-test mnemonic with no agbcc implication), the ARM mode is the evidence.",
  "sub_080003E0": "Wave 47 (orchestrator). ARM code, `arm_func_start sub_080003E0`; target of the linker veneer pair sub_08071900 / sub_08071904, both already in this file. See sub_08000234.",
  "sub_08000494": "Wave 47 (orchestrator). ARM code, `arm_func_start sub_08000494`, in the same ARM block as the five veneer targets around it. See sub_08000234.",
  "sub_08000540": "Wave 47 (orchestrator). ARM code, `arm_func_start sub_08000540`; target of the linker veneer pair sub_08071908 / sub_0807190C, both already in this file. See sub_08000234.",
  "sub_08000554": "Wave 47 (orchestrator). ARM code, `arm_func_start sub_08000554`; target of the linker veneer pair sub_08071910 / sub_08071914, both already in this file. See sub_08000234.",
  "sub_0802C62A": "Not code at all -- inter-function alignment padding. sub_0802C604 ends `bx r1` at 0x0802C628, and the two bytes at 0x0802C62A are the 0x0000 the assembler emits for `.align 2, 0` ahead of the 4-aligned sub_0802C62C. `movs r0, r0` is simply how 0x0000 disassembles. Zero callers. WAVE 24 (W24-A): these two bytes are now BLOCKING, not merely inert. work/sub_0802C604/sub_0802C604.c compiles to all 38 bytes of sub_0802C604 with `0 of 38 differ` and `relocs: match`, and agbcc's own `.text` tail supplies this same 0x0000 pad -- verified against baserom.gba at 0x0802C62A -- so the draft reproduces 0x0802C604..0x0802C62B, all forty bytes. trymatch still reports +2 because it compares the whole `.text` section against a 38-byte window. sub_0802C604 is therefore parked on the PIPELINE and not on the decompilation; promoting it must also drop this stub from asm/, or the two bytes are emitted twice. No matched function in the tree has size % 4 == 2, so promote.py has never had to do this. See the wave-24 chapter in docs/agbcc-codegen.md. WAVE 28: PROVEN, and the entry must now STAY for a mechanical reason. tools/trymatch.py sub_0802C604 --unit compiles the unit's C alone and reproduces all 40 bytes of its .text -- 0x0802C604..0x0802C62B, these two padding bytes included -- with relocations matching, so the compiler demonstrably emits them and this is not hand-written assembly. sub_0802C604 is promoted as of wave 28 and both builds are green. DO NOT DELETE THIS ENTRY: tools/split_asm.py reads asm-resident.json to know the member is CARRIED by the promoted unit rather than left behind as assembly. Without it the unit trips 'mixes promoted and unpromoted functions' and the promotion reverts. The entry is documentation of padding plus a load-bearing flag, not a claim of hand-written asm -- read it that way when using this file as the evidence base for what hand-written assembly looks like.",
  "sub_08063614": "Wave 44 (W44-A). A cycle-counting busy-wait, hand-written. TWO independent tells, neither of them 'I could not match it'. (1) It READS THE PROGRAM COUNTER: `mov r2, pc; lsrs r2, r2, #0x18` takes the top byte of PC to find out which memory region it is executing from, then picks the per-iteration cycle cost from it -- 0xc for 0x02xxxxxx (EWRAM), 0xd for 0x08xxxxxx (ROM), 4 otherwise (IWRAM). agbcc has no construct that materialises PC in a general register; the argument is therefore a delay in CYCLES and the divisor is the cost of one pass round the loop on whichever bus the code was copied to. (2) The loop is TWO instructions, `subs r0, r0, r1; bgt`, feeding the conditional branch from the subtract's own flags with no `cmp`. Measured with compile_probe in wave 44: both `while (a > 0) a -= s;` and `do { a -= s; } while (a > 0);` compile to `sub r0, r0, r1; cmp r0, #0; bgt`, i.e. agbcc ALWAYS re-emits the compare and never reuses the flags a `sub` already set. There is no C spelling that drops it. Its one caller sub_0806362C is matched (wave 44) and calls it with the constant 0x258; it is declared `void sub_08063614(int)` in include/unknown-functions.h purely so that caller can name it. ORCHESTRATOR SCAN (wave 44), independent of both tells above and it CLOSES THE CLASS: scanning every thumb_func_start body in asm/*.s for a `mov rN, pc` used as a data value returns EXACTLY ONE function ROM-wide -- this one -- and no MATCHED function contains such a read. So there is no counter-example in the tree, and no second instance is waiting to be found. Compare the leading-nop class in docs/agbcc-codegen.md, which is closed the same way at three members.",
  "sub_0806F734": "Hand-rolled mode switch (`add r2, pc, #0; bx r2`) at the head of the m4a/MP2K sound driver. Hand-written, not linker-generated.",
  "sub_0806F744": "m4a/MP2K SoundMain, hand-written assembly. FE-named SoundMain at `full` strength, and per the wave-10 note above the name is not the evidence -- three things in the body are, none of them a calling convention. (1) It RETURNS BEFORE ITS PROLOGUE: the first five instructions load SOUND_INFO_PTR, compare [ptr] against the 0x68736D53 ident and `bx lr` on mismatch, and only then does `push {r4, r5, r6, r7, lr}` plus the four high registers appear. agbcc emits the frame first, always. (2) It never reaches an epilogue -- the tail is `ldr r3, =gUnknown_03000FB1; bx r3`, an indirect branch to a RAM address with the frame still up and 0x18 bytes of locals still allocated, i.e. it jumps into the mixer that was relocated into IWRAM and lets THAT return. A compiler emits `bl`+epilogue, not a branch away with a live frame. (3) It reloads r0 with `ldr r0, [sp, #0x18]` after each call -- a stack slot inside its own register-save area, because r0 was pushed as part of `push {r0, r1, r2, r3, r4}` alongside the transferred high registers. It also `bl`s sub_0806FB66, already recorded here as `a bx r3 tail plus a literal pool` rather than a function. Its only caller is sub_0807046C, which is m4aSoundMain -- the ordinary C forwarder beside it, matched in wave 12 -- so this is the m4a.c / m4a_asm.s split again and not a hard function. Declared `void sub_0806F744(void)` in include/unknown-functions.h purely so that forwarder can call it.",
  "sub_0806F7C8": "Wave 34 (W34-E). The m4a/MP2K driver's THUMB->ARM entry stub, and either half of it is decisive on its own. (1) `beq sub_0806F828` is a CONDITIONAL BRANCH INTO ANOTHER FUNCTION's entry point; agbcc has no THUMB tail-call optimisation and reaches another function only through `bl`. There is no push, no pop and no `bx lr` in the twelve bytes -- the stream never returns, it only dispatches. (2) `add r1, pc, #0x4; bx r1` targets 0x0806F7D4 with bit 0 CLEAR, i.e. a switch to ARM state, and asm/code-0806CFC8.s:4952 declares sub_0806F7D4 with `arm_func_start`. agbcc compiles this tree entirely as THUMB and never emits a state change. Do NOT confuse this with `bl _call_via_rN`, which the brief correctly calls ordinary compiler output: the discriminator here is the ARM-mode target plus the cross-function conditional branch, not the `bx` itself. Sits inside the m4a.c / m4a_asm.s split this file's header describes for 0x0806F734-0x080718E4, and scores as an ideal cheap target on the address-locality screen -- 12 bytes, zero calls, one data_ref -- which is why it needs recording.",
  "sub_0806F7D4": "Wave 47 (orchestrator). ARM code, declared `arm_func_start sub_0806F7D4` at asm/code-0806CFC8.s:4952. agbcc compiles this tree entirely as THUMB and never emits an ARM instruction, so no C source reaches this at any type model -- the same argument that already carries sub_0806F9A4, sub_0806F9A8 and the six ARM veneer halves at 0x080718EC-0x08071914. Its only entry is sub_0806F7C8's `add r1, pc, #0x4; bx r1` with bit 0 clear, i.e. the deliberate THUMB->ARM state change already recorded on that function; the body opens `cmp r4, #2` reading r4, r5, r6 and r8 live with no prologue, and never returns -- it falls into the mixer's unrolled copy at sub_0806F828 and thence into sub_0806F856, both continuations rather than functions. It is the SoundMainRAM half of the m4a.c / m4a_asm.s split this file's header describes. Recorded because the ARM units do not appear in the THUMB address-locality screen but DO sit in the work queue indefinitely -- the same gap the header notes for the veneers, where all six THUMB halves were recorded in wave 18 and all six ARM halves were missed.",
  "sub_0806F828": "Wave 47 (orchestrator). Continuation block inside the m4a mixer, not a function, and it is the one member of this hand-written run that LEAKS INTO THE THUMB SCREEN -- 46 bytes, one loop, zero calls, zero data_refs, which scores as an ideal cheap target on tools/overlap_screen.py's address-locality listing. It was offered as a wave-47 candidate, which is why it needed recording. Three tells, none of them 'I could not match it'. (1) NO PROLOGUE and it reads registers it never writes: the second instruction is `mov r1, r8` and it uses r5 and r6 as live pointers from the first, so r5/r6/r8 are inbound state from sub_0806F7D4's ARM code above. (2) Its only entry is `beq sub_0806F828` from sub_0806F7C8 -- a CONDITIONAL BRANCH INTO another function's entry point, the tell already recorded on sub_0806F7C8 itself, and agbcc reaches another function only through `bl`. (3) It never returns: the stream ends `subs r1, #1; bgt _0806F842` and falls straight through into `non_word_aligned_thumb_func_start sub_0806F856`, already in this file for that exact reason. Body is the zero-fill of the two mixer buffers, `stm r5!, {r0}` / `stm r6!, {r0}` unrolled eight deep with a Duff's-device style entry ladder (`lsrs r1, r1, #3; blo` then `lsrs r1, r1, #1; blo`) that jumps into the middle of the unrolled run -- a computed entry into a loop body, which no C source expresses.",
  "sub_0806F856": "Continuation entry point inside the m4a mixer's unrolled `stm` copy loop -- the preceding instruction stream ends `subs r1,#1; bgt _0806F842` and falls straight through into it, so there is no prologue and no callable boundary here. It also opens `ldr r4,[sp,#0x18]`, reading a stack slot in a frame it never set up. Non-word-aligned, which is the giveaway: agbcc never emits a function at an odd address. Found in wave 11 while checking the odd-address tell; it sits between sub_0806F734 and sub_0806F9A4, both already recorded here.",
  "sub_0806F960": "Wave 47 (orchestrator). ARM code, declared `arm_func_start sub_0806F960` at asm/code-0806CFC8.s:5154 -- see sub_0806F7D4 for why ARM mode is decisive on its own in a tree agbcc compiles entirely as THUMB. Corroborated twice without appealing to the mode: it opens `str r8, [sp]` on a frame it never established, and it is entered by `add r0, pc, #0x4; bx r0` from the tail of sub_0806F856, which is itself already in this file as a continuation rather than a function. Body uses sl and fp as working registers (`ldrb sl, [r4, #0xa]`, `ldrb fp, [r4, #0xb]`) alongside r8, i.e. four high registers live at once with no save sequence anywhere. It is the ARM inner mixer of SoundMainRAM.",
  "sub_0806F9A4": "ARM code cut out as a THUMB unit. It sits between ARM instructions using r8/lr and `arm_func_start sub_0806F9AC`; the ARM stream above branches straight over it with `bgt sub_0806F9AC`. Its two halfwords are one ARM word. Note the usual grep does NOT catch this one -- it is a word-aligned `thumb_func_start`, not `non_word_aligned`; what gives it away is an `arm_func_start` immediately below and a body that reads registers it never sets and never returns.",
  "sub_0806F9A8": "ARM code cut out as a THUMB unit, same as sub_0806F9A4 directly above it. Body is `movs r0, #4; lsls r0, r4, #0xe`, which reads r4 without setting it and has no `bx`. Reassembled as one ARM word, 0x03A02004 is `moveq r2, #4` -- what the m4a mixer actually executes.",
  "sub_0806F9AC": "Wave 47 (orchestrator). ARM code, declared `arm_func_start sub_0806F9AC` at asm/code-0806CFC8.s:5185 -- see sub_0806F7D4. At 412 bytes it is the largest unit of the hand-written m4a run and the main ARM mixer loop. sub_0806F9A4 and sub_0806F9A8 immediately above it are already in this file precisely BECAUSE the ARM stream branches over them with `bgt sub_0806F9AC`, so this function was already load-bearing evidence for two existing entries while sitting unrecorded itself. It also runs into sub_0806FB48, recorded in wave 33 as the last basic block of the routine it terminates -- that routine is this one, and the wave-33 entry's `b _0806F860` observation resolves into this ARM stream's caller. Recording it closes the 0x0806F7C8-0x0806FC0A run: every unit in that span is now either in this file or matched.",
  "sub_0806FB48": "Wave 33 (W33-A). NOT A FUNCTION -- it is the last basic block of the one before it, and three independent things say so. (1) No prologue: it opens `ldr r0, [sp, #4]` on a frame it never established. (2) It branches OUT of its own extent backwards, `b _0806F860`, into the middle of the routine that starts around 0x0806F7xx. (3) Its epilogue pops EIGHT registers and `add sp, #0x1c` that it never pushed or reserved -- `pop {r0-r7}; mov r8,r0; mov sb,r1; mov sl,r2; mov fp,r3; pop {r3}`. A C function cannot be written that emits a stack-restoring epilogue for a frame it did not create, so no spelling exists at any type model. It sits inside the m4a driver run that this file already records from sub_0806F734 to sub_0806FD80, sub_0806FB66 included, so the containing routine is hand-written like its neighbours and this is a splitter boundary landing on a label the disassembler could not tell from an entry point. Recorded so the widened address-locality screen stops offering it: it is 30 bytes with zero data_refs and zero calls and therefore scores as an ideal cheap target.",
  "sub_0806FB66": "m4a/MP2K sound driver. Not a function: a `bx r3` tail plus a literal pool, and the pool word is 0x68736D53 = \"Smsh\", the m4a ID_NUMBER that its apparent caller sub_0806F744 compares against. Same shape as sub_0807003C.",
  "sub_0806FB6C": "m4a/MP2K, hand-written assembly. It WRITES r4 -- a callee-saved register -- with no `push` anywhere in the function, saving and restoring it through ip (`mov ip, r4` ... `mov r4, ip`). agbcc's THUMB prologue saves callee-saved registers with `push` without exception; the ten uses of ip across the 959 promoted units are all VALUES under register pressure inside functions that also push r4-r7, never the save slot for a callee-saved register in a function with no frame at all. Body is a 64-byte zero fill -- four `stm r0!, {r1, r2, r3, r4}` with r1-r4 all zeroed -- which is why it needed a fourth register in the first place. Sits directly above the hand-written 0x0806FBD4-0x0806FD97 run and directly below sub_0806FB66, already recorded.",
  "sub_0806FB84": "m4a/MP2K. NOT MATCHABLE AS SPLIT: the 80-byte unit contains TWO complete routines. The first, 0x0806FB84-0x0806FBA3, is a doubly-linked-list unlink on r0 that ends `bx lr` at 0x0806FBA2 and looks like ordinary compiler output. The second begins at the label _0806FBA4 with its own `push {r4, r5, lr}` frame and its own `pop {r4, r5}; pop {r0}; bx r0`, and it is hand-written on three independent tells: it takes its pointer in r1 with r0 never read (the ply_* convention), it uses THUMB `tst`, which agbcc has no pattern for (zero occurrences in 959 promoted units; eight probed spellings of AND-test-discard all emit `and` + `cmp #0`), and it is entered by a bare `b` from sub_0806FC34, already in this file. No C source compiles to one 80-byte function with two frames, so the unit as split will never match. WHAT IS NOT SETTLED: whether the first 32 bytes on their own are compiler output. If the split is ever corrected to cut at 0x0806FBA4, re-test that half before trusting this entry. FE-named ClearChain, and per the header note that name is not evidence of which half of the file it came from.",
  "sub_0806FBD4": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Here the `bl` target is `_0806FBEE`, two bytes into sub_0806FBEC -- a genuine second entry point, already recorded. Note this is one of only FOUR `bl` sites in the whole tree that really are second-entry-point calls: of the 19 `bl _0XXXXXXX` sites, 11 are agbcc's long-branch expansion inside a single function (THUMB `b` reaches +-2 KB, `bl` reaches +-4 MB, and lr is already on the stack) and 4 target labels that are real function entries the splitter merged into the unit above. See docs/agbcc-codegen.md -- the `bl`-to-a-label tell is worth nothing until the target is resolved against the index. Body is a 0x24-iteration loop copying gUnknown_081B9D64's pointer table through sub_0806FBEC's sample-pointer sanity check, which is why lr has to survive a `bl` inside a loop and why ip is used at all.",
  "sub_0806FBEC": "m4a/MP2K sound driver, hand-written assembly. Three independent tells, none of which is a calling convention: (1) TWO callable entry points -- the body is `ldrb r3,[r2]` followed by the label _0806FBEE, and `bl _0806FBEE` appears four times in the file (once in sub_0806FCF8's predecessor and three times in a row at 0x0806FDxx) alongside `bl sub_0806FBEC`, so both halfwords are called; sub_0806FC0A also reaches _0806FBEE with a bare `b`. (2) It RETURNS IN r3 -- every caller does `bl _0806FBEE; str r3, [r1, #0xNN]` -- which no agbcc output does, the same tell already recorded for sub_0807031C. (3) It saves and restores r0 with an explicit `push {r0}` / `pop {r0}` INSIDE the body, which is not a frame agbcc builds. Body is the MP2K sample-pointer sanity check: it zeroes r3 unless r2 is below 0x02000000-ish (`lsrs r0,r2,#0x19`) or lies in the ROM window above gUnknown_081B9D64. Its r2 argument is NOT the evidence -- see the header note about sub_08020680.",
  "sub_0806FC08": "m4a/MP2K sound driver, hand-written assembly. It is a single `ldr r2, [r1, #0x40]` that falls through into non_word_aligned_thumb_func_start sub_0806FC0A, and BOTH are bl targets -- two callable entry points two bytes apart in one instruction stream. See also sub_0806F740, a raw `.hword 0xFF1E` the disassembler could not decode.",
  "sub_0806FC0A": "Second entry point into one instruction stream. sub_0806FC08 is a single `ldr r2,[r1,#0x40]` that falls through into this, and this then does `b _0806FBEE` -- a backward branch into sub_0806FBEC's body. Two callable entries plus a cross-function branch: no C produces this. Non-word-aligned. Its immediate neighbours sub_0806FC08 and sub_0806FC34 were already recorded; this one was missed, and would have been handed to an agent as a free 12-byte leaf.",
  "sub_0806FC34": "m4a/MP2K track-command handler, hand-written assembly. FE-named ply_patt, and the name is not what settles it. Takes the MusicPlayerTrack pointer in **r1** with r0 never read, exactly like sub_0806FC50 (ply_pend) and the ply_* block at 0x08071840. Corroborated twice over by control flow that leaves the function: it ends in `b _0806FC14` and `b _0806FBA4`, both labels inside OTHER functions (_0806FC14 is a `push {lr}` entry point in the middle of sub_0806FC0A, _0806FBA4 is a `push {r4, r5, lr}` entry inside the routine before sub_0806FBEC), and it is itself branched INTO -- sub_0806F73C is `movs r0, r0; b _0806FC48`, a jump-table stub landing on this function's second-to-last instruction. A compiler emits neither. Field offsets it confirms: patternLevel at +0x02 (`ldrb r2,[r1,#2]`, bounded `cmp #3; bhs`), cmdPtr at +0x40, patternStack[] at +0x44 -- the same three sub_0806FC50 gives.",
  "sub_0806FC50": "m4a/MP2K track-command handler, hand-written assembly. Takes the MusicPlayerTrack pointer in **r1**; r0 is never read, and agbcc passes the first parameter in r0 without exception. Same calling convention as the ply_* block at 0x08071840 and reached the same way. The body is ply_pend, and it names three MusicPlayerTrack fields at once: `ldrb r2,[r1,#2]` is patternLevel, `ldr r2,[r3,#0x44]` indexes patternStack[] after `lsls r2,#2`, and `str r2,[r1,#0x40]` is cmdPtr -- the same cmdPtr offset already recorded for sub_0806FC08 and the whole ply_* block. Its neighbour sub_0806FC64 (not split out separately) takes r1 the same way and falls into sub_0806FC16.",
  "sub_0806FC64": "m4a/MP2K track-command handler, hand-written assembly. FE-named ply_rept, and the name is not the evidence. Three things are. (1) CONTROL FLOW THAT LEAVES THE FUNCTION: it reaches `_0806FC16` twice with a bare `b`, and that label is inside sub_0806FC08's unit -- specifically inside the `push {lr}` entry at 0x0806FC14 -- both of which are already in this file. (2) It takes the MusicPlayerTrack in r1 with r0 never read, the ply_* convention. (3) It consumes sub_0806FC08's r3 return (`bl sub_0806FC08` then `cmp ip, r3`) and uses ip as the compare temporary ACROSS that call, which needs the callee to preserve ip -- the same contract the `mov ip, lr` handlers around it rely on, and one no compiler can assume. Sits in the middle of the 0x0806FBD4-0x0806FD97 run, every other unit of which is hand-written or already recorded.",
  "sub_0806FC94": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x1d. ",
  "sub_0806FCA0": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x1c. It also reads the MusicPlayerInfo in r0 alongside the track in r1, writing `b * 2` to +0x1c and `(b * 2) * [r0, #0x1e] >> 8` to +0x20 there -- two live context pointers at once, in r0 and r1.",
  "sub_0806FCB4": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x0a. then ORs 0xc into track->flags at +0x00.",
  "sub_0806FCC8": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. This one calls `_0806FBEE` -- two bytes into sub_0806FBEC, a genuine second entry point, already recorded -- THREE times in a row, consuming the r3 return before each next call. FE-named ply_voice: it indexes a 12-byte instrument record out of `[r0, #0x30]` by the command byte and runs all three of its words through the sample-pointer sanity check into track +0x24 / +0x28 / +0x2c. Three of the tree's four genuine `bl`-to-a-second-entry-point sites are in this function; see docs/agbcc-codegen.md for why the other 15 sites of that shape are ordinary compiler output.",
  "sub_0806FCF8": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x12. then ORs 3 into track->flags at +0x00.",
  "sub_0806FD0C": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x14. biased by -0x40 first, then ORs 3 into track->flags.",
  "sub_0806FD20": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x0e. biased by -0x40 first, then ORs 0xc into track->flags.",
  "sub_0806FD34": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x0f. then ORs 0xc into track->flags.",
  "sub_0806FD48": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x1b. ",
  "sub_0806FD54": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x18. guarded by a compare against the field's current value, and ORs 0xf into track->flags only when it changed.",
  "sub_0806FD6C": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Corroborated twice over: it takes the MusicPlayerTrack pointer in r1 with r0 dead, the ply_* convention already recorded for the 0x08071840 block, and it consumes a return in r3 from sub_0806FC08, which is itself in this file because returning outside r0 is something no agbcc output does. Body: fetch the next command byte and store it to track +0x0c. biased by -0x40 first, then ORs 0xc into track->flags.",
  "sub_0806FD80": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Its callee is sub_0806FC0A, the non-word-aligned SECOND ENTRY POINT of sub_0806FC08 and already in this file, and it consumes the same r3 return. It also computes a hardware register address (`0x04000060 + b`) BEFORE the call and keeps it in r0 across it, which an ordinary calling convention would clobber. Body writes the command byte straight into a sound-channel register in the 0x04000060 block.",
  "sub_0806FDE4": "Wave 47 (orchestrator). m4a/MP2K MPlayMain, hand-written assembly. TWO tells, either decisive on its own. (1) IT RETURNS BEFORE ITS PROLOGUE: the first five instructions are `ldr r2, =0x68736D53; ldr r3, [r0, #0x34]; cmp r2, r3; beq _0806FDEE; bx lr`, and only after that does `push {r0, lr}` appear. agbcc emits the frame first, always -- the same argument already recorded for sub_0806F744 (SoundMain), which is this routine's sibling. Note the push is `{r0, lr}`, an ARGUMENT register in the save list, which is not a frame agbcc builds either. (2) It contains EIGHT THUMB `tst` instructions; see sub_080702C0 for the wave-47 re-measurement of that tell across the full 3,754-function matched corpus. Its address is stored into SoundInfo by sub_08070B34 (MPlayOpen) as the driver's per-frame callback, so it needs a declaration in include/unknown-functions.h for that store to name it, and it is already reachable that way.",
  "sub_0807003C": "m4a/MP2K sound driver. Not a function at all: the `bx r3` tail of the routine above it plus a literal pool. The split drew a unit boundary mid-function.",
  "sub_0807004C": "Wave 47 (W47-A, closed by the orchestrator). m4a/MP2K MPlayStop's per-track half, hand-written assembly. W47-A drafted it and reached 14.7% with the FIRST DIFFERENCE AT +0x9 -- the high byte of the instruction at +0x8, which is `tst r0, r1`. That is the whole gap: the surrounding prologue and the linked-list walk are structurally right and the one instruction agbcc cannot emit sits four instructions in. Corroborated by the calling convention: it takes the MusicPlayerTrack in r1 (`adds r5, r1, #0` is the second instruction) and never reads r0. Per this file's header that convention is not sufficient alone -- a dead leading parameter looks identical -- but combined with a `tst` that eleven probed spellings cannot produce it settles the function. See sub_080702C0 for the measurement. NOTE: it is called by sub_08070C90 (matched wave 47), sub_08070BAC and sub_08070CD0, so it must keep its declaration `void sub_0807004C(struct MusicPlayerInfo *, struct MusicPlayerTrack *)` in include/unknown-functions.h -- do not delete that when reading this entry as 'not a function'.",
  "sub_08070090": "m4a/MP2K ChnVolSet, hand-written assembly -- the FE name ChnVolSetAsm says so and the body confirms it independently. It takes BOTH its operands in callee-saved registers it never writes: the first instruction is `ldrb r1, [r4, #0x12]` and it later reads `ldrb r0, [r5, #0x10]`, with r4 and r5 arriving live and r0-r3 all dead on entry. There is no `push`, so those are not saved values being restored -- they are inbound arguments. agbcc passes arguments in r0-r3 and treats r4-r7 as callee-saved, so no C source reaches this; it is a subroutine spliced into callers that have already set up the channel (r4) and track (r5) pointers, which is exactly what its two callers sub_0806FDE4 (MPlayMain) and sub_080700C0 (ply_note) do. Body is the stereo volume computation: `chan->rightVol = min(0xff, (env * (0x80 + pan) * track->volMR) >> 14)` and the mirrored `0x7f - pan` for the left, so it also pins three offsets -- +0x12 and +0x14 (SIGNED pan, `movs r0,#0x14; ldrsb r2,[r4,r0]`) on the channel, +0x10/+0x11 on the track, and the two results stored at channel +2/+3.",
  "sub_080700C0": "Wave 47 (orchestrator). m4a/MP2K ply_note, hand-written assembly. THE EVIDENCE IS THE `tst` COUNT AND LITTLE ELSE, so read the strength honestly: it has ELEVEN THUMB `tst` instructions, more than any other function in the tree, out of 37 ROM-wide. What makes that sufficient here rather than merely corroborating is the wave-47 re-measurement recorded under sub_080702C0 -- across all 3,754 MATCHED functions there is not one `tst`, and eleven probed spellings of AND-test-discard (eight in wave 18, three more in wave 47 covering the loop-invariant register-mask form) all emit `mov` + `and` + `cmp #0`. Eleven independent chances to emit the pattern agbcc has no rule for is not a marginal call. Unlike its neighbours it DOES carry an ordinary-looking agbcc prologue (`push {r4-r7}` plus the four high registers, `sub sp, #0x18`), so the frame is NOT a tell here and should not be cited as one. WHAT WOULD REFUTE THIS: a single matched function anywhere in the tree containing a THUMB `tst`, or one C spelling that produces one. Both are cheap to check and neither has ever succeeded. Its two callees sub_08070090 (ChnVolSetAsm) and sub_08070300 (the hand-written ClearModM) are both already in this file for reasons of their own.",
  "sub_080702C0": "Wave 47 (W47-A, closed by the orchestrator). m4a/MP2K track-command handler, hand-written assembly. THREE tells. (1) TWO THUMB `tst` instructions, and wave 47 re-measured that tell on the full corpus rather than trusting wave 18's figure: sweeping every func_start body in asm/*.s finds 37 `tst` in 13 functions, and cross-referencing data/functions.json shows ZERO of them are matched -- across 3,754 matched functions, up from the 959 wave 18 had. Three further spellings were probed in wave 47, chosen for THIS function's exact shape (a loop-invariant mask hoisted into a register, tested against a loaded byte, result discarded): `if ((f & 0x83) && !(f & 0x40))` with a u8 mask, with an int mask, and with explicit `!= 0` comparisons. All three emit `mov rX, #mask; and rX, rX, rY; cmp rX, #0`, never `tst`, and all three also hoist the mask exactly as the original does -- so the LICM is reproducible and only the instruction is not. Eleven spellings now, none successful. (2) It takes the MusicPlayerTrack in r1 and never reads r0. (3) It is the ply_* body shape this file records for sub_0806FC08, sub_0806FC34, sub_0806FC50 and the 0x08071840 block: `ldr r2, [r1, #0x40]` reads cmdPtr, the command byte is consumed and stored to a track field, and `str r2, [r1, #0x40]` advances it. W47-A's draft reached 12.5% with the first difference at +0x0 and the size 12 bytes over, because every `tst` costs the AND-test-discard form two extra instructions. THIS ENTRY CORRECTS A CLAIM ELSEWHERE IN THIS FILE: the sub_08070328 entry lists sub_080702C0, sub_0807004C and sub_0806FDE4 among 'ordinary compiled m4a.c routines in between'. That parenthetical was an aside about address order, not a per-function finding, and all three are hand-written -- the interleave is finer than it claimed.",
  "sub_08070300": "m4a/MP2K ClearModM, the hand-written copy. Takes the MusicPlayerTrack pointer in **r1**; r0 is never read. Both call sites confirm it directly: sub_080700C0 (ply_note) does `adds r1, r5, #0; bl sub_08070300` with r0 untouched, and sub_08070328 (ply_lfos) leaves r1 alone across `bl sub_0807031C` -- itself asm-resident -- and calls in with `mov ip, lr` standing in for the frame. The decisive corroboration is that the COMPILED copy of the same routine also exists, at sub_08071564: identical semantics, r0 convention, the two zero stores in the opposite order (0x1a then 0x16 rather than 0x16 then 0x1a), and the `orrs` destination on the constant rather than on the loaded flags. One C source cannot produce both, and only the r0 one is compiler output. This is the m4a.c / m4a_asm.s split, not a hard function.",
  "sub_0807031C": "m4a/MP2K sound driver, hand-written assembly. Returns its result in r3 rather than r0, which no agbcc output does.",
  "sub_08070328": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. Same shape as the 0x0806FC94-0x0806FD80 run but a hundred bytes further on, with other routines in between -- this driver interleaves at instruction granularity, so proximity is not the argument. WAVE 47 CORRECTION: this sentence used to name sub_0806FD98, sub_0806FDE4, sub_0807004C, sub_080700C0, sub_080702C0 and sub_08070350 as 'ordinary compiled m4a.c routines'. That was an aside about address order and it was wrong on four of the six -- sub_0806FDE4, sub_0807004C, sub_080700C0 and sub_080702C0 are all hand-written and now have entries of their own. Only sub_0806FD98 and sub_08070350 are compiler output. Do not read a list of neighbours in a comment as a per-function finding. Both of ITS callees are already in this file for reasons of their own: sub_0807031C returns in r3, and sub_08070300 (the hand-written ClearModM) takes the track in r1. Body stores the command byte to track +0x19 and calls ClearModM when it is zero.",
  "sub_0807033C": "m4a/MP2K track-command handler, hand-written assembly. It carries the `mov ip, lr` ... `bx ip` frameless-call idiom, which wave 18 established as DECISIVE ON ITS OWN: agbcc returns from a THUMB function that calls anything by pushing lr and popping it into a LOW register (`push {lr}` ... `pop {r0}; bx r0`), and across the 959 promoted units in build/src/decomp/ it writes ip exactly ten times, every one of them `mov ip, rLOW` holding a VALUE under register pressure and read straight back into a low register. There is no `mov ip, lr` and no `bx ip` in any agbcc output, and a probe over a leaf forwarder, a void forwarder, a self-recursive function and a ten-live-value body confirms the epilogue shape is not a pressure effect. asm/ has exactly 15 `mov ip, lr` and exactly 15 `bx ip`; they pair 1:1 and all 15 are in this driver. The idiom is only legal because the callee is also hand-written and known not to touch ip -- a contract no compiler can assume. The twin of sub_08070328 immediately above it: same two callees (sub_0807031C's r3 return and sub_08070300's r1 argument, both already in this file), storing to track +0x17 instead of +0x19.",
  "sub_080717F8": "m4a/MP2K track-command handler, hand-written assembly. FE-named ply_xwave, and it is the same ply_* family as the 0x08071840 block below rather than the m4a.c side the name might suggest. Two tells. It takes the MusicPlayerTrack pointer in **r1** (`ldr r2, [r1, #0x40]` first instruction, r0 never read) and it reads **r4 uninitialised**: after `push {r4, lr}` the very next use of r4 is `ands r4, r0`, so the incoming r4 is an operand, and the `pop {r4}` then throws the result away -- a register that is simultaneously an inbound argument and callee-saved is not something a compiler emits. Body assembles a 32-bit word out of four unaligned bytes at track->cmdPtr, one `ldrb`/shift/mask-and-or per byte with pool words 0xFFFFFF00 / 0xFFFF00FF / 0xFF00FFFF / 0x00FFFFFF, stores it to track +0x28 and advances cmdPtr by 4. The byte-at-a-time assembly with explicit clear masks is itself the m4a_asm.s idiom: agbcc would emit a single `ldr` for an aligned word and would not clear a field it is about to overwrite entirely.",
  "sub_08071840": "m4a/MP2K track-command handler (`ply_*`), hand-written assembly. Takes the MusicPlayerTrack pointer in **r1**, not r0 -- agbcc passes the first parameter in r0 without exception, so no C source compiles to this. Body reads one byte from track->cmdPtr at [r1, #0x40], stores it to a track field, and advances cmdPtr. Same cmdPtr shape already recorded for sub_0806FC08. Entered from a jump table, which is why the whole block shares r1. Corroborating: register allocation differs across structurally identical bodies in this block (sub_08071840 uses r0-for-address / r2-for-byte, sub_08071868 the reverse), which one C source shape cannot produce. NOT evidence, do not cite it: the 12-byte members omit a cmdPtr reload the others perform -- that is only the `strb` displacement limit of 31, since offsets >=0x20 must materialise the address into a register and clobber r0. Field offset here: 0x24.",
  "sub_08071854": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x2c.",
  "sub_08071868": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x2d.",
  "sub_0807187C": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x2e.",
  "sub_08071890": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x2f.",
  "sub_080718A4": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x1e (fits the strb displacement, hence 12 bytes rather than 20).",
  "sub_080718B0": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x1f (fits the strb displacement, hence 12 bytes rather than 20).",
  "sub_080718BC": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x26.",
  "sub_080718D0": "m4a/MP2K track-command handler, same block and same evidence as sub_08071840. Field offset 0x27.",
  "sub_0808BB30": "Wave 47 (orchestrator). NOT AGBCC OUTPUT -- prebuilt C-runtime code, the `__do_global_dtors` half of gcc's crtstuff. THE TELL IS THE RETURN: it ends `pop {r4, pc}`, a NON-INTERWORKING return. This tree is built with THUMB interworking and agbcc's epilogue is invariably `pop {rN}; pop {r0}; bx r0` -- popping lr into a LOW register and branching through it -- which is why the surrounding entries in this file quote that shape as the baseline. Wave 47 swept every func_start body in asm/*.s for a THUMB `pop` whose register list contains `pc`: FOUR functions ROM-wide, and ZERO of them are matched, against 3,754 matched functions at the time. All four cluster at 0x0808BB30-0x0808D680, which is the C runtime. Body confirms the identity: it walks a null-terminated array of function pointers at `*gUnknown_086170E4`, calling each through `_call_via_r0` and advancing the stored cursor by 4 as it goes, so the list pointer is CONSUMED -- destructors run once. Its address is passed to sub_0808D680 (atexit) by sub_0808BB58, which is the crtstuff registration handshake.",
  "sub_0808BB58": "Wave 47 (orchestrator). NOT AGBCC OUTPUT -- the `__do_global_ctors` half of gcc's crtstuff, and the twin of sub_0808BB30 above. Same decisive tell: it ends `pop {r4, r5, pc}`, a non-interworking return that agbcc does not emit under this tree's flags; see sub_0808BB30 for the ROM-wide sweep (4 functions, 0 matched). The body is the textbook `__do_global_ctors` and names the data for you: `gUnknown_086170EC` is `__CTOR_LIST__`, whose first word is the sentinel **-1** (`movs r0,#1; rsbs r0,r0,#0; cmp r1,r0`) and whose list is null-terminated -- the function counts the entries when the sentinel is present, then calls them in REVERSE order (`subs r4,#4; subs r5,#1`), which is the C++ static-initialisation contract. It finishes with `atexit(sub_0808BB30)` via sub_0808D680. Nothing in this tree's own source produces a ctor list; this came in with the toolchain.",
  "sub_0808BBA4": "Wave 47 (orchestrator). MOVED HERE FROM data/parked.json, where an earlier wave recorded it as compiler output that had not been matched yet. It is not compiler output. It ends in a THUMB `pop` whose register list contains `pc` -- a non-interworking return agbcc does not emit under this tree's flags -- and it is one of only four such functions ROM-wide, all four in the 0x0808BB30-0x0808D680 C-runtime cluster and none of them matched; see sub_0808BB30 for the sweep. A park entry claims a function is ordinary compiler output that simply has not been cracked, so leaving it there would keep offering it to future waves as a tractable near-miss. If the old park entry's diff is ever wanted it is in the git history of data/parked.json.",
  "sub_0808D680": "Wave 47 (orchestrator). NOT AGBCC OUTPUT -- this is newlib's `__register_exitproc`, i.e. `atexit`, and it arrived as a prebuilt library object. Two independent tells. (1) It ends `pop {r4, r5, r6, pc}`, the non-interworking return; see sub_0808BB30 for the ROM-wide sweep that finds four such functions and zero matched ones. (2) The body is newlib's reentrancy structure, spelled exactly: `gUnknown_086173EC` is the `_GLOBAL_REENT` pointer, `_atexit` sits at +0x148 (`movs r4,#0xa4; lsls r4,#1`) and the static `_atexit0` block at +0x14c, the per-block index is at +4 with a capacity of 32 (`cmp r0,#0x1f; ble`), and an overflow allocates a fresh 0x88-byte block through sub_08014E44 (malloc) and links it at the head. Returning -1 on allocation failure and 0 on success is `atexit`'s documented contract. No part of this tree's source declares a reent struct. Its only caller is sub_0808BB58 (__do_global_ctors), also in this file.",
  "sub_080718E8": "Linker THUMB->ARM interworking veneer: `bx pc; nop` plus an arm_func_start holding `b sub_08000304`. The 4-byte unit the index reports is only the THUMB half of an 8-byte veneer, cut at the mode change. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_080718EC": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_080718E8 -- a single `b sub_08000304`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_080718F0": "Linker THUMB->ARM interworking veneer, branches to sub_08000234.",
  "sub_080718F4": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_080718F0 -- a single `b sub_08000234`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_080718F8": "Linker THUMB->ARM interworking veneer, branches to sub_0800043C.",
  "sub_080718FC": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_080718F8 -- a single `b sub_0800043C`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_08071900": "Linker THUMB->ARM interworking veneer, branches to sub_080003E0.",
  "sub_08071904": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_08071900 -- a single `b sub_080003E0`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_08071908": "Linker THUMB->ARM interworking veneer, branches to sub_08000540.",
  "sub_0807190C": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_08071908 -- a single `b sub_08000540`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_08071910": "Linker THUMB->ARM interworking veneer, branches to sub_08000554.",
  "sub_08071914": "ARM half of the linker THUMB->ARM interworking veneer whose THUMB half is sub_08071910 -- a single `b sub_08000554`. The index splits the 8-byte veneer into two 4-byte units at the mode change, so both halves need an entry or the ARM one sits in the work queue forever. These are the only six veneers in the tree (swept for `bx pc` + `nop` immediately before an arm_func_start across all of asm/); all six THUMB halves were recorded in wave 18 and all six ARM halves were missed. The linker synthesises these when a THUMB bl targets an ARM symbol; no C construct emits one.",
  "sub_0807004C": "TrackStop, 68 bytes, in the m4a span. WAVE 47 (W47-A) entered it on its own evidence, which docs/agbcc-codegen.md had explicitly deferred ('they should be entered on their own evidence'). The C is NOT in doubt -- work/sub_0807004C/sub_0807004C.c reproduces the function instruction for instruction except at exactly three spots, and all three are places where agbcc provably cannot emit what the ROM holds. (1) `tst r0, r1` at +0x08. agbcc has no THUMB `tst`: zero occurrences across the 959 promoted units, eight spellings probed by an earlier wave and three more here (`if (track->flags & MPT_FLG_EXIST)` written direct, as `!= 0`, and behind an early `return`), every one emitting `and` + `cmp #0`. (2) `ands r0, r3` at +0x18, whose flags are consumed straight by the following `beq` AND whose VALUE is then the call argument already sitting in r0. agbcc puts the AND result in the mask's register and copies it out -- `movs r2, #7; ands r2, r0; cmp r2, #0; ...; adds r0, r2, #0` -- at all three spellings, including one that binds the masked value to its own local. (3) `bl sub_0807003C`, which is a bare `bx r3`, i.e. `_call_via_r3`, for an indirect call carrying ONE argument in r0. agbcc parks the pointer in the first free scratch register, so a one-argument indirect call is `_call_via_r1`, and that is what all three spellings produced. r3 is a scratch register, so the wave-46 carve-out (the arity rule holds only for r0-r3, not for a callee-saved veneer like `_call_via_r5`) does not rescue it. Consistent with the m4a.c / m4a_asm.s interleave this chapter already describes: the algorithm is the canonical MP2K TrackStop, hand-written.",
  "sub_080702C0": "ply_endtie, 64 bytes, in the m4a span, and a LEAF (`push {r4, r5}` ... `pop {r4, r5}; bx lr`, no `lr` saved). WAVE 47 (W47-A) entered it on its own evidence, per the deferral in docs/agbcc-codegen.md. Two independent tells, one mnemonic and one structural. (1) `tst r2, r4` and `tst r2, r5` -- two of them, in the channel-walk loop. agbcc emits no THUMB `tst` at all (zero in 959 promoted units; eleven spellings probed across two waves), only `and` + `cmp #0`. (2) `cmp r3, #0x80; bhs` on the command byte. This one is NOT a mnemonic quibble: GCC canonicalises an unsigned comparison against a constant to the adjacent constant, so `key < 0x80` compiles to `cmp #0x7f; bhi` and the inverted `key >= 0x80` to `cmp #0x7f; bls` -- both probed, both 0x7f. BOTH constants fit an imm8, so nothing about THUMB encoding forces the choice; agbcc simply never emits the 0x80 form. (Writing `key` as `u8` instead gives a third wrong answer, `movs r0,#0; ldrsb r0,[r2,r0]; cmp r0,#0; blt` -- a sign test.) Corroborating, not load-bearing: the ROM keeps the 'found' block as the loop body's fall-through with a forward `b` to the epilogue, while agbcc hoisted it out of the loop and branched backward into it at all three spellings tried (`return`, `break`, and a `for` loop with the advance in the third clause), which also costs a copy of `chan->status`. work/sub_080702C0/sub_080702C0.c reproduces the whole algorithm and every memory access at the right offset; its residual is exactly these three things -- the two `tst`s, the 0x7f-vs-0x80 compare, and the block layout (which is what the +12 bytes are: one extra copy of `chan->status` and one extra hoisted 0x40)."
 }
}
