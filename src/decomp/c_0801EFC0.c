#include "global.h"

/* Promoted from assembly; each function below is byte-for-byte
 * identical to the original. Order is address order and must
 * stay that way -- the linker places this file's .text as one
 * contiguous block at 0x0801EFC0.
 * sub_0801EFC0 @ 0x0801EFC0
 */

/* MATCHED. Byte-for-byte the same function as sub_0801E0A4 -- identical
 * instruction stream and identical pool words. One C body, two
 * addresses; read that one for the derivation. */
void sub_0801EFC0(void)
{
    sub_08011C90(gUnknown_03002520, (void *)0x07000000, 0x400);
}
