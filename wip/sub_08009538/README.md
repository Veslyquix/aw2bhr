# sub_08009538

0x08009538, 488 bytes, THUMB, parked.

Best score so far: 34.4%, +52 bytes (best.c).

## Files

- `sub_08009538.c`: the current draft
- `best.c`: the closest attempt, when it is not the draft
- `target.s`: the original assembly

## What has been tried

From `data/parked.json`.

PARKED wave 37 (W37-F). Control flow fully settled -- all four tile cases (0xFC/0xFD/0x11C/0x11D), every `||` neighbour pair and every guard match the ROM instruction for instruction. REMAINING DIFF is a single register-allocation inversion: the ROM has r5=x, r6=y, r7=ok, r8=the kept neighbour coordinate, sb=the gUnknown_0808D818 pool address; the candidate puts `ok` in r8 and the kept neighbour in r7, so every `ok = 1` costs `movs rN,#1; mov r8,rN` instead of `movs r7,#1` and every `if (ok == 0)` costs `mov r1,r8; cmp r1,#0` instead of `cmp r7,#0`. CORRECTED wave 80 (W80-A): this is NOT a bare tie and the instruction order is NOT already correct. Read by instruction, every case's first check in the ROM is `subs r4,rY,#1 / adds r0,.. / adds r1,r4,#0 / bl / mov r8,r4 / cmp r0,#0 / beq / movs r7,#1` -- an EXTRA COPY `mov r8,r4` after the call that no allocation of one pseudo produces. The kept coordinate is TWO pseudos: a per-case scratch that is the call argument, and a second variable assigned from it AFTER the call and before the result is tested. With one per-case pseudo (the wave-37 draft) there is no copy, `ok` loses r7, and the symmetric cases cross-jump wholesale: 448 bytes (-40), 18.0%, not size-exact. `n = y - 1; r = f(x, n); keep = n; if (r) ok = 1;` reproduces the copy exactly (draft now at work/sub_08009538/sub_08009538.c, 456 bytes, 17.4%). RULED OUT (all leave `ok` in r8): nx/ny at function scope (wave 37); nx/ny declared per case (wave 37); one function-scope kept variable plus a per-case scratch without the post-call copy (wave 37); `u8 ok` (wave 37); function-scope nx/ny WITH the post-call copy -- the kept value takes r5 ahead of x and y (wave 80); ONE function-scope `keep` shared by all four cases WITH the copy -- keep r7, ok r8, still inverted (wave 80). What the ROM needs is `ok` outranking `keep` on allocno_compare priority (refs*log2(refs)/live_length); no measured spelling does that without adding instructions. Seven compiler profiles swept by the wave-80 orchestrator: none helps. CONFIRMED, do not re-derive: the tail is `r = 0; if (ok == 0) r = 1; return r;` and NOT `return ok == 0;` (do_store_flag would add an `adds r0, rN, #0` the ROM lacks); cases 0xFD and 0x11D share their last block at _080096DC purely by cross-jumping from symmetric source; the `beq L; b far` pairs are Thumb conditional-branch RANGE expansions (the candidate emits the same `@long jump` sequences), not source; the `if (ok == 0) { if (A) {...} if (ok == 0 && B) {...} }` nesting is forced because A's false edge goes to the next guard while the outer test's and B's both go to the exit. PERMUTER (wave 80, W80-A): run 300 s / 4 threads from the valid draft after work/sub_08009538/best.c was found holding a SEMANTICALLY INVALID 46.5% variant (`ok = tile; switch (ok)` plus a `volatile long nx`; snapshot kept as scratch). Result: best 34.4% at +56 bytes, i.e. nothing; best.c now holds that +56-byte variant, so do NOT chain from it -- copy the draft over best.c first. NEXT STEP: a spelling in which `ok` has more references or a shorter live range than the shared kept coordinate, with the post-call copy preserved. Needs "rodata": ["0x0808D818"] on promotion.
